---
phase: 10-research-page
plan: 02
type: execute
wave: 2
depends_on: ["10-01"]
files_modified:
  - src/components/ResearchClient.tsx
  - messages/en.json
  - messages/zh-HK.json
autonomous: true
requirements: [RES-01]

must_haves:
  truths:
    - "/en/research and /zh-HK/research render the crypto-valuation explainer covering HKD-equivalent valuation, unit-segmentation, the stale/missing-rate fail-safe, base un-staked tier, and the hkEligible fail-closed gate (RES-01, D-02)"
    - "The crypto copy is accurate to valuateCrypto.ts + Phase 7 07-VERIFICATION.md — no invented behavior (D-02)"
    - "The FULL research namespace (shell + ranking + crypto) has en/zh-HK key parity — no missing-string fallback on either locale (D-06, Phase 10 success criterion 3)"
  artifacts:
    - crypto-valuation explainer section in src/components/ResearchClient.tsx
    - research.crypto keys in messages/en.json and messages/zh-HK.json
  key_links:
    - "ResearchClient crypto section → research.crypto keys (both locales)"
    - "The whole research namespace passes the nested-aware en/zh-HK parity gate"
---

<objective>
Add the second required explainer — "how crypto rewards are valued in HKD" — to the Research page, accurate to the Phase 7 valuation engine, and close the phase with a full-namespace bilingual parity gate.

Depends on 10-01 (the route, `ResearchClient`, and `research` namespace must exist). This is the horizontal EXPANSION of the tracer: it adds one more editorial section to the proven slice and completes bilingual parity across the whole namespace. Content stays plain React + next-intl — NO CMS, NO MDX, NO new dependencies (D-03, RES-01). Same v2 editorial layout as 10-01 (D-04). Static content: no engine/data-layer calls — the crypto copy describes what `valuateCrypto.ts` does, it does not call it.

Purpose: let a visitor understand, in either language, how crypto-card rewards are turned into an HKD-equivalent value so they can be compared honestly beside fiat cards, and why an unobtainable card is never recommended.
Output: the crypto-valuation section in `ResearchClient` + `research.crypto` keys (both locales) + a full-namespace parity gate proving Phase 10 success criterion 3.
</objective>

<execution_context>
@/Users/jeffreyng/.claude/gsd-core/workflows/execute-plan.md
@/Users/jeffreyng/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-research-page/10-CONTEXT.md
@.planning/design/ui-contract-v2.md
@.planning/phases/10-research-page/10-01-SUMMARY.md
</context>

<artifacts_this_phase_produces>
This plan (10-02) produces:
- The crypto-valuation explainer `<section>` in `src/components/ResearchClient.tsx` (hairline-separated, same v2 editorial layout as the ranking section).
- A `research.crypto` sub-object in BOTH `messages/en.json` and `messages/zh-HK.json` with real Traditional-Chinese copy.
- A completed, full-namespace en/zh-HK parity gate over the entire `research` namespace (shell + ranking from 10-01 + crypto from this plan).

Together with 10-01, this completes Phase 10: a bilingual Research page with both required explainers, reachable from the nav, in the v2 design system.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Render the crypto-valuation explainer section + author accurate bilingual copy</name>
  <files>src/components/ResearchClient.tsx, messages/en.json, messages/zh-HK.json</files>
  <read_first>
    - `src/lib/engine/valuateCrypto.ts` — the ACCURACY SOURCE (D-02). Note the real model: `hkdEquivalent = rewardAmount × hkdPerUnit` from an INJECTED static rate table (the engine never fetches); a stale rate (older than the 24h threshold) keeps the last-known value and flags staleness (the card is NOT dropped for mere staleness); a missing asset / absent-from-table / non-positive / NaN rate yields `hkdEquivalent: null` ("value unavailable", not ranked, never fabricated, never thrown).
    - `.planning/phases/07-crypto-hkd-valuation-engine-hkeligible-gate/07-VERIFICATION.md` — the Phase 7 verified behavior: unit-segmented beside fiat (partition-before-sort so the fiat ranking is byte-identical / unaffected), fail-safe on stale/missing rates, base un-staked tier by default (staking-gated higher tiers surface as conditional, not assumed-met), `hkEligible` fail-closed inside `recommendCards()` (a card an HK resident can't obtain is never recommended, though the directory still lists it).
    - `src/lib/engine/recommendCards.ts` — lines ~74-83 (partition-before-sort: `cardType === 'credit'` is fiat; crypto is valued separately) and ~62-70 (the `hkEligible === false` fail-closed gate). These confirm the "fiat ranking is unaffected" and "unobtainable cards never surface" claims.
    - `src/components/ResearchClient.tsx` (from 10-01) — the section/heading pattern to mirror for the new crypto section; add it after the ranking section with a hairline rule between.
    - `.planning/design/ui-contract-v2.md` §1-§3 — tokens + editorial layout; if any numeric example is shown, use Geist Mono with `tabular-nums` (contract §2).
  </read_first>
  <action>
    Add a crypto-valuation `<section>` to `ResearchClient.tsx` (after the ranking section, separated by a 1px `--border` hairline rule, same v2 editorial layout), and add a `research.crypto` sub-object to BOTH message files. Cover, in plain consumer language but faithful to `valuateCrypto.ts` + Phase 7 (D-02):
      1. HKD-equivalent valuation — a crypto reward (e.g. USDC/CRO) is converted to an HKD-equivalent value using a maintained rate table, so crypto rewards can be compared in the same currency a Hong Kong user thinks in.
      2. Unit-segmentation — crypto cards are valued and shown in their own segment beside fiat cards; this is deliberate so the fiat ranking is never changed by crypto valuation.
      3. Fail-safe on rates — if a rate is stale, CardGPT shows the last known value with an "as of <time>" marker rather than hiding the card; if a rate is missing or unusable, the reward shows as "value unavailable" and is never ranked #1 on a guessed number.
      4. Base tier by default — staking- or holding-gated higher reward tiers are NOT assumed; the base, un-staked rate is used by default and higher tiers are shown as conditional.
      5. HK-availability gate — a card a Hong Kong resident cannot obtain (`hkEligible` false) is never returned as a recommendation, even though it still appears in the card directory.
    Do NOT invent behavior beyond the source (no live-price claims — the rate table is maintained/injected, not fetched live by the engine; no precision guarantees). Add all keys under `research.crypto` (e.g. `heading`, `lead`, `hkdValuation`, `segmentation`, `failSafe`, `baseTier`, `hkEligibility`) and author REAL Traditional-Chinese copy for each — full parity, no placeholders. Any bulleted list rendered from an array must be a parallel array of equal length in both locales.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
    <automated>node -e "const e=require('./messages/en.json').research.crypto,z=require('./messages/zh-HK.json').research.crypto;if(!e||!z)throw new Error('research.crypto missing in a locale');console.log('crypto keys present in both locales')"</automated>
    <automated>npm test</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` exits 0.
    - `research.crypto` exists in BOTH locales (node check passes).
    - `npm test` stays green.
    - Observable: `/en/research` crypto section names HKD-equivalent valuation, unit-segmentation, the stale/missing-rate fail-safe, base un-staked tier, and the hkEligible gate — each traceable to `valuateCrypto.ts` / Phase 7 (no invented behavior).
    - Observable: `/zh-HK/research` renders the same crypto content in Traditional Chinese (繁體).
  </acceptance_criteria>
  <done>The crypto-valuation explainer is present, accurate to Phase 7 / valuateCrypto.ts, rendered in the v2 editorial layout, and bilingual; committed.</done>
</task>

<task type="auto">
  <name>Task 2: Full research-namespace parity gate + build verification (Phase 10 success criterion 3)</name>
  <files>messages/en.json, messages/zh-HK.json</files>
  <read_first>
    - `.planning/phases/10-research-page/10-CONTEXT.md` — D-06 (every new string in BOTH message files, real Traditional-Chinese copy, key parity enforced) and Phase 10 success criterion 3 (both locales have full key parity — no missing-string fallback).
    - `messages/en.json` + `messages/zh-HK.json` — the now-complete `research` namespace (shell + ranking + crypto). This task is the closing gate; if it fails, the fix is to add the missing/real zh-HK string, never to delete an en key.
  </read_first>
  <action>
    Run the nested-aware parity gate over the WHOLE `research` namespace and confirm every en key has a real Traditional-Chinese counterpart (D-06). Confirm no key is an English placeholder in `zh-HK.json` (spot-check the crypto + ranking bodies read as 繁體, not English). Confirm `/[locale]/research` still builds and both explainer sections render in both locales. This task adds no new content — it is the verification checkpoint that closes RES-01 success criterion 3; if the deep-parity check reports a mismatch, add the missing real zh-HK string and re-run until green.
  </action>
  <verify>
    <automated>node -e "const e=require('./messages/en.json').research,z=require('./messages/zh-HK.json').research;function ks(o,p=''){return Object.entries(o).flatMap(([k,v])=>v&&typeof v==='object'?ks(v,p+k+'.'):[p+k])}const ek=ks(e).sort(),zk=ks(z).sort();const miss=ek.filter(k=>!zk.includes(k)),extra=zk.filter(k=>!ek.includes(k));if(miss.length||extra.length)throw new Error('research parity mismatch missing='+JSON.stringify(miss)+' extra='+JSON.stringify(extra));console.log('research parity ok',ek.length)"</automated>
    <automated>npx tsc --noEmit</automated>
    <automated>npx next build 2>&1 | grep -i research</automated>
    <automated>npm test</automated>
  </verify>
  <acceptance_criteria>
    - Deep-parity gate prints "research parity ok N" — the full `research` namespace (shell + ranking + crypto) has identical nested-aware key sets across en and zh-HK, zero missing, zero extra.
    - `npx tsc --noEmit` exits 0.
    - `npx next build` compiles and still lists `/[locale]/research`.
    - `npm test` green.
    - Observable: neither `/en/research` nor `/zh-HK/research` shows a raw key or English fallback in the zh-HK view — Phase 10 success criterion 3 holds.
  </acceptance_criteria>
  <done>The entire research namespace has full en/zh-HK parity with real Traditional-Chinese copy; the Research page builds and renders both explainers in both locales; Phase 10 success criterion 3 is verified; committed.</done>
</task>

</tasks>

<verification>
- Full nested-aware en/zh-HK parity on the `research` namespace (shell + ranking + crypto) passes.
- `npx tsc --noEmit` clean; `npx next build` lists `/[locale]/research`; `npm test` green.
- No new dependencies (RES-01 / D-03).
- Human sign-off (non-blocking): after execution, view `/en/research` and `/zh-HK/research` in both light and dark mode at localhost and confirm the copy reads well and the layout matches the v2 editorial system — this is Jeff's visual sign-off per 10-CONTEXT discretion. (Recorded as a `<human-check>`, not a blocking checkpoint.)
- No `<threat_model>`: static public content, no trust boundaries, no new inputs/deps.
</verification>

<success_criteria>
The Research page includes a crypto-valuation explainer accurate to Phase 7 / `valuateCrypto.ts`; both required explainers render in both locales; the whole `research` namespace has full en/zh-HK key parity with real Traditional-Chinese copy (Phase 10 success criterion 3).
</success_criteria>

<output>
Create `.planning/phases/10-research-page/10-02-SUMMARY.md` when done.
</output>
