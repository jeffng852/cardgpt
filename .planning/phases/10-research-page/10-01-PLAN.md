---
phase: 10-research-page
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/[locale]/research/page.tsx
  - src/components/ResearchClient.tsx
  - src/components/HomeClient.tsx
  - messages/en.json
  - messages/zh-HK.json
autonomous: true
requirements: [RES-01]

must_haves:
  truths:
    - "/en/research and /zh-HK/research render the ranking-methodology explainer in the v2 design system, chrome reused (RES-01, D-04)"
    - "A header 'Research' nav link routes to /[locale]/research (D-05)"
    - "The ranking copy is accurate to recommendCards.ts + transactionParser.ts — net value = reward − fees, the 5-level tie-break chain, preference filtering, bilingual free-text parsing (D-01)"
    - "The research namespace has en/zh-HK key parity with real Traditional-Chinese copy — no missing-string fallback (D-06)"
  artifacts:
    - src/app/[locale]/research/page.tsx
    - src/components/ResearchClient.tsx
    - research namespace (page shell + ranking keys) in messages/en.json and messages/zh-HK.json
    - Research nav link in src/components/HomeClient.tsx
  key_links:
    - "HomeClient 'Research' Link href='/research' → the new /[locale]/research route"
    - "ResearchClient useTranslations('research') → messages/{en,zh-HK}.json research namespace"
    - "server page.tsx renders <ResearchClient/>"
---

<objective>
Stand up the public bilingual Research route end-to-end in the live v2 design system, and deliver the first of two required explainers — "how CardGPT ranks" — accurate to the real engine, not marketing.

This is the phase TRACER: one path through every layer this phase touches — a new App-Router route → a client chrome shell reusing the v2 header/logo/toggle/switcher → the next-intl `research` namespace in both locales → the "Research" nav entry wired into the app header. Proving this slice renders in both locales AND both themes de-risks the whole phase before the crypto explainer expands it.

Content is plain React + next-intl message keys — NO CMS, NO MDX, NO new dependencies (D-03, RES-01). Static content page: no engine or data-layer calls (the "accuracy" comes from writing copy that matches `recommendCards.ts`, not from calling it). Built into the v2 brutalist-editorial system (D-04): uppercase Rethink display headings, Inter body at a comfortable reading measure, hairline section rules, flat/square, dark mode via the existing next-themes `.dark` class.

Purpose: give an HK visitor a plain-language but truthful account of how the recommender ranks cards, in English or Traditional Chinese, and make it reachable from the app nav (D-05).
Output: `/[locale]/research` route + `ResearchClient` chrome + ranking-methodology section + `research` i18n namespace (both locales) + the "Research" header link.
</objective>

<execution_context>
@/Users/jeffreyng/.claude/gsd-core/workflows/execute-plan.md
@/Users/jeffreyng/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-research-page/10-CONTEXT.md
@.planning/design/ui-contract-v2.md
</context>

<artifacts_this_phase_produces>
This plan (10-01) produces:
- `src/app/[locale]/research/page.tsx` — new server component route (mirrors `cards/page.tsx`: a thin server shell that renders the client component). NO data-layer call.
- `src/components/ResearchClient.tsx` — new client component: v2 chrome (header with Logo home-link, Directory link, Research active state, DarkModeToggle, LanguageSwitcher) + editorial `<main>` long-form reading layout + footer; renders the page intro and the ranking-methodology section from the `research` namespace.
- `research` namespace added to BOTH `messages/en.json` and `messages/zh-HK.json` — page eyebrow/title/intro + ranking-methodology section (heading, lead, the net-value explanation, the 5-level tie-break chain, preference filtering, the bilingual "type a purchase" parsing explanation). Real zh-HK copy, full parity.
- "Research" nav link added to `src/components/HomeClient.tsx` header (mirrors the Phase 9 "Directory" link).

Plan 10-02 (wave 2) expands `ResearchClient.tsx` + the `research` namespace with the crypto-valuation explainer and the full-namespace parity gate.
</artifacts_this_phase_produces>

<tasks>

<task type="tracer">
  <name>Task 1: Research route + v2 chrome shell + nav wiring + ranking section rendering (one path, both locales)</name>
  <files>src/app/[locale]/research/page.tsx, src/components/ResearchClient.tsx, src/components/HomeClient.tsx, messages/en.json, messages/zh-HK.json</files>
  <read_first>
    - `.planning/design/ui-contract-v2.md` — the LIVE design system: §2 typography (Rethink Sans uppercase display, Inter body, Geist Mono for any numerics), §3 shape/spacing (square/flat, 1px `--border` hairlines, 2px `--border-strong` emphasis, no shadows), §1 tokens (bg/fg/muted-fg/border/brand). This section is the editorial long-form reading surface.
    - `src/app/[locale]/cards/page.tsx` — the STRUCTURAL TEMPLATE: a thin `async` server component with `export const dynamic` and no i18n plumbing of its own, rendering a single client component. `research/page.tsx` mirrors this shape (a static content page needs no `force-dynamic` — omit it or keep it consistent with the app; do NOT call `loadCards`/any engine).
    - `src/components/CardDirectoryClient.tsx` — the CHROME + client-component pattern to mirror: `'use client'`, `useTranslations('<namespace>')`, imports `Link/useRouter/usePathname` from `@/i18n/routing`, `Logo`, `DarkModeToggle`, `LanguageSwitcher`; sticky hairline-bottom header (`border-b border-border bg-bg`), `max-w-7xl` container, uppercase display `h1`. Copy this header structure.
    - `src/components/HomeClient.tsx` — lines ~82-90: the existing `<Link href="/cards">` "Directory" nav button (reads `tDirectory('navLink')`). Add the "Research" link RIGHT NEXT TO IT using the identical button classes and a new `research('navLink')` key.
    - `messages/en.json` + `messages/zh-HK.json` — top-level namespaces are siblings (`directory`, `cardDetail`, …); add a NEW sibling `research` namespace to BOTH. Study the `directory` namespace as the shape/parity model.
  </read_first>
  <action>
    Create `src/app/[locale]/research/page.tsx` as a server component that renders `<ResearchClient/>` (mirror `cards/page.tsx`'s thin-server-shell shape; this is static content, so make NO `loadCards`/engine/data-layer call — per RES-01 / D-03 this is a next-intl content page).

    Create `src/components/ResearchClient.tsx` as a `'use client'` component using `useTranslations('research')`. Reuse the v2 chrome exactly as `CardDirectoryClient` does (D-04, D-05): a sticky hairline-bottom header with a `Logo` home-`Link`, a "Directory" `Link` to `/cards`, a "Research" affordance shown in its active state (mint-filled per contract §5 nav rule since this IS the research page), then `DarkModeToggle` + `LanguageSwitcher`. Below the header, an editorial `<main>` with a constrained reading measure (e.g. `max-w-3xl` prose column inside a `max-w-7xl` frame), a page eyebrow + uppercase Rethink display title + Inter lead intro, then the ranking-methodology `<section>` separated by a 1px `--border` hairline rule. Use uppercase Rethink display for section headings, Inter for body at 15-17px with relaxed leading, square/flat/no-shadow surfaces, tokens only (`text-fg`/`text-muted-fg`/`border-border`) so dark mode flips for free via the next-themes `.dark` class. Add a footer or reuse the hairline-top footer pattern (Claude's discretion — keep it consistent with the other pages). Do NOT hand-write copy strings in the component — every visible string reads from the `research` namespace.

    In `src/components/HomeClient.tsx`, add a "Research" nav `Link href="/research"` immediately beside the existing Directory link in the header, using the identical button classes, reading `useTranslations('research')` → `navLink` (D-05).

    In BOTH `messages/en.json` and `messages/zh-HK.json`, add the `research` namespace with the page shell keys and the ranking-section STRUCTURE keys this task renders: `navLink`, `pageEyebrow`, `pageTitle`, `pageIntro`, and a `ranking` sub-object with `heading` + `lead` (the detailed ranking body copy is authored in Task 2, but wire the section to render `ranking.heading` + `ranking.lead` now so the tracer shows a real section). Author REAL Traditional-Chinese copy for every key in `zh-HK.json` — no English placeholders. Keep en/zh-HK key sets identical.

    Verify the route renders in BOTH locales AND both themes before finishing (this is the tracer's end-to-end proof).
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
    <automated>npx next build 2>&1 | grep -E "/\[locale\]/research|●|ƒ" | grep -i research</automated>
    <automated>node -e "const e=require('./messages/en.json').research,z=require('./messages/zh-HK.json').research;function ks(o,p=''){return Object.entries(o).flatMap(([k,v])=>v&&typeof v==='object'?ks(v,p+k+'.'):[p+k])}const ek=ks(e).sort(),zk=ks(z).sort();const miss=ek.filter(k=>!zk.includes(k)),extra=zk.filter(k=>!ek.includes(k));if(miss.length||extra.length)throw new Error('research parity mismatch missing='+JSON.stringify(miss)+' extra='+JSON.stringify(extra));console.log('research parity ok',ek.length)"</automated>
    <automated>npm test</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` exits 0.
    - `npx next build` compiles and the build output lists `/[locale]/research` as a route.
    - `node` deep-parity check prints "research parity ok N" — the `research` namespace has identical (nested-aware) key sets across en and zh-HK.
    - `npm test` stays green (no regressions to the existing 14-file vitest suite).
    - Observable: `/en/research` renders the v2 chrome header + a ranking-methodology section; the header "Research" affordance shows active state.
    - Observable: `/zh-HK/research` renders the same layout with Traditional-Chinese (繁體) copy, no English fallback.
    - Observable: the home header "Research" link navigates to `/[locale]/research` (D-05).
    - Observable: toggling dark mode flips the page via the `.dark` class (tokens only, no hardcoded colors).
  </acceptance_criteria>
  <done>The `/[locale]/research` route renders end-to-end in both locales and both themes with the v2 chrome and a ranking section; the "Research" nav link routes there; the research namespace has en/zh-HK parity; committed.</done>
</task>

<task type="auto">
  <name>Task 2: Author the ranking-methodology copy accurate to the engine (net value, tie-breaks, preference filter, bilingual parsing)</name>
  <files>src/components/ResearchClient.tsx, messages/en.json, messages/zh-HK.json</files>
  <read_first>
    - `src/lib/engine/recommendCards.ts` — the ACCURACY SOURCE (D-01). Note the real algorithm: filter inactive/excluded/`hkEligible===false` cards → apply preference filters (preferred reward units, max annual fee) → calculate rewards → optional min-reward-rate filter → SORT BY NET VALUE (reward minus fees) with a deterministic 5-level tie-break chain (lines ~125-154): (1) higher net value, (2) higher reward amount, (3) lower annual fee, (4) preferred issuer, (5) alphabetical by card name; top card marked "Recommended". Describe THIS, not invented marketing.
    - `src/lib/parser/transactionParser.ts` — the bilingual free-text → structured `Transaction` parser (amount, currency, category, merchant, payment type) with per-field confidence, English + Traditional-Chinese keyword tables. This is the "type a purchase" explanation.
    - `.planning/phases/10-research-page/10-CONTEXT.md` — D-01 (content is accurate-to-code, plain-language but true, NO invented claims) + the discretion note on section breakdown.
    - `.planning/design/ui-contract-v2.md` §2 — keep body copy in Inter at a readable measure; use uppercase Rethink only for sub-headings.
  </read_first>
  <action>
    Author the full ranking-methodology body into the `research.ranking` sub-object in BOTH message files, and render it in `ResearchClient.tsx` as editorial prose (Inter body, optional hairline-separated sub-blocks with uppercase Rethink sub-headings). Cover, in plain consumer language but faithful to `recommendCards.ts` + `transactionParser.ts` (D-01):
      1. Bilingual free-text parsing — a visitor types a purchase in English or Traditional Chinese and CardGPT reads amount, currency, category, merchant, and payment type from it (the "type a purchase" step).
      2. Net-value ranking — cards are ranked by net value, defined as reward earned minus fees, so a high-reward card with a high foreign-transaction fee correctly loses to a cheaper one. Give the FX-fee example.
      3. The deterministic 5-level tie-break chain, in order: net value, then reward amount, then lower annual fee, then a preferred issuer, then alphabetical — so equal cards never order arbitrarily.
      4. Preference filtering — reward-unit preference, maximum annual fee, and excluded cards narrow the candidate set before ranking; the top result is the recommendation.
    Do NOT invent claims the engine does not make (no "AI-powered scoring", no accuracy percentages). Keep every number/behavior traceable to the source files. Add all new keys to `research.ranking` (e.g. `parsing`, `netValue`, `netValueExample`, `tieBreak` with the ordered steps, `preferences`) and author REAL Traditional-Chinese copy for each — full en/zh-HK parity, no placeholders. If the tie-break steps are rendered as a list, model them as a parallel array in both locales so key/index parity holds.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
    <automated>node -e "const e=require('./messages/en.json').research,z=require('./messages/zh-HK.json').research;function ks(o,p=''){return Object.entries(o).flatMap(([k,v])=>v&&typeof v==='object'?ks(v,p+k+'.'):[p+k])}const ek=ks(e).sort(),zk=ks(z).sort();const miss=ek.filter(k=>!zk.includes(k)),extra=zk.filter(k=>!ek.includes(k));if(miss.length||extra.length)throw new Error('research parity mismatch missing='+JSON.stringify(miss)+' extra='+JSON.stringify(extra));console.log('research parity ok',ek.length)"</automated>
    <automated>npm test</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` exits 0.
    - Deep-parity check prints "research parity ok N" with the expanded `research.ranking` keys present in BOTH locales.
    - `npm test` stays green.
    - Observable: `/en/research` ranking section names net value = reward − fees, the ordered 5-level tie-break chain, preference filtering, and the bilingual "type a purchase" parsing step — each traceable to `recommendCards.ts` / `transactionParser.ts` (no invented marketing).
    - Observable: `/zh-HK/research` renders the same ranking content in Traditional Chinese (繁體), full parity.
  </acceptance_criteria>
  <done>The ranking-methodology explainer is complete, accurate to the engine, rendered in the v2 editorial layout, and bilingual with full parity; committed.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` clean (repo policy: `npm install` first; `npx next build` verifies the build since `npm run build` is health-check-gated).
- `npx next build` compiles and lists `/[locale]/research` as a route.
- Deep en/zh-HK parity on the `research` namespace passes (nested-aware).
- `npm test` green (14-file vitest suite unchanged).
- No new dependencies added (RES-01 / D-03): no CMS, no MDX, no markdown lib — grep `package.json` diff is empty.
- No `<threat_model>`: this is static public content — no trust boundaries, no new inputs, no new deps, no data-layer/engine surface.
</verification>

<success_criteria>
The `/[locale]/research` route exists and renders in both en and zh-HK with the v2 chrome; the ranking-methodology explainer is present and accurate to `recommendCards.ts` + `transactionParser.ts`; the "Research" header nav link routes to it; the `research` namespace has full en/zh-HK key parity with real Traditional-Chinese copy.
</success_criteria>

<output>
Create `.planning/phases/10-research-page/10-01-SUMMARY.md` when done.
</output>
