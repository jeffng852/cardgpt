---
phase: 09-data-page-card-directory
plan: 02
type: execute
wave: 2
depends_on: [09-01]
files_modified:
  - src/app/[locale]/cards/[id]/page.tsx
  - src/lib/directory/cardProvenance.ts
  - src/lib/directory/__tests__/cardProvenance.test.ts
  - src/components/CardRecommendationList.tsx
autonomous: true
requirements: [DIR-01, DIR-02, DIR-03]

must_haves:
  truths:
    - "Visiting /[locale]/cards/{knownId} renders a dedicated detail view (D-02) with the card's full reward rules, fees, and an Apply CTA — read-only, no recommendCards() call (DIR-01)."
    - "Visiting /[locale]/cards/{unknownId} triggers Next notFound() (404) rather than crashing or rendering an empty card (D-02)."
    - "The detail view surfaces HK-availability (from hkEligible), a last-verified date (from card data / DB metadata), and a provenance note (DIR-02b / DEC-DATA-001)."
    - "The Apply CTA reuses the Phase 8 affiliate treatment: applyCtaProps(url) -> rel='sponsored nofollow noopener', rendered only when applyUrl exists (AFF-01 continuity)."
    - "Each recommendation result in CardRecommendationList deep-links to /[locale]/cards/{card.id} (DIR-03)."
  artifacts:
    - "src/app/[locale]/cards/[id]/page.tsx (server component, force-dynamic, getCardById + notFound)"
    - "src/lib/directory/cardProvenance.ts (pure DIR-02b label derivation)"
    - "src/lib/directory/__tests__/cardProvenance.test.ts"
    - "A deep-link from CardRecommendationList result cards into the detail route (DIR-03)"
  key_links:
    - "loadCards() -> findCardById(cards, id) (from Plan 01) -> notFound() when null — the ungated resolution path."
    - "cardProvenance(card, metadata) -> DIR-02b labels rendered on the detail page."
    - "CardRecommendationList result -> Link href=/cards/{card.id} — the DIR-03 deep-link, matching the Plan 01 tile href contract."
  deferred:
    - "DIR-02 per-card grid BADGES — banner+detail labels only in v1 (badges deferred)."
    - "Crypto-specific detail sections (staking tiers, per-asset surfacing) — deferred until real crypto card data lands."
---

<objective>
Expand the directory into a dedicated per-card **detail route** `/[locale]/cards/[id]` (D-02) that renders a
card's full reward rules, fees, and affiliate Apply CTA, honestly labeled with HK-availability, a last-verified
date, and a provenance note (DIR-02b) — and wire the recommender→detail **deep-link** (DIR-03) so a
recommendation result opens the card's detail view.

Purpose: Deliver the shareable/SEO-friendly detail contract the phase goal hangs on (the deep-link target),
with honest provenance labeling for the accepted bulk data (DEC-DATA-001), reusing Phase 8's affiliate CTA
treatment rather than re-deriving it.
Output: The detail route, a pure provenance-label helper + tests, and the DIR-03 deep-link in the recommender.
</objective>

<execution_context>
@/Users/jeffreyng/.claude/gsd-core/workflows/execute-plan.md
@/Users/jeffreyng/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-data-page-card-directory/09-CONTEXT.md
@.planning/phases/09-data-page-card-directory/09-01-SUMMARY.md
</context>

<artifacts_this_phase_produces>
New route: `/[locale]/cards/[id]` (card detail). New pure module `src/lib/directory/cardProvenance.ts`
(DIR-02b label derivation) + test. Modified: `CardRecommendationList.tsx` gains a per-result deep-link into
the detail route (DIR-03). Consumes (does not create): `findCardById` from Plan 01's cardDirectory.ts, the
`directory.detail.*` i18n keys added by Plan 01, and `applyCtaProps` from Phase 8.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Card detail route with provenance labeling and Apply CTA</name>
  <files>src/app/[locale]/cards/[id]/page.tsx, src/lib/directory/cardProvenance.ts</files>
  <read_first>
    - src/app/[locale]/page.tsx — the `force-dynamic` + `await loadCards()` server-component pattern.
    - src/app/[locale]/layout.tsx — how `params` is a Promise (`const { locale } = await params`); the [id] route destructures `{ locale, id }` the same way, and uses `notFound` from `next/navigation`.
    - src/lib/data/loadCards.ts — `loadCards()`, `getCardById(id)`, and `getDatabaseMetadata()` (lastUpdated/nextReviewDate for last-verified).
    - src/lib/directory/cardDirectory.ts (from Plan 01) — `findCardById(cards, id)` returns the card or null; use it to drive `notFound()`.
    - src/components/CardRecommendationList.tsx — the reward-rule / fees rendering vocabulary (rate formatting `(rate*100).toFixed(1)`, breakdown rows, fees block, the free/annual-fee treatment) AND the Apply CTA block at lines ~460-474 using `applyCtaProps(card.applyUrl)!` with `rel="sponsored nofollow noopener"` — reuse this exact CTA treatment (D-05 / AFF-01), only when `card.applyUrl` exists.
    - src/lib/affiliate/applyCtaProps.ts — the helper's contract (returns null when no url).
    - src/types/card.ts — CreditCard.rewards[], fees, hkEligible, rewardPrograms, lastUpdated.
    - src/app/[locale]/privacy/page.tsx — header + back-`Link` composition for a secondary [locale] page.
  </read_first>
  <action>
    Create `src/app/[locale]/cards/[id]/page.tsx` as a server component: `export const dynamic = 'force-dynamic'`; `const { locale, id } = await params`; `const cards = await loadCards()`; `const card = findCardById(cards, id)`; if `card` is null call `notFound()` (D-02 unknown-id handling). Also `await getDatabaseMetadata()` for the last-verified fallback date.

    Render the detail view reusing the CardRecommendationList visual vocabulary (D-08 — clean/conventional, Phase 11 restyles): a sticky header with Logo + a locale-preserving back-`Link` to `/cards` (label `directory.backToDirectory`); the card image (getCardImageUrl/hasCardImage with the SVG placeholder fallback), name, issuer, and card-type badge; the **full reward-rule list** (each rule's description — use `description_zh` when locale is zh-HK — priority tag, rate as a percentage, cap/validUntil/actionRequired where present); the **fees** block (annual fee with the free treatment, foreignTransactionFeeRate, redemptionFee); and, only when `card.applyUrl` is set, the Apply CTA anchor spread from `applyCtaProps(card.applyUrl)!` (rel `sponsored nofollow noopener`, target `_blank`) — identical treatment to CardRecommendationList.

    Add a **provenance section** (DIR-02b / DEC-DATA-001) rendering the output of `cardProvenance(card, metadata)`: an HK-availability label (`directory.detail.hkAvailable` vs `directory.detail.hkNotAvailable`), a last-verified date (`directory.detail.lastVerified` + the date), and a provenance note (`directory.detail.provenanceNote`) stating some card data is bulk/community-sourced. Do NOT call `recommendCards()` — read-only (DIR-01). All strings come from `useTranslations`/`getTranslations` on the `directory` namespace (keys already exist from Plan 01). Server components read messages via `next-intl/server` `getTranslations`.

    Create `src/lib/directory/cardProvenance.ts` exporting a pure `cardProvenance(card, metadata)` that returns a plain object of the DIR-02b facts (no i18n, no JSX): `{ hkAvailable: boolean; lastVerified: string; }`. Rule for `hkAvailable`: `card.hkEligible === false` → `false`; otherwise (`true` or `undefined`) → `true` (matches the Phase 7 fail-closed-but-undefined-eligible convention — undefined legacy cards are HK-available). Rule for `lastVerified`: prefer the card's own `lastUpdated`, falling back to `metadata.lastUpdated`, formatted as a `YYYY-MM-DD` date string (slice the ISO date). The page maps these facts to the i18n labels; the helper stays render-free so it is node-env testable (Task 2).
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` passes.
    - `npm run build` succeeds and lists a dynamic `/[locale]/cards/[id]` route.
    - Manual: `/en/cards/citi-cash-back` renders full reward rules, fees, an Apply CTA (with `rel="sponsored nofollow noopener"` — check the anchor), and the HK-availability + last-verified + provenance note; `/zh-HK/cards/citi-cash-back` renders the same in Traditional Chinese.
    - Manual: `/en/cards/does-not-exist` returns a 404 (notFound), not a crash or blank card.
    - No `recommendCards` import anywhere under `src/app/[locale]/cards/`.
  </acceptance_criteria>
  <done>The detail route renders a card's full data with honest provenance labeling and the affiliate Apply CTA, 404s unknown ids, and is bilingual — committed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Provenance-label derivation tests + recommender deep-link (DIR-03)</name>
  <files>src/lib/directory/__tests__/cardProvenance.test.ts, src/components/CardRecommendationList.tsx</files>
  <behavior>
    - cardProvenance: hkEligible === false → hkAvailable false.
    - cardProvenance: hkEligible === true → hkAvailable true.
    - cardProvenance: hkEligible === undefined → hkAvailable true (legacy-card convention).
    - cardProvenance: lastVerified prefers card.lastUpdated, falls back to metadata.lastUpdated, and is a YYYY-MM-DD string.
    - findCardById (from Plan 01) unknown id → null (the notFound trigger the detail route relies on) — re-asserted here as the DIR-02b/notFound contract.
  </behavior>
  <read_first>
    - src/lib/directory/cardProvenance.ts (Task 1) — the function under test.
    - src/lib/directory/cardDirectory.ts (Plan 01) — `findCardById` for the notFound-contract assertion.
    - src/lib/data/__tests__/loadCards.test.ts — the `card(overrides)` factory + vitest node-env style to copy.
    - src/components/CardRecommendationList.tsx — the result-card markup (the `card.name` / `card.issuer` header inside each recommendation at lines ~208-256, and the existing `Image`/expand button) where the deep-link is added; note it is a `'use client'` component already importing from `next/image` — add `Link` from `@/i18n/routing`.
    - src/i18n/routing.ts — `Link` import for the locale-preserving deep-link.
  </read_first>
  <action>
    Create `src/lib/directory/__tests__/cardProvenance.test.ts` (vitest, node-env, `card(overrides)` factory copied from loadCards.test.ts) covering every case in the behavior block above: hkAvailable for hkEligible true/false/undefined, and lastVerified preference (card.lastUpdated over metadata) + fallback + YYYY-MM-DD shape. Import `findCardById` from `../cardDirectory` and assert an unknown id returns `null` (documents the notFound contract the detail route depends on).

    In `src/components/CardRecommendationList.tsx`, add the DIR-03 deep-link: import `Link` from `@/i18n/routing` and add a "View details" link (label `results` namespace `viewDetails` if present, else reuse the `directory.viewDetails` key added in Plan 01 — pick whichever namespace the component already loads; if it must load the directory namespace, add `useTranslations('directory')`) on each recommendation result that navigates to `/cards/${card.id}`. Place it unobtrusively in the expanded detail area near the Apply CTA (or as a secondary link under the card header) so it does not disrupt the existing ranked-result layout or the Apply CTA. Do NOT change ranking, filtering, or the reward math — this is purely an added navigation affordance (DIR-03).
  </action>
  <verify>
    <automated>npx vitest run src/lib/directory/__tests__/cardProvenance.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/lib/directory/__tests__/cardProvenance.test.ts` passes with all behavior cases.
    - `npx tsc --noEmit` passes (CardRecommendationList deep-link typechecks).
    - Manual: a recommendation result shows a working link to `/[locale]/cards/{id}` that opens the correct detail page (DIR-03), and the Apply CTA + ranking are unchanged.
  </acceptance_criteria>
  <done>Provenance-label derivation is unit-tested green, and recommendation results deep-link into the detail route without altering ranking or the Apply CTA.</done>
</task>

</tasks>

<threat_model>
Read-only public card data, low risk. Trust boundary: anonymous visitor → public `/[locale]/cards/[id]` route.

| Threat ID | Category | Component | Severity | Disposition | Mitigation |
|-----------|----------|-----------|----------|-------------|------------|
| T-09-ID | Tampering | `[id]` route param → findCardById key lookup | low | mitigate | Param is used only as an exact-match key into the loaded set (no query/SQL, no fs path); unknown → notFound(). |
| T-09-TAB | Tampering | external Apply CTA (target=_blank) | low | mitigate | Reuse applyCtaProps → `rel="sponsored nofollow noopener"` (noopener blocks tab-nabbing, Phase 8 T-08-AFF-TAB). |
| T-09-XSS | Info-disclosure | card reward text rendered on detail page | low | mitigate | React auto-escaping; no dangerouslySetInnerHTML. |
</threat_model>

<verification>
- `npx tsc --noEmit` clean; `npm run build` includes `/[locale]/cards/[id]`.
- `npx vitest run src/lib/directory/__tests__/cardProvenance.test.ts` green.
- Known id renders full detail + provenance; unknown id 404s; recommendation results deep-link into detail.
</verification>

<success_criteria>
A visitor can open any card's dedicated bilingual detail view (full rewards, fees, affiliate Apply CTA) with
honest HK-availability + last-verified + provenance labeling, unknown ids 404, and a recommendation result
deep-links straight into that detail view — satisfying DIR-02b and DIR-03.
</success_criteria>

<output>
Create `.planning/phases/09-data-page-card-directory/09-02-SUMMARY.md` when done.
</output>
