---
phase: 09-data-page-card-directory
plan: 03
type: execute
wave: 3
depends_on: ["09-01"]
files_modified:
  - src/lib/cards/directoryControls.ts
  - src/lib/cards/__tests__/directoryControls.test.ts
  - src/components/CardDirectoryClient.tsx
  - messages/en.json
  - messages/zh-HK.json
autonomous: true
requirements: [DIR-01]
must_haves:
  truths:
    - "Typing in the directory search box filters tiles to cards whose name OR issuer matches (case-insensitive substring); clearing the box restores all cards (09-CONTEXT D-03)."
    - "A sort control reorders tiles by top reward rate (desc), annual fee (asc), or name (locale-aware) — driven by pure comparators unit-tested in node-env vitest (09-CONTEXT D-03)."
    - "Search + sort state is reflected in the URL query so a filtered/sorted view is shareable and survives reload (Claude's discretion -> URL params)."
    - "When search matches nothing a bilingual empty-state renders; no recommendation-engine call is made (DIR-01)."
  artifacts:
    - "src/lib/cards/directoryControls.ts — pure filterCards + sortCards + topRewardRate helpers"
    - "src/lib/cards/__tests__/directoryControls.test.ts — node-env vitest for the pure controls"
    - "src/components/CardDirectoryClient.tsx — search input + sort select + URL-synced state + empty-state"
    - "messages/en.json + messages/zh-HK.json — directory.* search/sort/empty keys"
  key_links:
    - "search input + sort select -> URL query (useRouter/usePathname + searchParams) -> filterCards/sortCards -> rendered grid"
    - "topRewardRate(card) = max rewardRate across card.rewards -> the reward-rate sort key"
  deferred:
    - "type/issuer/hkEligible FILTERS (the DIR-01 filter clause) remain deferred — v1 ships search + sort only (09-CONTEXT D-03, RQ-001)."
---

<objective>
Add client-side search (name + issuer) and sort (reward rate / annual fee / name) to the directory, with shareable URL-synced state and a bilingual empty-state — expanding the tracer grid from 09-01.

Purpose: deliver the DIR-01 search + sort controls over the server-loaded set, with the matching/ordering logic in a pure, unit-tested helper (single-artifact simplicity — no component-render devDependency).

Output: an interactive, shareable directory; the filter clause stays explicitly deferred.
</objective>

<execution_context>
@/Users/jeffreyng/.claude/gsd-core/workflows/execute-plan.md
@/Users/jeffreyng/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-data-page-card-directory/09-CONTEXT.md
@.planning/design/ui-contract-v2.md

# Reuse targets (read before implementing)
@src/components/CardDirectoryClient.tsx
@src/lib/cards/buildCardView.ts
@src/lib/cards/__tests__/buildCardView.test.ts
@src/lib/affiliate/applyCtaProps.ts
@src/types/card.ts
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: Pure directory controls helper + node-env vitest</name>
  <files>src/lib/cards/directoryControls.ts, src/lib/cards/__tests__/directoryControls.test.ts</files>
  <read_first>
    - `src/lib/cards/__tests__/buildCardView.test.ts` — the node-env vitest + `makeCard` fixture pattern to mirror (no component-render devDependency, per single-artifact simplicity).
    - `src/types/card.ts` — CreditCard shape: `name`, `issuer`, `fees.annualFee`, `rewards[].rewardRate`.
    - `.planning/phases/09-data-page-card-directory/09-CONTEXT.md` D-03 (search matches name + issuer; sort by reward rate / annual fee / name; filters deferred).
  </read_first>
  <behavior>
    - filterCards(cards, ""): returns all cards unchanged.
    - filterCards(cards, "citi"): case-insensitive; matches when name OR issuer contains the normalized query; trims whitespace.
    - topRewardRate(card): returns the max rewardRate across card.rewards (0 when none).
    - sortCards(cards, "rewardRate"): descending by topRewardRate. sortCards(cards, "annualFee"): ascending by fees.annualFee. sortCards(cards, "name"): locale-aware ascending by name. All sorts are stable / non-mutating (operate on a copy).
  </behavior>
  <action>
    Write the failing tests first in `src/lib/cards/__tests__/directoryControls.test.ts` (RED), covering the behaviors above with a small `makeCard` fixture mirroring buildCardView.test.ts. Then implement `src/lib/cards/directoryControls.ts` (GREEN): export a `SortKey` type (rewardRate | annualFee | name), `topRewardRate(card)`, `filterCards(cards, query)` (case-insensitive, trimmed, name-or-issuer substring), and `sortCards(cards, key)` (non-mutating copy; rewardRate desc, annualFee asc, name locale-aware asc). Keep it pure and render-free — no next-intl, no React imports.
  </action>
  <verify>
    <automated>npm install && npx vitest run src/lib/cards/__tests__/directoryControls.test.ts</automated>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/lib/cards/__tests__/directoryControls.test.ts` green — covers empty query, name match, issuer match, and each sort key — DIR-01 / 09-CONTEXT D-03.
    - `npx tsc --noEmit` clean; the helper imports no React/next-intl (pure).
  </acceptance_criteria>
  <done>filterCards + sortCards + topRewardRate exist as pure helpers with passing node-env vitest coverage; committed as a RED then GREEN pair.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Wire search + sort into the client with URL-synced shareable state</name>
  <files>src/components/CardDirectoryClient.tsx</files>
  <read_first>
    - `src/components/CardDirectoryClient.tsx` (from 09-01, the file being extended) — where the grid maps cards to browse tiles.
    - `.planning/design/ui-contract-v2.md` section 5 Inputs/selects (white, 1px border-strong, 2px radius, Inter, 2px mint focus outline) — the search input + sort select styling.
    - `src/i18n/routing.ts` — `useRouter` + `usePathname` for locale-aware URL updates; read the query via Next's `useSearchParams`.
    - `src/lib/cards/directoryControls.ts` (Task 1) — filterCards + sortCards.
  </read_first>
  <action>
    Extend `CardDirectoryClient.tsx` with a controls row above the grid: a free-text search input (placeholder from i18n) and a sort select (options: reward rate, annual fee, name). Drive both from URL query params (e.g. `q` and `sort`) for shareability — read current values from `useSearchParams`, and on change update the URL via next-intl `useRouter` + `usePathname` (replace, not push, so back-button isn't spammed). Derive the rendered list by running `filterCards(cards, q)` then `sortCards(..., sort)` from the Task 1 helper, then map to browse tiles exactly as 09-01 does. Style the controls per contract section 5 (hairline inputs, 2px radius, mint focus outline). Keep the route read-only — no recommendation-engine call. Render a live count label (from `directory.cardCount`) reflecting the filtered length.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
    <automated>test -z "$(grep -rl recommendCards src/components/CardDirectoryClient.tsx 2>/dev/null)"</automated>
    <human-check>Run `npm run dev:skip-check`, open /en/cards: typing in search narrows the grid by name/issuer; changing sort reorders tiles; the URL gains ?q=...&sort=...; reloading or sharing that URL restores the same filtered/sorted view.</human-check>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` clean; grep confirms no recommendation-engine call in the client (read-only) — DIR-01.
    - Observable: search filters, sort reorders, and the state is reflected in the URL and survives reload — DIR-01 / 09-CONTEXT D-03.
  </acceptance_criteria>
  <done>The directory has working search + sort backed by the pure helper, with URL-synced shareable state; committed.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Bilingual empty-state + search/sort labels</name>
  <files>src/components/CardDirectoryClient.tsx, messages/en.json, messages/zh-HK.json</files>
  <read_first>
    - `src/components/CardRecommendationList.tsx` — the existing v2 empty-state pattern (bordered box, uppercase display heading, muted hint) to mirror.
    - `src/components/CardDirectoryClient.tsx` (Task 2) — where the filtered list is computed.
    - `.planning/phases/09-data-page-card-directory/09-CONTEXT.md` D-06 (bilingual, both message files).
  </read_first>
  <action>
    In `CardDirectoryClient.tsx`, render a bilingual empty-state when the filtered list is empty (a v2 bordered box with an uppercase display heading + muted hint, mirroring CardRecommendationList's no-results block). Add the new `directory` keys used by search + sort + empty-state to BOTH `messages/en.json` and `messages/zh-HK.json` with full parity: searchPlaceholder, sortLabel, sortRewardRate, sortAnnualFee, sortName, emptyTitle, emptyHint. Author real Traditional-Chinese copy for every key — no placeholders, no missing keys.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
    <automated>node -e "const e=require('./messages/en.json').directory,z=require('./messages/zh-HK.json').directory;const k=Object.keys(e);if(k.some(x=>!z[x]))throw new Error('zh-HK missing directory key');console.log('directory parity ok',k.length)"</automated>
    <human-check>On /en/cards, type a query that matches nothing (e.g. zzz): a bilingual empty-state renders. Switch to /zh-HK/cards and confirm the search placeholder, sort options, and empty-state all read in Chinese.</human-check>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` clean; the directory namespace has en/zh-HK parity including searchPlaceholder, sortLabel, sortRewardRate, sortAnnualFee, sortName, emptyTitle, emptyHint with real zh copy — 09-CONTEXT D-06.
    - Observable: a no-match search shows a bilingual empty-state — DIR-01 / 09-CONTEXT D-03.
  </acceptance_criteria>
  <done>The directory renders a bilingual empty-state on no match, and all search/sort/empty i18n keys have full en/zh-HK parity; committed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| visitor browser -> directory search/sort | User-controlled `q` and `sort` query params reflected into UI state; read-only, no writes, no auth. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09-03-01 | Tampering | search `q` param rendered in the DOM | low | mitigate | React auto-escapes the query wherever it is echoed; no dangerouslySetInnerHTML, no eval; the query only feeds a pure string filter over the in-memory card set. |
| T-09-03-02 | Tampering | `sort` param -> SortKey | low | mitigate | sortCards falls back to a default order for any unrecognized key; an out-of-range value cannot throw or reorder unexpectedly. |

No package installs in this plan — package-legitimacy gate not applicable.
</threat_model>

<verification>
- `npx vitest run src/lib/cards/__tests__/directoryControls.test.ts` green — pure filter/sort logic covered.
- `npx tsc --noEmit` clean across the helper + client changes.
- Read-only: grep confirms no recommendation-engine call in the client.
- i18n: the directory namespace (incl. search/sort/empty keys) has en/zh-HK parity with real Chinese copy.
- `npm run build` compiles the directory route (health-check may need env; vitest + tsc are the automated gate, build runs in CI).
</verification>

<success_criteria>
- Search filters the grid by name/issuer and sort reorders by reward rate / annual fee / name — DIR-01, 09-CONTEXT D-03.
- Search + sort state is URL-synced and shareable.
- A no-match search shows a bilingual empty-state.
- The DIR-01 filter clause (type/issuer/hkEligible) stays explicitly deferred.
</success_criteria>

## Artifacts this phase produces (Plan 09-03)

- **New helper:** `src/lib/cards/directoryControls.ts` (pure filterCards + sortCards + topRewardRate).
- **New test:** `src/lib/cards/__tests__/directoryControls.test.ts` (node-env vitest).
- **Modified component:** `src/components/CardDirectoryClient.tsx` (search input + sort select + URL-synced state + empty-state).
- **New i18n keys:** `directory.{searchPlaceholder, sortLabel, sortRewardRate, sortAnnualFee, sortName, emptyTitle, emptyHint}` in both message files.

<output>
Create `.planning/phases/09-data-page-card-directory/09-03-SUMMARY.md` when done.
</output>