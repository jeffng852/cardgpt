---
phase: 09-data-page-card-directory
plan: 03
type: execute
wave: 2
depends_on: [09-01]
files_modified:
  - src/components/CardDirectoryClient.tsx
autonomous: true
requirements: [DIR-01]

must_haves:
  truths:
    - "The directory has a free-text search input that narrows the grid to cards whose name OR issuer matches the query (D-03), live as the visitor types."
    - "The directory has a sort control offering reward rate / annual fee / name (D-03), reordering the grid via the Plan 01 sortCards helper."
    - "Search + sort state is reflected in the URL query params (shareable filtered/sorted views — D-03 discretion), and a shared URL restores that state."
    - "When search matches nothing the grid shows the bilingual empty-state (directory.empty), not a blank page."
    - "No recommendCards() call — search/sort is pure client-side interactivity over the server-loaded set (DIR-01 read-only)."
  artifacts:
    - "Search input + sort control wired into src/components/CardDirectoryClient.tsx using the Plan 01 pure helpers"
  key_links:
    - "searchCards / sortCards (from Plan 01 cardDirectory.ts) drive the rendered subset — the same functions unit-tested in Plan 01."
    - "URL query params (?q=&sort=) <-> component state via next-intl/next navigation (useRouter/useSearchParams) — the shareable-state link."
  deferred:
    - "DIR-01 filter clause (type/issuer/hkEligible filters) — DEFERRED; v1 is search + sort only. getCardsByIssuer/getCardsByRewardUnit already exist to build filters on later."
---

<objective>
Expand the directory grid (Plan 01) with the D-03 interactivity: a free-text **search** (name/issuer) and a
**sort** control (reward rate / annual fee / name), driven by the pure helpers Plan 01 already implemented and
unit-tested, with state kept in URL query params so a filtered/sorted view is shareable.

Purpose: Complete DIR-01's browse-and-find surface for v1 (filters remain deferred) without adding any engine
call or new dependency — pure client-side interactivity over the server-loaded card set.
Output: Search + sort controls wired into CardDirectoryClient, URL-synced and bilingual.
</objective>

<execution_context>
@/Users/jeffreyng/.claude/gsd-core/workflows/execute-plan.md
@/Users/jeffreyng/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-data-page-card-directory/09-CONTEXT.md
@.planning/phases/09-data-page-card-directory/09-01-SUMMARY.md
</context>

<artifacts_this_phase_produces>
Modified: `CardDirectoryClient.tsx` gains a search input + sort control with URL-query-param state and a
bilingual empty state. Consumes (does not create): `searchCards`, `sortCards`, `SortKey` from Plan 01's
cardDirectory.ts, and the `directory.search.*` / `directory.sort.*` / `directory.empty` i18n keys added by
Plan 01. No new files, no new dependencies.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Search + sort controls with URL-synced state</name>
  <files>src/components/CardDirectoryClient.tsx</files>
  <read_first>
    - src/components/CardDirectoryClient.tsx (from Plan 01) — the client grid this task extends; it already receives `cards` + `metadata`, renders the header/banner/grid, and uses `useTranslations('directory')`.
    - src/lib/directory/cardDirectory.ts (Plan 01) — `searchCards(cards, query)`, `sortCards(cards, key)`, and the `SortKey` union that constrains the sort control's options (exactly rewardRate/annualFee/name — D-03).
    - src/i18n/routing.ts — exports `useRouter` and `usePathname` (createNavigation wrappers) for locale-aware URL updates; use `useSearchParams` from `next/navigation` to read `?q=`/`?sort=`.
    - src/components/CardRecommendationList.tsx — the existing filter-tab / control styling vocabulary (border-b tabs, `text-text-secondary`, `border-primary` active state) to match the sort control to the app's look (D-08).
    - messages/en.json (directory namespace, added in Plan 01) — the exact `search.*`, `sort.*`, `count`, and `empty` keys to reference.
  </read_first>
  <action>
    Extend `src/components/CardDirectoryClient.tsx` to add search + sort over the `cards` prop, computing the rendered subset as `sortCards(searchCards(cards, query), sortKey)` (D-03). Initialize `query` and `sortKey` from the URL: read `?q=` (string, default '') and `?sort=` (one of the `SortKey` values, default `'rewardRate'`) via `useSearchParams`; on each change, push the updated query string with `useRouter` + `usePathname` from `@/i18n/routing` so the URL stays shareable and locale-preserving (D-03 discretion: URL params chosen for shareability). Guard against an invalid `?sort=` value by falling back to `'rewardRate'`.

    Render a text `<input>` bound to `query` (placeholder `directory.search.placeholder`, accessible label `directory.search.label`) and a sort control (a `<select>` or button group) whose options are the three `SortKey` values labeled by `directory.sort.rewardRate` / `directory.sort.annualFee` / `directory.sort.name` (label `directory.sort.label`). Show the live result count (`directory.count` with the rendered subset length). When the rendered subset is empty, show the `directory.empty` bilingual empty-state message instead of an empty grid. Do NOT add type/issuer filters (deferred — D-03). Do NOT call `recommendCards()` or add any dependency; keep everything client-side over the already-loaded set (DIR-01 read-only).
  </action>
  <verify>
    <automated>npx vitest run src/lib/directory/__tests__/cardDirectory.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` passes (the extended client component typechecks against the SortKey union).
    - `npx vitest run src/lib/directory/__tests__/cardDirectory.test.ts` passes (the searchCards/sortCards logic this UI wires is covered by Plan 01's tests).
    - `npm run build` succeeds.
    - Manual: typing in the search box narrows the grid by name/issuer live; changing the sort reorders by reward rate / annual fee / name; the URL updates to `?q=...&sort=...` and reloading (or opening a shared URL) restores the same search + sort; a no-match query shows the bilingual empty-state; `/zh-HK/cards` shows all control labels in Traditional Chinese.
    - `grep -rL recommendCards src/components/CardDirectoryClient.tsx` confirms no engine call in the directory client.
  </acceptance_criteria>
  <done>The directory grid supports live name/issuer search and reward-rate/annual-fee/name sort with shareable URL state and a bilingual empty state, purely client-side — committed.</done>
</task>

</tasks>

<threat_model>
Read-only public card data, low risk. Trust boundary: anonymous visitor → client-side search/sort over the
already-loaded set (no new server surface, no writes, no engine call).

| Threat ID | Category | Component | Severity | Disposition | Mitigation |
|-----------|----------|-----------|----------|-------------|------------|
| T-09-QP | Tampering | `?q=` / `?sort=` URL params | low | mitigate | `q` is only lower-cased and substring-matched in-memory (no eval/DOM injection); `sort` is validated against the SortKey union with a safe default. |
</threat_model>

<verification>
- `npx tsc --noEmit` clean; `npm run build` succeeds.
- `npx vitest run src/lib/directory/__tests__/cardDirectory.test.ts` green (helpers this UI consumes).
- Search narrows, sort reorders, URL reflects state and restores on reload, empty-state shows, bilingual labels render.
</verification>

<success_criteria>
A visitor can search the directory by card name or issuer and sort by reward rate, annual fee, or name, with
the result shareable via URL and a graceful bilingual empty-state — completing v1 DIR-01 (browse + search +
sort + detail) with filters explicitly deferred.
</success_criteria>

<output>
Create `.planning/phases/09-data-page-card-directory/09-03-SUMMARY.md` when done.
</output>
