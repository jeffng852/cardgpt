---
phase: 09-data-page-card-directory
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/[locale]/cards/page.tsx
  - src/components/CardDirectoryClient.tsx
  - src/lib/directory/cardDirectory.ts
  - src/lib/directory/__tests__/cardDirectory.test.ts
  - src/components/HomeClient.tsx
  - messages/en.json
  - messages/zh-HK.json
autonomous: true
requirements: [DIR-01, DIR-02]

must_haves:
  truths:
    - "Visiting /en/cards and /zh-HK/cards renders a responsive grid of ALL loaded cards (live count via loadCards(); 11 today, never a hardcoded number — D-05)."
    - "Each tile shows issuer, card name, top reward rate, annual fee, and a card-type badge (credit/crypto/prepaid) — D-01."
    - "A page-level provenance banner sits at the top disclosing that some card data is bulk/community-sourced (DIR-02a / D-04 / DEC-DATA-001)."
    - "A 'Card Directory' nav entry on the home header links to /cards (D-07)."
    - "The directory reads through the ungated async force-dynamic loadCards() path — NO recommendCards() call (DIR-01 read-only), so cards the recommender would gate out still appear."
    - "Every new UI string exists in BOTH messages/en.json and messages/zh-HK.json with full key parity (D-06)."
  artifacts:
    - "src/app/[locale]/cards/page.tsx (server component, force-dynamic)"
    - "src/components/CardDirectoryClient.tsx (client grid renderer)"
    - "src/lib/directory/cardDirectory.ts (pure helpers: topRewardRate, searchCards, sortCards, findCardById, SortKey)"
    - "src/lib/directory/__tests__/cardDirectory.test.ts"
    - "directory namespace added to messages/en.json + messages/zh-HK.json (complete — grid, banner, controls, detail, nav, deep-link)"
    - "Card Directory nav link in src/components/HomeClient.tsx"
  key_links:
    - "loadCards() (async, force-dynamic, ungated) -> CardDirectoryClient prop — the immediate-visibility path (no CDN-stale regression, DIR-01)."
    - "Each tile Link href = /cards/{card.id} using @/i18n/routing Link so locale is preserved and matches the Plan 02 detail route param."
    - "i18n key parity en <-> zh-HK — a missing key produces a visible fallback bug."
  deferred:
    - "DIR-01 filter clause (type/issuer/hkEligible filters) — DEFERRED to a later phase (only credit cards in prod today; nothing to filter until the set grows / crypto seeds). v1 ships browse + search + sort only."
    - "DIR-02 per-card provenance BADGES in the grid — DEFERRED. v1 uses a page-level banner (here) + detail-page labels (Plan 02)."
---

<objective>
Build the tracer slice for Phase 9: a browsable, bilingual **Card Directory** at `/[locale]/cards` that
loads the full live card set through the existing `loadCards()` path and renders a responsive grid of
tiles, with a page-level provenance banner and a nav entry from the home page. This is the thinnest
end-to-end path — real data → server component → client grid → rendered tiles, reachable via nav —
proven before the detail route (Plan 02) and search/sort interactivity (Plan 03) expand from it.

Purpose: Prove the directory architecture (ungated async data → server → client grid) end-to-end on the
executor's best early-context tokens, and establish the pure-helper + full-i18n foundation both Wave 2
plans consume, so neither of them has to touch `messages/*.json` (clean parallel ownership).
Output: The directory route, its client grid, the pure directory helpers + tests, the complete
`directory` i18n namespace in both languages, and the home nav link.
</objective>

<execution_context>
@/Users/jeffreyng/.claude/gsd-core/workflows/execute-plan.md
@/Users/jeffreyng/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-data-page-card-directory/09-CONTEXT.md
</context>

<artifacts_this_phase_produces>
New route: `/[locale]/cards` (directory grid). New component: `CardDirectoryClient`. New pure-helper module
`src/lib/directory/cardDirectory.ts` (`topRewardRate`, `searchCards`, `sortCards`, `findCardById`, `SortKey`).
New test: `cardDirectory.test.ts`. New i18n keys: the complete `directory` namespace in `en.json` + `zh-HK.json`.
Modified: `HomeClient.tsx` gains a Card Directory nav link. (Detail route, deep-link, and search/sort UI are
produced in Plans 02–03.)
</artifacts_this_phase_produces>

<tasks>

<task type="tracer">
  <name>Task 1: Directory grid route, reachable end-to-end from home</name>
  <files>src/app/[locale]/cards/page.tsx, src/components/CardDirectoryClient.tsx, src/lib/directory/cardDirectory.ts, src/components/HomeClient.tsx, messages/en.json, messages/zh-HK.json</files>
  <read_first>
    - src/app/[locale]/page.tsx — the server-component pattern to mirror EXACTLY: `export const dynamic = 'force-dynamic'`, `await loadCards()`, then render a client component with the cards as a prop.
    - src/components/HomeClient.tsx — the `'use client'` prop pattern, the sticky header chrome (Logo + appName + DarkModeToggle + LanguageSwitcher), and `useTranslations` usage; this is where the nav link is added.
    - src/app/[locale]/privacy/page.tsx — a secondary [locale] page's header composition (a `Link` from `@/i18n/routing` back to `/`), the model for the directory header + back-nav.
    - src/components/CardRecommendationList.tsx — the existing card-tile visual vocabulary (image via getCardImageUrl/hasCardImage with SVG placeholder fallback, issuer/name typography, Tailwind `bg-surface`/`ring-border`/`text-text-*` tokens). Reuse this vocabulary; do NOT invent a new design language (D-08 — Phase 11 restyles).
    - src/lib/data/loadCards.ts — `loadCards()` (async, ungated, Redis-in-prod) and `getDatabaseMetadata()` (lastUpdated/version/nextReviewDate for the banner).
    - src/types/card.ts — CreditCard shape (name, issuer, cardType, rewards[].rewardRate, fees.annualFee, applyUrl, hkEligible) and RewardUnit.
    - src/i18n/routing.ts — import `Link` from `@/i18n/routing` for locale-preserving links.
  </read_first>
  <action>
    Create `src/app/[locale]/cards/page.tsx` as a server component mirroring `[locale]/page.tsx`: set `export const dynamic = 'force-dynamic'` (DIR-01 immediate-visibility, no CDN-stale regression), `await loadCards()` for the full ungated card set, and `await getDatabaseMetadata()` for the banner's last-updated line. Render `CardDirectoryClient` passing `cards` and `metadata`. Do NOT call `recommendCards()` — the directory is read-only (DIR-01).

    Create `src/components/CardDirectoryClient.tsx` (`'use client'`) that: (a) renders the same sticky header chrome as HomeClient (Logo, app name, DarkModeToggle, LanguageSwitcher) plus a locale-preserving `Link` back to home; (b) renders a **page-level provenance banner** at the top of the main content (DIR-02a / D-04 / DEC-DATA-001) reading from the `directory.banner.*` i18n keys, including the last-updated date from `metadata.lastUpdated`; (c) renders a responsive grid (Tailwind `grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-4` or similar) of card tiles. Each tile (D-01) shows: the card image via `getCardImageUrl`/`hasCardImage` with the existing SVG placeholder fallback, the card name, the issuer, the **top reward rate** formatted as a percentage via `topRewardRate(card)` from cardDirectory.ts, the annual fee (`$X` or the "free" label when 0), and a small **card-type badge** rendering `cardType` (credit/crypto/prepaid) via a `directory.cardType.*` i18n key. Do NOT hardcode the card count — render `cards.length` live (D-05). Wrap each tile in a `Link` from `@/i18n/routing` with `href={`/cards/${card.id}`}` (the DIR-03/detail target; the route lands in Plan 02, the link contract is set here). All visible text comes from `useTranslations('directory')`.

    Create `src/lib/directory/cardDirectory.ts` and export `topRewardRate(card: CreditCard): number` — the maximum `rewardRate` across `card.rewards` (0 when none) — used by the tile now. (The remaining helpers `searchCards`, `sortCards`, `findCardById`, `SortKey` are added in Task 2; create the file here with `topRewardRate` only.)

    In `src/components/HomeClient.tsx` add a **Card Directory** nav link in the existing header's right-hand control cluster (beside DarkModeToggle/LanguageSwitcher), using `Link` from `@/i18n/routing` with `href="/cards"` and the `directory.nav.link` i18n label (D-07). Match the existing header link styling; do not restructure the header.

    Add the COMPLETE `directory` namespace to BOTH `messages/en.json` and `messages/zh-HK.json` (D-06) — every key this phase needs, so Plans 02–03 never edit these files. Include at minimum: `nav.link`, `title`, `subtitle`, `banner.title`, `banner.body` (bulk/community-sourced disclosure), `banner.lastUpdated`, `search.placeholder`, `search.label`, `sort.label`, `sort.rewardRate`, `sort.annualFee`, `sort.name`, `count` (e.g. "{n} cards"), `empty` (no-match state), `cardType.credit`, `cardType.crypto`, `cardType.prepaid`, `annualFee`, `free`, `topRate`, `viewDetails` (tile/deep-link CTA), `backToDirectory`, and a `detail.*` sub-object for Plan 02: `detail.rewards`, `detail.fees`, `detail.annualFee`, `detail.fxFee`, `detail.applyHere`, `detail.hkAvailable`, `detail.hkNotAvailable`, `detail.lastVerified`, `detail.provenanceNote`, `detail.notFoundTitle`, `detail.notFoundBody`. Traditional-Chinese (zh-HK) values must be real translations, not English placeholders. Keep en/zh-HK key trees identical (parity).
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` passes (route + client component + helper typecheck clean).
    - `npm run build` succeeds and lists `/[locale]/cards` among the built routes.
    - Manual: `/en/cards` renders a grid of every loaded card (count matches `loadCards()` length, not a hardcoded number) with the provenance banner visible at top; `/zh-HK/cards` renders the same in Traditional Chinese with no raw i18n keys showing.
    - The home header shows a Card Directory link that navigates to `/cards`.
    - `node -e "const en=require('./messages/en.json'); const zh=require('./messages/zh-HK.json'); const keys=o=>Object.keys(o).flatMap(k=>o[k]&&typeof o[k]==='object'?Object.keys(o[k]).map(s=>k+'.'+s):[k]); const a=keys(en.directory).sort(), b=keys(zh.directory).sort(); if(JSON.stringify(a)!==JSON.stringify(b))throw new Error('directory i18n key parity mismatch'); console.log('parity OK',a.length)"` prints parity OK.
  </acceptance_criteria>
  <done>The directory grid renders the full live card set end-to-end (data → server → client → tiles) with the provenance banner, is reachable via a home nav link, and is bilingual — committed.</done>
</task>

<task type="auto">
  <name>Task 2: Pure directory helpers (search, sort, find) with unit tests</name>
  <files>src/lib/directory/cardDirectory.ts, src/lib/directory/__tests__/cardDirectory.test.ts</files>
  <read_first>
    - src/lib/affiliate/applyCtaProps.ts — the Phase 8 "extract a pure, render-free helper so it is node-env unit-testable without a DOM/component-render devDependency" pattern to mirror (single-artifact simplicity).
    - src/lib/data/__tests__/loadCards.test.ts — the vitest style in this repo: `import { describe, it, expect } from 'vitest'`, a small `card(overrides)` factory, node environment.
    - src/lib/directory/cardDirectory.ts — the module started in Task 1 (already exports `topRewardRate`); extend it here.
    - src/types/card.ts — CreditCard / RewardRule / RewardUnit for the helper signatures.
    - vitest.config.ts — confirms node environment + `@`→src alias + the `src/**/*.test.ts` include glob (this test file is collected automatically).
  </read_first>
  <action>
    Extend `src/lib/directory/cardDirectory.ts` with pure, side-effect-free helpers (mirroring the applyCtaProps extraction pattern so search/sort/lookup logic is unit-testable in node-env vitest without any component render):

    - `export type SortKey = 'rewardRate' | 'annualFee' | 'name'` (D-03: exactly these three sorts — reward rate / annual fee / name; NO type/issuer filters, which are deferred).
    - `searchCards(cards: CreditCard[], query: string): CreditCard[]` — case-insensitive, whitespace-trimmed substring match on `name` OR `issuer` (D-03 search scope). An empty/whitespace query returns all cards unchanged. Normalize both sides with `.toLowerCase()`; do not mutate the input array.
    - `sortCards(cards: CreditCard[], key: SortKey): CreditCard[]` — return a NEW sorted array (never mutate input): `rewardRate` descending by `topRewardRate(card)`; `annualFee` ascending by `card.fees.annualFee`; `name` ascending by `localeCompare`. Ties fall back to `name` ascending so ordering is deterministic.
    - `findCardById(cards: CreditCard[], id: string): CreditCard | null` — returns the matching card or `null` (this is the resolution the Plan 02 detail page uses to decide `notFound()`; extracting it here makes the not-found branch unit-testable without async Redis).

    Create `src/lib/directory/__tests__/cardDirectory.test.ts` with a `card(overrides)` factory (copy the loadCards.test.ts style) and cover: topRewardRate picks the max rate (and returns 0 for a card with no rewards); searchCards matches on name and on issuer, is case-insensitive, and returns all cards for an empty query; sortCards orders correctly for each SortKey, is stable/deterministic on ties, and does not mutate its input; findCardById returns the card for a known id and `null` for an unknown id.
  </action>
  <verify>
    <automated>npx vitest run src/lib/directory/__tests__/cardDirectory.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/lib/directory/__tests__/cardDirectory.test.ts` passes with tests for all four helpers.
    - The test suite includes an explicit case asserting `findCardById(cards, 'nope')` returns `null` (the notFound trigger consumed by Plan 02).
    - The test suite asserts `sortCards`/`searchCards` do not mutate the input array.
    - `npx tsc --noEmit` passes.
  </acceptance_criteria>
  <done>All four pure directory helpers exist and are green under vitest; the search/sort/lookup logic Wave 2 builds on is proven independent of any DOM.</done>
</task>

</tasks>

<threat_model>
Read-only public card data, low risk. Trust boundary: anonymous visitor → public `/[locale]/cards` route (no auth, no writes, no engine call). Notable items:

| Threat ID | Category | Component | Severity | Disposition | Mitigation |
|-----------|----------|-----------|----------|-------------|------------|
| T-09-XSS | Tampering/Info-disclosure | card fields rendered in tiles | low | mitigate | Render all card text through React (auto-escaped); no `dangerouslySetInnerHTML`. |
| T-09-PUB | Info-disclosure | new i18n strings + code (PUBLIC repo) | low | mitigate | No secrets, no admin/exploit detail in any new file (repo is public — CLAUDE.md rule). |
</threat_model>

<verification>
- `npx tsc --noEmit` clean.
- `npm run build` succeeds and includes the `/[locale]/cards` route.
- `npx vitest run src/lib/directory/__tests__/cardDirectory.test.ts` green.
- `/en/cards` and `/zh-HK/cards` render the full live grid + provenance banner; home nav link reaches the directory; i18n key parity holds.
</verification>

<success_criteria>
A visitor can reach a bilingual Card Directory from the home page and browse the full live card set as a
grid of tiles (issuer, name, top reward rate, annual fee, type badge) under a provenance banner, read-only
through the ungated force-dynamic loadCards() path — with the pure search/sort/find helpers implemented and
unit-tested, ready for Wave 2 expansion.
</success_criteria>

<output>
Create `.planning/phases/09-data-page-card-directory/09-01-SUMMARY.md` when done.
</output>
