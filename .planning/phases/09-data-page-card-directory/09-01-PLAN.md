---
phase: 09-data-page-card-directory
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/[locale]/cards/page.tsx
  - src/components/CardDirectoryClient.tsx
  - src/components/CreditCardCard.tsx
  - src/lib/cards/buildCardView.ts
  - src/lib/cards/__tests__/buildCardView.test.ts
  - src/components/HomeClient.tsx
  - messages/en.json
  - messages/zh-HK.json
autonomous: true
requirements: [DIR-01, DIR-02]
must_haves:
  truths:
    - "Visiting /en/cards and /zh-HK/cards renders a responsive grid with one browse-mode CreditCardCard tile per active card returned by loadCards() — the live count, never a hardcoded 11 (09-CONTEXT D-05)."
    - "Each directory tile is a single link to /[locale]/cards/[id]; browse-mode tiles show the issuer + type badge + best-for chips + a VIEW CARD footer and OMIT the ranked reward-calc rows (09-CONTEXT D-01)."
    - "A page-level provenance banner disclosing bulk/community-sourced data (DEC-DATA-001, 09-CONTEXT D-04) sits at the top of the directory in both languages (DIR-02)."
    - "The home header carries a Directory nav link that routes to /cards (09-CONTEXT D-07)."
    - "The directory route is read-only — it loads cards through the ungated async force-dynamic path (loadCards) and makes NO recommendation-engine call (DIR-01)."
  artifacts:
    - "src/app/[locale]/cards/page.tsx — server component, force-dynamic, loads all cards"
    - "src/components/CardDirectoryClient.tsx — client grid of browse tiles + provenance banner"
    - "src/components/CreditCardCard.tsx — browse-mode footer renders a non-anchor VIEW CARD affordance"
    - "messages/en.json + messages/zh-HK.json — directory.* namespace"
  key_links:
    - "server page loadCards() -> CardDirectoryClient cards prop -> buildCardView(card, browse) -> CreditCardCard"
    - "tile Link href -> /[locale]/cards/[id] (detail route built in 09-02)"
    - "home header -> Directory nav link -> /cards"
  deferred:
    - "type/issuer/hkEligible FILTERS (the DIR-01 filter clause) — v1 ships browse only; search + sort land in 09-03 (09-CONTEXT D-03, RQ-001)."
    - "per-card provenance badges — page-level banner only in v1 (09-CONTEXT D-04); per-card precision deferred with crypto seeding."
---

<objective>
Build the public Card Directory as an end-to-end vertical slice in the v2 design system, REUSING the shared `CreditCardCard` in browse mode — the tracer for Phase 9.

Purpose: prove data -> server -> client -> shared-card -> nav works before expanding into detail pages (09-02) and search/sort (09-03). This is where an architectural dead-end (e.g. the browse footer conflicting with a tile-level link) surfaces after one commit, not ten.

Output: a reachable `/[locale]/cards` grid of every active card, a provenance banner, a home nav entry, and the browse-mode footer fix on the shared card — all bilingual, all v2-styled.
</objective>

<execution_context>
@/Users/jeffreyng/.claude/gsd-core/workflows/execute-plan.md
@/Users/jeffreyng/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-data-page-card-directory/09-CONTEXT.md
@.planning/design/ui-contract-v2.md

# Reuse targets (read before implementing)
@src/components/CreditCardCard.tsx
@src/lib/cards/buildCardView.ts
@src/lib/data/loadCards.ts
@src/app/[locale]/page.tsx
@src/components/HomeClient.tsx
@src/types/card.ts
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: End-to-end directory grid — one path, data to shared card to nav</name>
  <files>src/components/CreditCardCard.tsx, src/lib/cards/buildCardView.ts, src/lib/cards/__tests__/buildCardView.test.ts, src/app/[locale]/cards/page.tsx, src/components/CardDirectoryClient.tsx, src/components/HomeClient.tsx</files>
  <read_first>
    - `.planning/design/ui-contract-v2.md` section 5 (the shared card), section 1 tokens, section 2 fonts, and the Nav notes — the visual system this route MUST use.
    - `src/components/CreditCardCard.tsx` — the shared card. Note the footer (approx lines 196-215): browse mode currently renders an external anchor (applyCtaProps) or the no-apply-link text. That anchor is the nesting conflict the tracer resolves.
    - `src/lib/cards/buildCardView.ts` + `src/lib/cards/__tests__/buildCardView.test.ts` — browse mode ALREADY omits reward rows and labels the CTA VIEW CARD, and the existing tests lock that. Confirm; only extend if a gap is found.
    - `src/app/[locale]/page.tsx` + `src/components/HomeClient.tsx` — the server-to-client force-dynamic pattern to mirror, and the v2 header (sticky, border-b border-border bg-bg, Logo + DarkModeToggle + LanguageSwitcher) to add the nav link into.
    - `src/lib/data/loadCards.ts` — loadCards() is the ungated async path (returns ALL active cards; the hkEligible gate lives only in the recommender).
  </read_first>
  <behavior>
    - buildCardView(card, browse) omits the reward and topReward rows, keeps rewardType/annualFee/fxFee, and labels the CTA VIEW CARD (already true and tested — assert it stays true).
    - CreditCardCard in browse mode renders NO nested anchor in its footer, so a parent link can wrap the whole tile.
    - The directory server page returns one tile per active card from loadCards(), in both locales.
  </behavior>
  <action>
    Create `src/app/[locale]/cards/page.tsx` as a server component with `export const dynamic = 'force-dynamic'`, mirroring `page.tsx`: await `loadCards()` and pass the cards array to a new client `CardDirectoryClient`. Do NOT import or call the recommendation engine — the directory is read-only (DIR-01).

    Create `src/components/CardDirectoryClient.tsx` (client component) taking `cards: CreditCard[]`. Render the v2 page chrome by mirroring HomeClient's header (sticky, hairline-bottom, bg-bg, max-w-7xl, `Logo` linking home via next-intl Link, plus `DarkModeToggle` and `LanguageSwitcher`). Below it render a responsive grid (`grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-4 items-stretch`). For each card compute `buildCardView(card, { mode: 'browse' })` and render `CreditCardCard` with `mode="browse"`, wrapping EACH tile in a single next-intl `Link` to the card path `/cards/<card.id>` with `className="block h-full focus:outline-none focus-visible:outline focus-visible:outline-2 focus-visible:outline-brand"`. The live card count drives the grid — never hardcode a number (09-CONTEXT D-05).

    In `CreditCardCard.tsx`, add a browse-mode branch to the footer: when `mode` equals browse, render the VIEW CARD label as a NON-interactive footer element (a styled div/span with the same hairline-top + uppercase display treatment as the ranked footer, plus the trailing arrow), never an anchor. Gate strictly on `mode` so the ranked footer path (external applyCtaProps CTA plus the no-apply-link text) stays byte-identical — the recommender shipped on it in Phase 11.

    In `HomeClient.tsx`, add a Directory nav link into the header actions cluster (a next-intl Link to `/cards`, styled as a v2 outline nav button per contract Nav notes, label from `directory.navLink`). Add the minimal i18n keys this task needs — `directory.navLink` and `directory.title` — to BOTH `messages/en.json` and `messages/zh-HK.json` (the full directory namespace is finalized in Task 2).

    If, and only if, the browse CTA contract in buildCardView proves insufficient, extend `buildCardView.ts` and add a covering case to its test — otherwise leave both untouched.
  </action>
  <verify>
    <automated>npm install && npx vitest run src/lib/cards/__tests__/buildCardView.test.ts && npx tsc --noEmit</automated>
    <automated>test -z "$(grep -rl recommendCards 'src/app/[locale]/cards' src/components/CardDirectoryClient.tsx 2>/dev/null)"</automated>
    <human-check>Run `npm run dev:skip-check`, open /en/cards and /zh-HK/cards: a grid of every active card renders as browse tiles (type badge, best-for chips, VIEW CARD footer, NO reward-calc row), footers align across columns, and the browser console shows no nested-anchor hydration warning. The home header shows a Directory link that opens /cards.</human-check>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/lib/cards/__tests__/buildCardView.test.ts` green (browse contract intact) — DIR-01.
    - `npx tsc --noEmit` clean.
    - Grep finds no recommendation-engine entrypoint under the directory route or client (read-only) — DIR-01.
    - Observable: /en/cards renders N browse tiles where N equals the active card count from loadCards() (today 11, not hardcoded) — 09-CONTEXT D-05; each tile is a single anchor to /en/cards/[id]; the home header Directory link reaches /cards — 09-CONTEXT D-07.
  </acceptance_criteria>
  <done>Both locales render the full card grid of browse-mode CreditCardCard tiles, each linking to /cards/[id]; the shared card's browse footer is a non-anchor VIEW CARD affordance with ranked mode unchanged; the home header links to the directory; committed.</done>
</task>

<task type="auto">
  <name>Task 2: Provenance banner + full bilingual directory namespace</name>
  <files>src/components/CardDirectoryClient.tsx, messages/en.json, messages/zh-HK.json</files>
  <read_first>
    - `.planning/design/ui-contract-v2.md` section 1 (muted / muted-fg / border tokens), section 3 (flat, hairline, square) — the banner must be a flat hairline surface, not a shadowed callout.
    - `.planning/phases/09-data-page-card-directory/09-CONTEXT.md` D-04 (page-level banner, NOT per-card badges) + `.planning/STATE.md` DEC-DATA-001 (bulk-sourced from public listings, facts-only, provenance-labeled).
    - `src/components/CardDirectoryClient.tsx` (from Task 1, the file being extended) + `src/components/HomeClient.tsx` for useTranslations + section layout conventions.
  </read_first>
  <action>
    In `CardDirectoryClient.tsx`, add a page-level provenance banner at the top of the directory body (above the grid, below the header): a flat, square, muted/hairline surface with an uppercase display eyebrow and an Inter body line stating that some card data is bulk/community-sourced from public listings and may be less authoritative than the primary HK-card corpus (DEC-DATA-001, 09-CONTEXT D-04, satisfying DIR-02 at the page level). Add a directory page title and short subtitle around it. Keep it a SINGLE page-level disclosure — do NOT add per-card badges (deferred, D-04).

    Finalize the `directory` i18n namespace in BOTH `messages/en.json` and `messages/zh-HK.json` with full key parity: navLink, title, subtitle, provenanceEyebrow, provenanceBody, cardCount (a count label fed from the live array length, never a literal number). Author real Traditional-Chinese (zh-HK) copy for every key — no English placeholders, no missing keys (09-CONTEXT D-06). The browse tiles read their existing `results.*` keys (viewCard / cardTypes / bestFor), which already exist in both locales — do not duplicate them.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
    <automated>node -e "const e=require('./messages/en.json').directory,z=require('./messages/zh-HK.json').directory;const k=Object.keys(e);if(k.some(x=>!z[x]))throw new Error('zh-HK missing key');console.log('directory parity ok',k.length)"</automated>
    <human-check>Open /en/cards and /zh-HK/cards: a provenance banner disclosing bulk/community-sourced data sits above the grid, reads naturally in each language, and is a single page-level banner (no per-card badges).</human-check>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` clean; the directory namespace has identical keys in en.json and zh-HK.json with real zh copy (DIR-02 wording bilingual, 09-CONTEXT D-06).
    - Observable: the provenance banner renders once at the top of the directory in both locales — DIR-02 / 09-CONTEXT D-04.
  </acceptance_criteria>
  <done>The directory shows a bilingual page-level provenance banner and title; the directory i18n namespace has full en/zh-HK parity; committed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| visitor browser -> directory route | Untrusted GET; read-only public card data, no auth, no writes, no user data persisted. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09-01-01 | Information disclosure | /[locale]/cards | low | accept | Card corpus is already public product data; the directory exposes nothing not already served by the recommender. |
| T-09-01-02 | Tampering | CardDirectoryClient rendering | low | mitigate | React auto-escapes all card fields; no dangerouslySetInnerHTML, no eval, no new dependency installs in this plan. |

No package installs in this plan — package-legitimacy gate not applicable.
</threat_model>

<verification>
- `npx tsc --noEmit` clean across the new route + client + shared-card change.
- `npx vitest run src/lib/cards/__tests__/buildCardView.test.ts` green — browse contract preserved.
- `npm run build` compiles the new `/[locale]/cards` route (health-check may need env; if it blocks locally, tsc + vitest are the automated gate and build runs in CI).
- Read-only: grep confirms no recommendation-engine call under the directory route/client.
- i18n: the directory namespace has en/zh-HK key parity with real Chinese copy.
</verification>

<success_criteria>
- /en/cards and /zh-HK/cards render a responsive grid of browse-mode CreditCardCard tiles, one per active card (live count) — DIR-01, 09-CONTEXT D-01/D-05.
- Every tile links to /[locale]/cards/[id]; the shared card's browse footer is non-anchor and ranked mode is unchanged.
- A bilingual page-level provenance banner sits atop the directory — DIR-02, 09-CONTEXT D-04.
- The home header links to the directory — 09-CONTEXT D-07.
</success_criteria>

## Artifacts this phase produces (Plan 09-01)

- **New route:** `src/app/[locale]/cards/page.tsx` (server, force-dynamic).
- **New component:** `src/components/CardDirectoryClient.tsx` (client grid + provenance banner + v2 chrome).
- **Modified shared component:** `src/components/CreditCardCard.tsx` (browse-mode non-anchor VIEW CARD footer; ranked mode untouched).
- **Possibly modified:** `src/lib/cards/buildCardView.ts` + its test (only if the browse CTA contract needs a branch).
- **Modified:** `src/components/HomeClient.tsx` (Directory nav link).
- **New i18n keys:** `directory` namespace (navLink, title, subtitle, provenanceEyebrow, provenanceBody, cardCount) in `messages/en.json` + `messages/zh-HK.json`.

<output>
Create `.planning/phases/09-data-page-card-directory/09-01-SUMMARY.md` when done.
</output>
