---
phase: 06-schema-crypto-type-fan-out-backfills
plan: 03
type: execute
wave: 2
depends_on: [06-01]
files_modified:
  - src/components/TransactionInput.tsx
  - src/components/CardRecommendationList.tsx
  - src/components/HomeClient.tsx
  - src/app/admin/cards/[id]/CardEditForm.tsx
  - src/app/admin/cards/[id]/rules/RuleForm.tsx
  - src/app/admin/upload/UploadExtractor.tsx
autonomous: true
requirements: [CRY-01, CRY-02]

must_haves:
  truths:
    - "A crypto reward unit flows through the public UI reward-type controls without a runtime crash"
    - "The admin rule editors offer crypto as a selectable reward unit"
    - "A new card created in admin defaults to (and can set) a cardType"
    - "Every hardcoded cash|miles|points union in the UI/admin layer also accepts crypto"
  artifacts:
    - path: "src/components/CardRecommendationList.tsx"
      provides: "filterRewardType state accepts crypto; crypto filter renders only when present"
      contains: "crypto"
    - path: "src/components/TransactionInput.tsx"
      provides: "RewardType union includes crypto"
      contains: "crypto"
    - path: "src/app/admin/cards/[id]/CardEditForm.tsx"
      provides: "cardType default + selector; crypto reward-unit option"
      contains: "cardType"
    - path: "src/app/admin/cards/[id]/rules/RuleForm.tsx"
      provides: "crypto reward-unit option"
      contains: "crypto"
  key_links:
    - from: "src/app/admin/cards/[id]/CardEditForm.tsx"
      to: "src/lib/data/cardRepository.ts"
      via: "new-card cardType default satisfies validateCard's required-cardType rule"
      pattern: "cardType:\\s*'credit'"
---

<objective>
Complete the reward-unit fan-out (CRY-02) across the public UI and admin authoring forms so a `crypto` reward renders and can be authored, and give the admin create form a `cardType` control + default (CRY-01 authoring) so newly created cards satisfy `validateCard`'s required-cardType rule (added in 06-02). This is the UI/admin half of the fan-out; 06-02 owns the engine/data/AI half. The two plans touch disjoint files and run in the same wave.

Purpose: A crypto card is displayable to visitors and authorable by admins, and no UI reward-type control assumes the closed cash|miles|points set.
Output: Crypto-aware public reward controls + admin rule/type selectors.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@CLAUDE.md
</context>

## Artifacts this phase produces

Phase 6 introduces `CardType`/`cardType`, widened `RewardUnit` (+crypto), `RewardPrograms.crypto`, `RewardCondition.minStaking`, `CreditCard.hkEligible` (06-01), removes `RewardCap` (06-01 types, 06-04 data), backfills `cardType` into `cards.json` (06-01) and prod Redis (06-05), and adds byte-identical guard `scripts/capture-ranking.mts` (06-04). This plan (06-03) produces the public-UI + admin-form reward-unit fan-out and the admin cardType control.

<tasks>

<task type="auto">
  <name>Task 1: Widen the public reward-type controls to tolerate crypto</name>
  <files>src/components/TransactionInput.tsx, src/components/CardRecommendationList.tsx, src/components/HomeClient.tsx</files>
  <read_first>
    - src/components/TransactionInput.tsx (line 8 `type RewardType = 'cash'|'miles'|'points'`; the selector at 205-231 that maps `['cash','miles','points']` with emoji at 224-226)
    - src/components/CardRecommendationList.tsx (line 26 `useState<'all'|'cash'|'miles'|'points'>`; `formatAmount` at 116-123; the per-unit filter buttons at 140-175, each gated on `availableRewardTypes.has(unit)`)
    - src/components/HomeClient.tsx (line 20 `type RewardType = 'cash'|'miles'|'points'`; line 40 `preferredRewardUnits: [selectedRewardType]`)
    - src/lib/engine/calculateReward.ts (getRewardUnitName — 06-02 added its crypto branch; formatAmount here calls it)
  </read_first>
  <action>
    In `TransactionInput.tsx`, widen the local `RewardType` union to include `'crypto'`. The reward-type selector currently hardcodes `['cash','miles','points']`; leave the public quick-selector as cash/miles/points for now (adding a crypto quick-pick is optional and out of scope until crypto cards exist), but ensure the `RewardType` type and any `setSelectedRewardType` usage accept crypto so no type error occurs when a crypto preference is passed. If you keep the emoji map, add a crypto glyph branch so a crypto selection would not render blank.

    In `CardRecommendationList.tsx`, widen the `filterRewardType` state union (line 26) to include `'crypto'`. Add a crypto filter button that renders ONLY when `availableRewardTypes.has('crypto')` (mirror the existing cash/miles/points buttons, counting `r.calculation.rewardUnit === 'crypto'`). `formatAmount`'s non-cash branch already routes through `getRewardUnitName`, which 06-02 taught to name crypto assets — so no crash; confirm crypto amounts render via that path.

    In `HomeClient.tsx`, widen the local `RewardType` union to include `'crypto'` so `preferredRewardUnits: [selectedRewardType]` typechecks against the widened engine union.

    Use the shared `RewardUnit` type from `@/types/card` where a local union is redundant, to avoid future desync.
  </action>
  <acceptance_criteria>
    - `grep -c "crypto" src/components/TransactionInput.tsx` returns at least 1.
    - `grep -c "crypto" src/components/CardRecommendationList.tsx` returns at least 1.
    - `grep -c "crypto" src/components/HomeClient.tsx` returns at least 1.
    - `npx tsc --noEmit` passes (run `npm install` first).
    - `npm run build` succeeds.
  </acceptance_criteria>
  <verify>
    <automated>grep -c "crypto" src/components/CardRecommendationList.tsx</automated>
  </verify>
  <done>Public reward-type controls accept and render crypto; the crypto filter appears only when crypto results exist; the app builds.</done>
</task>

<task type="auto">
  <name>Task 2: Add crypto reward-unit options and a cardType control to the admin forms</name>
  <files>src/app/admin/cards/[id]/CardEditForm.tsx, src/app/admin/cards/[id]/rules/RuleForm.tsx, src/app/admin/upload/UploadExtractor.tsx</files>
  <read_first>
    - src/app/admin/cards/[id]/CardEditForm.tsx (the `emptyCard` default at 24-37 — note it has NO cardType; the reward-unit `<option>` list at 1120-1126; the rewardPrograms handlers `updateRewardPrograms`/`hasRewardType` at 239-261 typed `'miles'|'points'`)
    - src/app/admin/cards/[id]/rules/RuleForm.tsx (the `<option>` list at 357-359; `emptyRule` rewardUnit default)
    - src/app/admin/upload/UploadExtractor.tsx (the `<option>` list at 591-593)
    - src/types/card.ts (CardType + RewardPrograms.crypto from 06-01)
    - src/lib/data/cardRepository.ts (validateCard now requires cardType — from 06-02)
  </read_first>
  <action>
    In `CardEditForm.tsx`: add `cardType: 'credit'` to the `emptyCard` default (so a newly created card carries a valid cardType and passes `validateCard`). Add a small cardType selector control in the card-level fields (a `<select>` with options credit / crypto / prepaid) bound to `card.cardType` via `updateCard`, so an admin can set the type. Add a `<option value="crypto">Crypto</option>` to the reward-unit `<option>` list (alongside cash/miles/points). Extending the `updateRewardPrograms`/`hasRewardType` handlers to also accept `'crypto'` (to name the crypto asset) is in scope if straightforward; if it materially enlarges the change, widen only the reward-unit option + cardType control here and leave crypto-asset naming to the natural JSON authoring path — note which you did in the SUMMARY.

    In `RuleForm.tsx`: add `<option value="crypto">Crypto</option>` to the reward-unit select (after points).

    In `UploadExtractor.tsx`: add `<option value="crypto">Crypto</option>` to the reward-unit select (after points).

    Do not alter save/submit logic or validation flow beyond adding the fields.
  </action>
  <acceptance_criteria>
    - `grep -Ec "cardType:\s*'credit'" src/app/admin/cards/[id]/CardEditForm.tsx` returns at least 1 (emptyCard default).
    - `grep -c "value=\"crypto\"" src/app/admin/cards/[id]/CardEditForm.tsx` returns at least 1.
    - `grep -c "value=\"crypto\"" src/app/admin/cards/[id]/rules/RuleForm.tsx` returns at least 1.
    - `grep -c "value=\"crypto\"" src/app/admin/upload/UploadExtractor.tsx` returns at least 1.
    - `npx tsc --noEmit` passes; `npm run build` succeeds.
  </acceptance_criteria>
  <verify>
    <automated>grep -c "value=\"crypto\"" src/app/admin/cards/[id]/rules/RuleForm.tsx</automated>
  </verify>
  <done>Admin rule editors offer crypto; the card form has a cardType selector and defaults new cards to credit; app builds.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| recommendation result → public UI controls | A crypto reward flows into filter/label controls that assumed a closed cash|miles|points set |
| admin form → validateCard | A newly authored card must carry a cardType or the write is rejected |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-03-01 | Denial of Service | `filterRewardType` / reward-type maps assuming closed set | mitigate | Widen unions to crypto; crypto controls render only when present; formatAmount routes crypto through getRewardUnitName (Task 1) |
| T-06-03-02 | Tampering | New admin card missing cardType | mitigate | emptyCard defaults cardType credit + cardType selector; validateCard (06-02) enforces (Task 2) |
| T-06-03-SC | Tampering | npm/pip/cargo installs | accept | No new package installs in this plan |
</threat_model>

<verification>
- `npm install` then `npx tsc --noEmit` — zero errors.
- `npm run build` succeeds (next build).
- Grep the UI/admin tree to confirm no reward-type union was missed: `grep -rn "'cash' | 'miles' | 'points'" src/components src/app` should only show sites that also include crypto or use the shared RewardUnit type.
- Do NOT run `npm test` / vitest / jest.
</verification>

<success_criteria>
- Public reward controls and admin rule editors accept/render crypto without crashing.
- Admin card form defaults new cards to a cardType and lets an admin set it.
- `npx tsc --noEmit` and `npm run build` are green.
</success_criteria>

<output>
Create `.planning/phases/06-schema-crypto-type-fan-out-backfills/06-03-SUMMARY.md` when done.
</output>
