---
phase: 11-ui-theme-refresh-thi-176
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/globals.css
  - src/app/[locale]/layout.tsx
  - src/components/Logo.tsx
  - src/components/HomeClient.tsx
  - src/components/TypingAnimation.tsx
autonomous: false
requirements: [UI-01]

must_haves:
  truths:
    - "Header renders the CardGPT wordmark with a blinking mint cursor; nav active state is mint (D-01, D-04)"
    - "Light and dark mode both render the v2 (brutalist-editorial) palette, and the dark-mode choice persists across reload (D-03)"
    - "npm run build, npx tsc --noEmit, and npm test all pass — no behavior regressed by the token/font swap (D-02)"
  artifacts:
    - src/app/globals.css
    - src/app/[locale]/layout.tsx
    - src/components/Logo.tsx
    - src/components/HomeClient.tsx
    - src/components/TypingAnimation.tsx
  key_links:
    - "globals.css :root tokens <-> Tailwind v4 @theme inline mapping (every utility class must resolve to a v2 token)"
    - "next-themes .dark class <-> dark-token selector (dark tokens live under .dark, NOT data-theme — this is what keeps persistence working)"
---

<objective>
TRACER for Phase 11: stand up the v2 (brutalist-editorial) design system end-to-end and prove it renders in BOTH themes on the primary surface, before any expansion. Implement the LOCKED contract `.planning/design/ui-contract-v2.md` exactly (per D-01): global tokens (§1), fonts (§2 / D-04), the CardGPT wordmark logo (§4), and the full Home shell (header/nav, hero, features grid, footer) restyled onto the new tokens.

This is a re-skin, NOT a re-architecture (D-02): swap tokens, Tailwind classes, CSS and fonts only — do NOT change component logic, data flow, routing, the engine, or the data layer. Dark mode is KEPT (D-03): the existing next-themes ThemeProvider / DarkModeToggle / persistence mechanism stays exactly as-is; only the token VALUES change.

Purpose: globals.css tokens cascade to every surface — getting them (and the `@theme inline` mapping + fonts) right on the agent's best early tokens catches an architectural dead-end after one plan instead of after four.
Output: the v2 token system, the two new fonts wired, the wordmark logo, and a fully-restyled Home shell that renders correctly in light and dark.
</objective>

<execution_context>
@/Users/jeffreyng/.claude/gsd-core/workflows/execute-plan.md
@/Users/jeffreyng/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/design/ui-contract-v2.md
@.planning/phases/11-ui-theme-refresh-thi-176/11-CONTEXT.md
@src/app/globals.css
@src/app/[locale]/layout.tsx
@src/components/Logo.tsx
@src/components/HomeClient.tsx
@src/components/TypingAnimation.tsx
</context>

<tasks>

<task type="tracer" tdd="false">
  <name>Task 1: v2 tokens + fonts + wordmark logo (end-to-end foundation)</name>
  <files>src/app/globals.css, src/app/[locale]/layout.tsx, src/components/Logo.tsx</files>
  <read_first>
    - .planning/design/ui-contract-v2.md §1 (tokens, light + dark values), §2 (typography families), §3 (shape/flat), §4 (logo)
    - src/app/globals.css (current ChatGPT-teal tokens + existing @theme inline map + the `.dark` + `@media (prefers-color-scheme: dark)` structure being replaced)
    - src/app/[locale]/layout.tsx (current Geist Sans/Mono next/font wiring + body className with font variables)
    - src/components/Logo.tsx (current Image-based icon logo being replaced by a wordmark)
  </read_first>
  <precondition>node_modules is installed (run `npm install`) — required for `npx tsc --noEmit`, `npm run build`, and `npm test`; a fresh clone has none.</precondition>
  <reversibility rating="costly">A global token/theme change touches every component (D-01); getting the tokens + @theme mapping right here is what the rest of the phase builds on.</reversibility>
  <action>
    Replace the ChatGPT-teal / playful-fintech token set in globals.css with the v2 system from contract §1, keeping the EXISTING dark-mode mechanism (next-themes stamps a `.dark` class on `<html>` — see layout.tsx `attribute="class"`).

    Light tokens on `:root` (contract §1): --bg #ffffff, --fg #0b0b0b, --primary #121212, --primary-fg #ffffff, --brand (mint) #67ffc5, --surface #f5f5f5, --muted #f2f2f2, --muted-fg #636363, --border #dedede, --border-strong #121212, --ring #121212, --destructive #e40014. Neon accents: yellow #e1ff67, cyan #4af5fe. Card-type badge colors per §1 (CREDIT #121212; CRYPTO #067a58 light / #67ffc5 dark; PREPAID #1b6ea6 light / #4af5fe dark). Radius: 2px for chips/buttons/inputs, 0px (square) for cards.

    Dark tokens (contract §"Dark theme"): --bg #0b0b0b, --fg #f4f4f4, --primary #f4f4f4, --primary-fg #0b0b0b, --surface #141414, --muted #161616, --muted-fg #9a9a9a, --border #2b2b2b, --border-strong #f4f4f4; mint CTA stays. Write the dark set under BOTH the `.dark` selector AND `@media (prefers-color-scheme: dark) :root:not(.light)` (first-visit system preference) — mirroring the current file's two-block structure. Do NOT introduce `data-theme` selectors; the contract mentions data-theme, but this app's toggle is next-themes class-based (D-03) and switching mechanisms would break persistence.

    Update the `@theme inline` block so every Tailwind v4 utility resolves to the v2 tokens: map --color-bg/--color-fg/--color-primary/--color-primary-fg/--color-brand/--color-surface/--color-muted/--color-muted-fg/--color-border/--color-border-strong/--color-destructive plus the neon + badge colors. Remove the dead teal/purple/orange/blue accent mappings. Add font tokens: --font-display (Rethink Sans var), --font-sans (Inter var), --font-mono (Geist Mono var). Set `body { background: var(--bg); color: var(--fg); font-family: var(--font-sans); }` and give the flat rule: no box-shadow anywhere (separation is borders + background contrast per §3).

    In layout.tsx, load Rethink Sans (weights 700 and 800) and Inter (weights 400-600) via `next/font/google` alongside the retained Geist Mono (D-04): expose each as a CSS variable (e.g. --font-rethink / --font-inter / keep --font-geist-mono) and apply all three variable classes on `<body>`. Drop Geist Sans. Update the `viewport.themeColor` off the old teal (#10a37f) to a v2 value (near-black #121212 or mint per taste). Keep every other layout concern (metadata, ThemeProvider props, NextIntlClientProvider, favicon links) byte-identical.

    Rewrite Logo.tsx as the CardGPT wordmark (contract §4): render the text `CardGPT` in the display face (Rethink Sans) in camelCase with tight tracking, immediately followed by a mint block cursor `▍` (color --brand) that blinks via CSS. Guard the blink behind `prefers-reduced-motion: reduce` (no blink when reduced motion is set). Keep the component's existing props signature (`size`, `className`) so HomeClient's two call sites keep compiling; the `size` prop now scales font-size rather than an image box. No boxes, monochrome + the single mint accent.
  </action>
  <verify>
    <automated>cd /Users/jeffreyng/cardgpt && npx tsc --noEmit && npm test && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` exits 0
    - `npm test` exits 0 (existing vitest suite still green — token/font swap changes no logic)
    - `npm run build` exits 0
    - globals.css defines the v2 light tokens on :root and the v2 dark tokens under the `.dark` selector (dark mechanism unchanged from next-themes class attribute)
    - `@theme inline` maps utility color tokens to the new --bg/--fg/--primary/--brand/--border(-strong) vars (no residual teal/purple/orange/blue accent mappings)
    - layout.tsx loads Rethink Sans + Inter + Geist Mono via next/font/google, each exposed as a CSS var and applied on `<body>`
    - The header logo renders the `CardGPT` wordmark followed by a mint `▍` cursor that blinks in normal motion and does NOT blink under prefers-reduced-motion
  </acceptance_criteria>
  <done>The v2 tokens + `@theme inline` map + the three fonts + the wordmark logo are wired; the app builds, typechecks, and the existing test suite passes; the live header shows the new wordmark + mint cursor in both light and dark.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: restyle the full Home shell (header/nav, hero, features, footer)</name>
  <files>src/components/HomeClient.tsx, src/components/TypingAnimation.tsx</files>
  <read_first>
    - .planning/design/ui-contract-v2.md §2 (type scale: Hero H1 Rethink 800 uppercase clamp(38-62px); Section H2/H3 24-30px uppercase; Body Inter 15-17px), §3 (square/hairline/flat, two-column hero, full-bleed sections separated by hairline rules), §5 Nav (outline buttons; active view mint-filled with 1.5px black border)
    - src/components/HomeClient.tsx (header, hero with the teal->purple->orange gradient H2, TypingAnimation slot, TransactionInput/CardRecommendationList slots, 4-card features grid, footer)
    - src/components/TypingAnimation.tsx (blinking-cursor tagline component in the hero)
  </read_first>
  <action>
    Restyle HomeClient.tsx onto the v2 tokens (D-02 — markup structure, all data flow, the `handleSubmit`/`recommendCards` wiring, the TransactionInput and CardRecommendationList usages, and the `rateTable` prop threading stay untouched; only Tailwind classes change).

    Header/nav: convert the sticky header to a hairline-bottom bar (1px --border), keep the Logo + appName on the left and the DarkModeToggle + LanguageSwitcher on the right. Per contract §5, nav controls are outline buttons (white/--bg + 1px hairline); an active view is mint-filled with a 1.5px --primary border. Drop the backdrop-blur/teal treatment.

    Hero: replace the tri-color gradient `bg-clip-text` H2 with a flat uppercase display headline — Rethink Sans 800, clamp(38-62px), tight negative tracking, color --fg, `text-wrap: balance`. Subtitle/lead in Inter 15-17px, color --muted-fg. Keep the TypingAnimation slot (restyled in this task) and the TransactionInput + conditional CardRecommendationList slots exactly where they are.

    Features grid: replace the four gradient/rounded icon tiles with flat square hairline cells (1px --border, 0 radius, no shadow, no gradient); labels in the display face uppercase, body copy in Inter --muted-fg. Footer: hairline top rule, monochrome; keep all footer links and i18n keys.

    TypingAnimation.tsx: restyle only — the tagline text uses the hero type treatment; the inline cursor uses --brand (mint) or --fg; keep the existing blink and typing behavior, and keep it `aria-hidden` on the cursor. (Behavior — the typing cadence, cursor blink, text cycling — is unchanged.)

    Reuse existing next-intl keys throughout (common.tagline, common.heroDescription, common.animatedTaglines, features.*, footer.*). Do NOT edit messages/en.json or messages/zh-HK.json in this plan (string additions are owned by plan 11-02). Nav discretion (per 11-CONTEXT Claude's Discretion): keep nav minimal — do NOT add disabled Directory/Research entries (those phases build in later).
  </action>
  <verify>
    <automated>cd /Users/jeffreyng/cardgpt && npx tsc --noEmit && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` exits 0
    - `npm run build` exits 0
    - `npm test` still exits 0 (no logic changed)
    - Home header/nav, hero headline (uppercase Rethink display), features grid (flat square hairline cells), and footer all render on the v2 tokens with no residual teal/purple/orange gradient utility classes in HomeClient.tsx
    - The hero still renders TransactionInput and, after a submit, CardRecommendationList (data flow intact)
    - Both light and dark render correctly (no unstyled/black-on-black regions)
  </acceptance_criteria>
  <done>HomeClient renders the full v2 Home shell (header, hero, features, footer) in both themes with all slots and data flow intact; TypingAnimation is restyled with its behavior unchanged.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: human-verify the v2 foundation renders in both themes</name>
  <what-built>The v2 design system foundation: new globals.css tokens (light + dark), Rethink Sans + Inter + Geist Mono fonts, the CardGPT wordmark + mint cursor logo, and the fully-restyled Home shell (header/nav, hero, features, footer). This is the architectural proof before the card + input + chrome surfaces expand out.</what-built>
  <how-to-verify>
    1. `cd /Users/jeffreyng/cardgpt && npm run dev` and open http://localhost:3000
    2. Confirm the header shows the `CardGPT▍` wordmark with a blinking mint cursor and the hero headline is uppercase in the new display face on a white ground with hairline separators (flat, square, no shadows/gradients).
    3. Click the dark-mode toggle: the whole page flips to the dark palette (near-black --bg #0b0b0b, light --fg). Reload the page — the dark choice must PERSIST.
    4. Switch back to light and reload — light must persist.
    5. (Optional) In OS settings enable "reduce motion" and reload — the logo cursor should stop blinking.
  </how-to-verify>
  <resume-signal>Type "approved" or describe what looks wrong (e.g. wrong font, dark mode not persisting, cursor not mint).</resume-signal>
</task>

</tasks>

<threat_model>
No new trust boundary. This plan is a styling-only re-skin: no new inputs, routes, network calls, dependencies (fonts are self-hosted at build by next/font/google — not an npm/CDN add), or data flows are introduced.

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-01 | (none) | tokens/fonts/logo/shell | low | accept | Styling-only change; existing vitest suite + tsc + build guard against behavior regression. No attack surface added. |
</threat_model>

<verification>
- `npm run build` exits 0, `npx tsc --noEmit` exits 0, `npm test` green.
- Human-verify checkpoint confirms both themes render and dark-mode persists.
</verification>

<success_criteria>
The v2 token system, three fonts, wordmark logo, and Home shell are live and render correctly in light AND dark, with persistence intact and every existing test green.
</success_criteria>

<artifacts>
## Artifacts this phase produces (this plan)
- `src/app/globals.css` — v2 brutalist-editorial tokens (light on :root, dark under `.dark` + prefers-color-scheme) + Tailwind v4 `@theme inline` mapping + flat/square conventions
- `src/app/[locale]/layout.tsx` — Rethink Sans + Inter + Geist Mono via next/font/google, exposed as CSS vars
- `src/components/Logo.tsx` — `CardGPT▍` wordmark + blinking mint cursor (reduced-motion aware)
- `src/components/HomeClient.tsx` — full Home shell restyled (header/nav, hero, features grid, footer) on the v2 tokens
- `src/components/TypingAnimation.tsx` — restyled tagline cursor (behavior unchanged)
</artifacts>

<output>
Create `.planning/phases/11-ui-theme-refresh-thi-176/11-01-SUMMARY.md` when done.
</output>
