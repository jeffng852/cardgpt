---
phase: 11-ui-theme-refresh-thi-176
plan: 02
type: execute
wave: 2
depends_on: ["11-01"]
files_modified:
  - src/lib/cards/buildCardView.ts
  - src/lib/cards/__tests__/buildCardView.test.ts
  - src/components/CreditCardCard.tsx
  - src/components/CardRecommendationList.tsx
  - messages/en.json
  - messages/zh-HK.json
autonomous: true
requirements: [UI-01]

must_haves:
  truths:
    - "Submitting a purchase shows ranked v2 brutalist cards; the reward-type filter tabs and expand/collapse breakdown still work (D-02, D-05)"
    - "Result columns align: card name reserves a 2-line min-height, an identical data-grid row set within the view, and footers pin to the bottom (D-06)"
    - "The Apply CTA still carries rel=\"sponsored nofollow noopener\" and renders only when applyUrl exists; a link-less card still appears with 'No apply link · still ranked' (Phase 8 preserved)"
    - "The new buildCardView unit tests pass and the full pre-existing vitest suite (incl. applyCtaProps) stays green"
  artifacts:
    - src/lib/cards/buildCardView.ts
    - src/lib/cards/__tests__/buildCardView.test.ts
    - src/components/CreditCardCard.tsx
    - src/components/CardRecommendationList.tsx
    - messages/en.json
    - messages/zh-HK.json
  key_links:
    - "buildCardView -> CreditCardCard -> CardRecommendationList (shared card wiring; parameterized ranked vs browse so Phase 9's directory reuses it)"
    - "CardRecommendationList -> applyCtaProps (affiliate rel triplet preserved; guarded by the existing applyCtaProps test)"
---

<objective>
Build the shared brutalist card component (contract §5) and restyle the recommender results onto it. This is the crux of the re-skin: per D-05 the card is the SAME component Phase 9's directory will reuse, so build it cleanly and parameterized (ranked vs browse mode) now. Enforce the locked alignment rule (D-06). Implement to `.planning/design/ui-contract-v2.md` §5 exactly (D-01).

Re-skin, not re-architecture (D-02): the ranking engine, the recommendation data shape, the reward-type filter, the expand/collapse interaction, and the Phase 8 affiliate CTA (`rel="sponsored nofollow noopener"`, render-only-when-`applyUrl`) all stay behaviorally identical — only the presentation changes.

Purpose: get the reused card right once, in the final look, with the alignment rule that keeps result columns from drifting.
Output: a `buildCardView` pure helper (unit-tested), a `CreditCardCard` presentational component, and a rewired `CardRecommendationList` that renders ranked cards with all behaviors intact.
</objective>

<execution_context>
@/Users/jeffreyng/.claude/gsd-core/workflows/execute-plan.md
@/Users/jeffreyng/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/design/ui-contract-v2.md
@.planning/phases/11-ui-theme-refresh-thi-176/11-CONTEXT.md
@src/components/CardRecommendationList.tsx
@src/lib/affiliate/applyCtaProps.ts
@src/lib/affiliate/__tests__/applyCtaProps.test.ts
@src/types/card.ts
@src/types/recommendation.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: buildCardView pure helper + node-env vitest tests</name>
  <files>src/lib/cards/buildCardView.ts, src/lib/cards/__tests__/buildCardView.test.ts</files>
  <read_first>
    - .planning/design/ui-contract-v2.md §5 (card structure: header type badge / RECOMMENDED tag; data grid rows Reward · Top reward · Reward type · Annual fee · FX fee; best-for chips; footer CTA states) and §1 card-type badge colors
    - src/lib/affiliate/applyCtaProps.ts + src/lib/affiliate/__tests__/applyCtaProps.test.ts (the pure-helper + node-env test PATTERN to mirror — single-artifact simplicity, no component-render dep)
    - src/types/card.ts (CardType 'credit'|'crypto'|'prepaid' on `card.cardType`; `card.fees.annualFee`, `card.fees.foreignTransactionFeeRate`, `card.applyUrl`, `card.issuer`, `card.name`)
    - src/types/recommendation.ts (CardRecommendation, RuleContribution, calculation.rewardUnit / effectiveRate / ruleBreakdown)
  </read_first>
  <reversibility rating="costly">This view shape is reused by Phase 9's directory (D-05); getting the derived fields + CTA states right here avoids a downstream rework.</reversibility>
  <behavior>
    buildCardView(card, opts) returns a plain view object used by CreditCardCard. Cases to lock in tests (node-env vitest, mirroring applyCtaProps.test.ts):
    - Type badge: card.cardType 'credit' -> badge key CREDIT; 'crypto' -> CRYPTO; 'prepaid' -> PREPAID.
    - Footer CTA state: applyUrl present -> ctaProps === applyCtaProps(applyUrl) (rel "sponsored nofollow noopener", target _blank) and label = APPLY; applyUrl absent -> ctaProps null and a `noApplyLink` flag true (renders "No apply link · still ranked"); browse mode with a link -> label = VIEW CARD.
    - Data grid: in ranked mode with a calculation, `reward` = formatReward(calculation, card) and `topReward` = the max contribution rate across calculation.ruleBreakdown formatted as a percent; `rewardType`, `annualFee` (Free when 0), and `fxFee` (Free when 0/undefined) are populated. In browse mode (no calculation) the reward/topReward rows are omitted, not zeroed.
    - Best-for: derived from existing data only (rule categories / reward unit) — return a bounded list of chip labels; never fabricate a card field that does not exist.
    - `reserveNameLines` is always 2 (alignment rule).
  </behavior>
  <action>
    Create src/lib/cards/buildCardView.ts: a pure, render-free function that maps a CreditCard (+ optional CardRecommendation calculation + a `mode: 'ranked' | 'browse'` and `isTopPick` option) to a typed CardView object with: `typeBadge` (CREDIT|CRYPTO|PREPAID from card.cardType), `isTopPick`, `dataGrid` rows (reward, topReward, rewardType, annualFee, fxFee — reusing formatReward/getRewardUnitName from @/lib/engine/calculateReward and the existing free/percent formatting semantics), `bestFor` chip labels derived from existing rule categories / reward unit, `cta` ({ props: applyCtaProps(card.applyUrl), label, noApplyLink }), and `reserveNameLines: 2`. Delegate the affiliate anchor decision to the existing `applyCtaProps` helper — do NOT re-implement the rel triplet.

    Create src/lib/cards/__tests__/buildCardView.test.ts covering the cases in <behavior> with `import { describe, it, expect } from 'vitest'`, node-env (this repo's vitest is node-env, globals on; no DOM). Write the tests to fail first against a stub, then implement buildCardView until green.
  </action>
  <verify>
    <automated>cd /Users/jeffreyng/cardgpt && npm test -- src/lib/cards && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `npm test -- src/lib/cards` runs the new buildCardView tests and they pass
    - `npm test` (full suite) exits 0 — the pre-existing applyCtaProps test and all engine/data tests stay green
    - `npx tsc --noEmit` exits 0
    - buildCardView calls applyCtaProps for the CTA (rel triplet not re-implemented); returns null cta + noApplyLink=true when applyUrl is absent
    - typeBadge maps credit/crypto/prepaid -> CREDIT/CRYPTO/PREPAID; reserveNameLines === 2
  </acceptance_criteria>
  <done>buildCardView derives the card view (badge, data grid, best-for, CTA state, 2-line reserve) from existing card + recommendation data, is unit-tested in node-env vitest, and the full suite is green.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: CreditCardCard shared brutalist component (contract §5) + card strings</name>
  <files>src/components/CreditCardCard.tsx, messages/en.json, messages/zh-HK.json</files>
  <read_first>
    - .planning/design/ui-contract-v2.md §5 (the card: white/--bg surface, 1px hairline, square, flat; header issuer row + type badge / mint RECOMMENDED tag; hairline data grid with Geist Mono tabular values; best-for neon chips yellow #e1ff67 / cyan #4af5fe; uppercase footer CTA; top pick footer filled mint) and the locked Alignment rule (equal-width columns, name reserves 2 lines, identical data-grid row set, footer pinned bottom via margin-top:auto)
    - .planning/design/ui-contract-v2.md §1 card-type badge colors (CREDIT #121212; CRYPTO #067a58 light / #67ffc5 dark; PREPAID #1b6ea6 light / #4af5fe dark) and §"Neon accents" AA note (darkened variants for text/badges, pure neon only for large fills)
    - src/lib/cardImages.ts (getCardImageUrl / hasCardImage — reuse for the issuer logo bug)
    - messages/en.json + messages/zh-HK.json (results.* namespace; where new keys are added to BOTH files)
  </read_first>
  <action>
    Create src/components/CreditCardCard.tsx: a presentational component that takes a CardView (from buildCardView) plus a `mode: 'ranked' | 'browse'` prop and renders the contract §5 card — square (0 radius), 1px --border hairline, flat (no shadow). Structure, top to bottom: (1) header — small square issuer logo bug (reuse cardImages) + issuer name, with the type badge top-right using the §1 badge colors; the top pick shows a mint `RECOMMENDED` tag instead of a plain badge. Card name in uppercase display face with a reserved 2-line min-height (alignment rule D-06). (2) data grid — hairline label/value rows for Reward / Top reward / Reward type / Annual fee / FX fee; values in Geist Mono with tabular-nums; positive reward may use the mint-dark accent. (3) best-for — neon chips (yellow/cyan for active/selected, black-filled for the rest) using the AA-safe variants for text. (4) footer — uppercase CTA label from the CardView (`APPLY →` / `VIEW CARD →`); when noApplyLink, render "No apply link · still ranked"; the top pick footer is filled mint. Pin the footer to the bottom with margin-top:auto so all footers align across columns.

    When cta.props exists, spread them onto the `<a>` (they already carry the D-05 rel triplet + target). Use the v2 tokens/utilities from plan 11-01 (no hard-coded teal). Keep the component parameterized and self-contained so Phase 9's directory can render it in browse mode later (D-05) — but this plan only exercises ranked mode.

    Add the new UI strings to BOTH messages/en.json and messages/zh-HK.json (bilingual — next-intl): the type-badge labels (CREDIT/CRYPTO/PREPAID — or reuse an existing uppercase treatment), the `VIEW CARD` CTA, and the "No apply link · still ranked" line (`results.noApplyLink`). Provide accurate zh-HK translations. Reuse existing keys where they already exist (results.recommended, results.applyHere, results.annualFee, results.free, results.breakdown.fxFee).
  </action>
  <verify>
    <automated>cd /Users/jeffreyng/cardgpt && npx tsc --noEmit && node -e "JSON.parse(require('fs').readFileSync('messages/en.json','utf8'));JSON.parse(require('fs').readFileSync('messages/zh-HK.json','utf8'));console.log('json ok')" && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` exits 0 and `npm run build` exits 0
    - Both messages/en.json and messages/zh-HK.json parse as valid JSON and contain every new key in BOTH files (no English-only leakage)
    - CreditCardCard renders a square, flat, hairline card with a header type badge (or mint RECOMMENDED tag on the top pick), a Geist-Mono tabular data grid, best-for neon chips, and an uppercase footer CTA pinned to the bottom
    - The card name reserves a 2-line min-height (alignment rule D-06)
    - A CardView with no apply link renders "No apply link · still ranked" and no anchor
  </acceptance_criteria>
  <done>CreditCardCard renders the contract §5 brutalist card, parameterized ranked/browse, with the alignment rule enforced and new strings present in both languages.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: rewire CardRecommendationList to render ranked CreditCardCard (preserve all behavior)</name>
  <files>src/components/CardRecommendationList.tsx</files>
  <read_first>
    - src/components/CardRecommendationList.tsx (the reward-type filter tabs all/cash/miles/points/crypto; the expand/collapse breakdown; the loading + no-results states; the applyCtaProps-driven Apply anchor at the bottom of the expanded detail)
    - .planning/design/ui-contract-v2.md §5 (card + footer states) and §"Alignment rule"
    - src/components/CreditCardCard.tsx (the component built in Task 2 — the render target)
  </read_first>
  <action>
    Rewire CardRecommendationList.tsx to compose CreditCardCard (via buildCardView) in `mode="ranked"`, marking index 0 as the top pick (RECOMMENDED / mint footer). This is a presentation swap (D-02): the props contract (`recommendations`, `isLoading`) is unchanged, and every behavior is preserved —
    - the reward-type filter tabs (all / cash / miles / points / crypto with counts) stay and keep filtering `filteredRecommendations`;
    - the per-card expand/collapse detail (rule breakdown with rates/amounts, caps, expiry/action alerts, fees, min-income) stays — restyle it into the card's expandable region using hairlines, not the old slate/emerald/amber gradient tags;
    - the loading spinner state and the empty/no-results state are restyled onto v2 tokens but keep their copy/keys;
    - the Apply CTA continues to render ONLY when `card.applyUrl` exists and continues to use `applyCtaProps(card.applyUrl)!` so the `rel="sponsored nofollow noopener"` + `target="_blank"` treatment is byte-identical (Phase 8 / AFF-01). Do NOT drop or inline the rel — the existing applyCtaProps test must stay green.
    Apply the alignment rule across the rendered list (equal-width columns where a grid is used, identical data-grid rows, footers pinned to bottom).
  </action>
  <verify>
    <automated>cd /Users/jeffreyng/cardgpt && npx tsc --noEmit && npm test && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` exits 0, `npm test` exits 0 (applyCtaProps + full suite green), `npm run build` exits 0
    - CardRecommendationList renders CreditCardCard for each recommendation; the top result shows the RECOMMENDED / mint-footer treatment
    - The reward-type filter tabs and the expand/collapse breakdown both still function (behavior preserved)
    - The Apply anchor still spreads applyCtaProps output (rel="sponsored nofollow noopener", target _blank) and only renders when applyUrl exists; a link-less card still appears
    - Both light and dark render the results with no residual teal/emerald/amber gradient utilities
  </acceptance_criteria>
  <done>The recommender results render as ranked v2 brutalist cards with the filter, expand, affiliate-CTA, and link-less-card behaviors all intact and the full test suite green.</done>
</task>

</tasks>

<threat_model>
No new trust boundary. Styling + a pure view-builder over existing recommendation data; no new inputs, routes, network calls, or dependencies. The one behavior touched with security relevance — the affiliate Apply anchor — is preserved by delegating to the existing `applyCtaProps` helper and its passing test (rel="sponsored nofollow noopener").

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-02 | Tampering | Apply CTA rel treatment | low | mitigate | Reuse applyCtaProps (D-05 rel triplet); the existing applyCtaProps.test.ts stays in the suite and must pass, guarding the tab-nabbing / affiliate-signal behavior. |
</threat_model>

<verification>
- `npm test` green (buildCardView + applyCtaProps + full suite); `npx tsc --noEmit` and `npm run build` exit 0.
- Both message files valid JSON with new keys in both languages.
</verification>

<success_criteria>
The shared brutalist card is built, parameterized, and alignment-correct; the recommender results render on it with the filter, expand, affiliate-CTA, and link-less-card behaviors preserved and the whole suite green.
</success_criteria>

<artifacts>
## Artifacts this phase produces (this plan)
- `src/lib/cards/buildCardView.ts` — pure view-builder (type badge, data grid, best-for, CTA state, 2-line reserve) reused by the Phase 9 directory
- `src/lib/cards/__tests__/buildCardView.test.ts` — node-env vitest tests (mirrors the applyCtaProps pattern)
- `src/components/CreditCardCard.tsx` — shared brutalist card (contract §5), parameterized ranked/browse
- `src/components/CardRecommendationList.tsx` — rewired to ranked CreditCardCard, all behaviors preserved
- `messages/en.json` + `messages/zh-HK.json` — new card strings (type badges, VIEW CARD, no-apply-link) in both languages
</artifacts>

<output>
Create `.planning/phases/11-ui-theme-refresh-thi-176/11-02-SUMMARY.md` when done.
</output>
