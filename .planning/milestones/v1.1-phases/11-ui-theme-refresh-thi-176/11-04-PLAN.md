---
phase: 11-ui-theme-refresh-thi-176
plan: 04
type: execute
wave: 2
depends_on: ["11-01"]
files_modified:
  - src/components/HowItWorks.tsx
  - src/components/DarkModeToggle.tsx
  - src/components/LanguageSwitcher.tsx
autonomous: true
requirements: [UI-01]

must_haves:
  truths:
    - "The nav chrome — dark-mode toggle + EN·繁 language switch — renders in the v2 system with outline / mint-active treatment (D-01)"
    - "Dark-mode toggle + persistence and en<->zh-HK locale switching still work exactly as before (D-02, D-03)"
    - "How-It-Works renders as flat square hairline cards and its Try-Now scroll-to-input still focuses the input"
    - "npm run build and npx tsc --noEmit exit 0; the existing vitest suite stays green"
  artifacts:
    - src/components/HowItWorks.tsx
    - src/components/DarkModeToggle.tsx
    - src/components/LanguageSwitcher.tsx
  key_links:
    - "DarkModeToggle -> next-themes setTheme (theme choice + persistence preserved)"
    - "LanguageSwitcher -> i18n router.replace(pathname,{locale}) (locale switch preserved)"
---

<objective>
Restyle the chrome and the How-It-Works section onto the v2 system (contract §5 Nav + §3 shape, D-01): the dark-mode toggle, the language switcher, and the three-step How-It-Works cards. Implement to `.planning/design/ui-contract-v2.md` exactly.

Re-skin, not re-architecture (D-02): the dark-mode toggle mechanism + persistence (D-03) and the locale-switch behavior stay byte-identical — only Tailwind classes / markup styling change.

Purpose: finish the surface so all existing chrome + secondary sections match the v2 look while their behavior is untouched.
Output: restyled DarkModeToggle, LanguageSwitcher, and HowItWorks.
</objective>

<execution_context>
@/Users/jeffreyng/.claude/gsd-core/workflows/execute-plan.md
@/Users/jeffreyng/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/design/ui-contract-v2.md
@.planning/phases/11-ui-theme-refresh-thi-176/11-CONTEXT.md
@src/components/HowItWorks.tsx
@src/components/DarkModeToggle.tsx
@src/components/LanguageSwitcher.tsx
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: restyle DarkModeToggle + LanguageSwitcher (nav chrome) — behavior preserved</name>
  <files>src/components/DarkModeToggle.tsx, src/components/LanguageSwitcher.tsx</files>
  <read_first>
    - .planning/design/ui-contract-v2.md §5 Nav (outline buttons; active view mint-filled with a 1.5px black border; right side = language `EN · 繁` + dark-mode toggle) and §5 Buttons
    - src/components/DarkModeToggle.tsx (next-themes theme/setTheme; the mounted placeholder to avoid hydration mismatch; sun/moon icon crossfade; aria-labels)
    - src/components/LanguageSwitcher.tsx (useLocale + i18n router.replace(pathname,{locale}) + router.refresh; the EN / 中 buttons; the active-locale styling)
  </read_first>
  <action>
    Restyle DarkModeToggle.tsx onto v2 tokens (D-02, D-03 — keep next-themes theme/setTheme, the `mounted` placeholder, the isDark logic, the sun/moon icons, and the aria-labels EXACTLY; the toggle and its persistence must not change). Render it as a v2 outline icon button (white/--bg, 1px hairline, 2px radius, hover to --surface/--muted); recolor the placeholder pulse and hover ring off the old teal onto v2 tokens.

    Restyle LanguageSwitcher.tsx onto v2 tokens (D-02 — keep useLocale, handleChange -> router.replace(pathname,{locale}) + router.refresh, and the routing.locales map). Per §5, render the two locales as `EN · 繁` outline buttons; the active locale is mint-filled with a 1.5px --primary border (matching the nav active-state), inactive is white + 1px hairline. Change the zh-HK button label from `中` to `繁` (a display literal, not an i18n key) to match the contract; keep the aria-labels and the switch behavior. Remove the console.log debug lines.

    Reuse existing keys/aria strings; do NOT edit messages/en.json or messages/zh-HK.json in this plan.
  </action>
  <verify>
    <automated>cd /Users/jeffreyng/cardgpt && npx tsc --noEmit && npm test && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` exits 0, `npm test` exits 0, `npm run build` exits 0
    - DarkModeToggle is a v2 outline icon button; clicking it still flips theme via next-themes and the choice still persists across reload (mechanism unchanged)
    - LanguageSwitcher renders `EN · 繁` with the active locale mint-filled (1.5px black border); clicking still switches locale via router.replace and refreshes
    - No residual teal (`bg-background-secondary`/`bg-primary`-teal/gray-900) chrome classes; no console.log debug lines remain
  </acceptance_criteria>
  <done>The dark-mode toggle and language switch render as v2 nav chrome (outline / mint-active, EN·繁) with their theme-persistence and locale-switch behaviors intact.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: restyle HowItWorks (three-step section) — Try-Now behavior preserved</name>
  <files>src/components/HowItWorks.tsx</files>
  <read_first>
    - .planning/design/ui-contract-v2.md §2 (Section H2/H3 Rethink 24-30px uppercase), §3 (square cards 0 radius, 1px hairlines, flat — no shadows/blur/gradients; full-bleed sections separated by hairline rules), §5 Buttons
    - src/components/HowItWorks.tsx (three steps with gradient accentColor/accentLight + blur decorations; the section header; the Try-Now button that scrolls to input[type=text] and focuses it)
  </read_first>
  <action>
    Restyle HowItWorks.tsx onto v2 tokens (D-02 — keep the steps data, the useTranslations('howItWorks') keys, and the Try-Now scroll+focus handler EXACTLY). Replace the gradient step badges, gradient/blur background decorations, rounded cards, and shadows with the v2 treatment: flat square cards (0 radius, 1px --border hairline, no shadow), uppercase display headings (Rethink 24-30px), body in Inter --muted-fg, a single mint/neon accent at most for the step number. Remove the blurred color blobs (§3 flat rule). The Try-Now CTA becomes a mint-fill + 1.5px #121212 border uppercase button; keep its onClick that queries `input[type="text"]`, scrolls it into view, and focuses it. Reuse existing i18n keys; do NOT edit message files.
  </action>
  <verify>
    <automated>cd /Users/jeffreyng/cardgpt && npx tsc --noEmit && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` exits 0 and `npm run build` exits 0; `npm test` still exits 0
    - HowItWorks renders three flat square hairline step cards with uppercase display headings and no gradients/blur/shadows
    - The Try-Now button still scrolls to and focuses the transaction input
    - Both light and dark render correctly
  </acceptance_criteria>
  <done>HowItWorks renders as a flat brutalist three-step section on the v2 tokens with the Try-Now scroll-to-input behavior intact.</done>
</task>

</tasks>

<threat_model>
No new trust boundary. Styling-only re-skin of chrome + a static section; the theme mechanism, locale router, and scroll handler are unchanged, and no new inputs/routes/deps are introduced.

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-04 | (none) | DarkModeToggle / LanguageSwitcher / HowItWorks | low | accept | Presentation-only; theme persistence + locale switch preserved; existing suite + tsc + build guard against regression. |
</threat_model>

<verification>
- `npm run build` + `npx tsc --noEmit` exit 0; existing vitest suite green.
- Manual: dark toggle persists, language switch works, Try-Now focuses the input.
</verification>

<success_criteria>
The chrome and How-It-Works render in the v2 system with dark-mode persistence, locale switching, and the Try-Now scroll behavior all preserved.
</success_criteria>

<artifacts>
## Artifacts this phase produces (this plan)
- `src/components/DarkModeToggle.tsx` — v2 outline icon button; next-themes toggle + persistence preserved
- `src/components/LanguageSwitcher.tsx` — `EN · 繁` outline/mint-active nav switch; locale-switch behavior preserved
- `src/components/HowItWorks.tsx` — flat square hairline three-step section; Try-Now scroll-to-input preserved
</artifacts>

<output>
Create `.planning/phases/11-ui-theme-refresh-thi-176/11-04-SUMMARY.md` when done.
</output>
