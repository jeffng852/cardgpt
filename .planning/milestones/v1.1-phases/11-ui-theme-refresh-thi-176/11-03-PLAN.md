---
phase: 11-ui-theme-refresh-thi-176
plan: 03
type: execute
wave: 2
depends_on: ["11-01"]
files_modified:
  - src/components/TransactionInput.tsx
  - src/components/FloatingCards.tsx
autonomous: true
requirements: [UI-01]

must_haves:
  truths:
    - "The describe-your-purchase input, reward-type selector, and quick tags render in the v2 (brutalist-editorial) system (D-01)"
    - "Every input behavior still works: real-time parse + debounce, the AI category fallback (/api/parse-activity), tag->input population, character limit, and onSubmit(result, rewardType) (D-02)"
    - "npm run build and npx tsc --noEmit exit 0; the existing vitest suite stays green"
  artifacts:
    - src/components/TransactionInput.tsx
    - src/components/FloatingCards.tsx
  key_links:
    - "TransactionInput.onSubmit contract unchanged — HomeClient.handleSubmit still receives ParseResult + optional RewardType"
    - "FloatingCards prefers-reduced-motion guard preserved (returns null when reduce is set)"
---

<objective>
Restyle the recommender INPUT surface onto the v2 system (contract §5 inputs/chips/buttons, D-01): the describe-your-purchase form, the reward-type selector, the merchant quick-tags, the detected-info badges, and the FloatingCards background. Implement to `.planning/design/ui-contract-v2.md` exactly.

Re-skin, not re-architecture (D-02): the parse/debounce logic, the AI category fallback, the tag->input population, the max-length enforcement, and the `onSubmit(result, selectedRewardType)` contract all stay behaviorally identical — only Tailwind classes / markup styling change.

Purpose: the input is the entry point of the recommendation flow — it must look v2 while behaving exactly as before.
Output: a restyled TransactionInput and FloatingCards with all interactions intact.
</objective>

<execution_context>
@/Users/jeffreyng/.claude/gsd-core/workflows/execute-plan.md
@/Users/jeffreyng/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/design/ui-contract-v2.md
@.planning/phases/11-ui-theme-refresh-thi-176/11-CONTEXT.md
@src/components/TransactionInput.tsx
@src/components/FloatingCards.tsx
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: restyle TransactionInput (chips, input, badges, submit) — behavior preserved</name>
  <files>src/components/TransactionInput.tsx</files>
  <read_first>
    - .planning/design/ui-contract-v2.md §5 Inputs/selects (white, 1px --border-strong, 2px radius, Inter 14-16px; the simulator's main input uses a heavier 2px border; visible focus = 2px mint outline), §5 Chips (selectable: white + 1px black border; selected -> filled neon yellow/cyan, keep the black border), §5 Buttons (primary/active = mint fill + 1.5px #121212 border, 2px radius, uppercase display label)
    - src/components/TransactionInput.tsx (reward-type selector cash/miles/points; quickTags mcdonalds/wellcome/parknshop/sushiro/shell/cathay; handleInputChange debounce + parseTransaction; parseActivityWithAI fallback; handleQuickTag tag->input; the detected amount/merchant/category badges; the submit button + shimmer states)
  </read_first>
  <action>
    Restyle TransactionInput.tsx onto the v2 tokens (D-02 — all handlers, state, the 80-char MAX_INPUT_LENGTH, the 300ms+200ms debounce, the /api/parse-activity fallback, handleQuickTag, and the onSubmit call stay untouched; only classes/markup styling change).

    - Reward-type selector (cash / miles / points): render as selectable chips per §5 — white + 1px --border-strong, 2px radius; the selected type becomes a filled neon chip (yellow #e1ff67 or cyan #4af5fe) keeping the black border; label in the display face. Keep the single-select toggle behavior and the crypto branch (already present in the type union).
    - Quick tags: same chip treatment; keep the select/deselect + tag<->input mutation exactly.
    - Main input container: white, 2px --border-strong, 2px radius, Inter 15-16px; replace the rounded/teal/shadow focus ring with a 2px mint (--brand) focus outline. Keep the character-count indicator and its at-limit state (restyle color to --destructive or a neon, not amber teal).
    - Detected-info badges (amount / merchant / category) + the shimmer loading skeleton: restyle onto hairline surfaces (1px --border, --surface bg), monochrome + a single accent; keep the shimmer keyframe usage and the analyzing/parsed conditional logic.
    - Submit button: mint fill + 1.5px #121212 border, 2px radius, uppercase display label; keep the disabled + spinner/analyzing states and copy keys.
    - AI error block: restyle to a hairline --destructive treatment; keep the message + rate-limit copy.

    Fix the pre-existing dead token classes as you migrate (the file references `text-text-secondary`, `bg-surface`, `bg-input-bg`, `text-text-primary` which are NOT mapped in Tailwind v4 — replace them with real v2 utilities so styles actually apply). Reuse existing next-intl keys (input.*, rewardTypes.*, results.*); do NOT edit messages/en.json or messages/zh-HK.json in this plan.
  </action>
  <verify>
    <automated>cd /Users/jeffreyng/cardgpt && npx tsc --noEmit && npm test && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` exits 0, `npm test` exits 0, `npm run build` exits 0
    - Reward-type + quick-tag chips render as white/1px-black, selected -> filled neon with black border; the main input has a 2px border and a 2px mint focus outline; the submit button is mint-filled uppercase
    - Typing a purchase still triggers the debounced parse + detected badges; a no-category amount still calls the AI fallback; clicking a quick tag still populates/depopulates the input; submit still calls onSubmit with the parsed result + selected reward type
    - No residual teal/`bg-primary`-teal or unmapped `text-text-*`/`bg-surface`/`bg-input-bg` classes remain in the file
  </acceptance_criteria>
  <done>TransactionInput renders on the v2 system with brutalist chips, a 2px-border input, mint focus + submit, and every parse/AI/tag/submit behavior intact.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: restyle FloatingCards background (monochrome, flat) — reduced-motion preserved</name>
  <files>src/components/FloatingCards.tsx</files>
  <read_first>
    - .planning/design/ui-contract-v2.md §3 (square, flat, hairline; no shadows; monochrome + sparse accent)
    - src/components/FloatingCards.tsx (random floating card motifs; the prefers-reduced-motion guard that returns null; the resize regeneration; the float keyframes)
  </read_first>
  <action>
    Restyle FloatingCards.tsx card motifs onto the v2 look (D-02 — keep the generation logic, the resize listener, the float keyframes, and CRITICALLY the `prefers-reduced-motion: reduce` guard that returns null). Replace the teal gradient + rounded + shadow card mock with a flat, square, low-opacity monochrome motif using v2 tokens (hairline --border edges, --muted/--surface fills; no box-shadow, no gradient). Per 11-CONTEXT Claude's Discretion, you may simplify the motif (fewer decorative sub-elements) as long as it stays a subtle background and the reduced-motion + performance behavior is unchanged.
  </action>
  <verify>
    <automated>cd /Users/jeffreyng/cardgpt && npx tsc --noEmit && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` exits 0 and `npm run build` exits 0
    - FloatingCards motifs are flat/square/monochrome on v2 tokens (no teal gradient, no shadow)
    - The component still returns null under prefers-reduced-motion and still regenerates on resize
  </acceptance_criteria>
  <done>FloatingCards renders a subtle flat monochrome background consistent with the v2 system, with the reduced-motion guard intact.</done>
</task>

</tasks>

<threat_model>
No new trust boundary. Styling-only re-skin of the input surface; the AI fallback network call, parse logic, and submit contract are unchanged, and no new inputs/routes/deps are introduced.

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-03 | (none) | TransactionInput / FloatingCards | low | accept | Presentation-only; existing suite + tsc + build guard against behavior regression. |
</threat_model>

<verification>
- `npm run build` + `npx tsc --noEmit` exit 0; existing vitest suite green.
- Manual: parse, AI fallback, quick-tag population, and submit all still work.
</verification>

<success_criteria>
The recommender input surface renders in the v2 system with every input/parse/AI/tag/submit behavior preserved.
</success_criteria>

<artifacts>
## Artifacts this phase produces (this plan)
- `src/components/TransactionInput.tsx` — v2 chips, 2px-border input with mint focus, mint uppercase submit; all parse/AI/tag/submit behavior preserved (and pre-existing dead token classes fixed)
- `src/components/FloatingCards.tsx` — flat monochrome square background motif; reduced-motion guard preserved
</artifacts>

<output>
Create `.planning/phases/11-ui-theme-refresh-thi-176/11-03-SUMMARY.md` when done.
</output>
