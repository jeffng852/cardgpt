---
phase: 09-data-page-card-directory
plan: 02
type: execute
wave: 2
depends_on: ["09-01"]
files_modified:
  - src/app/[locale]/cards/[id]/page.tsx
  - src/components/CardRecommendationList.tsx
  - messages/en.json
  - messages/zh-HK.json
autonomous: true
requirements: [DIR-01, DIR-02, DIR-03]
must_haves:
  truths:
    - "/[locale]/cards/[id] renders a single card's full detail (all reward rules with bilingual descriptions, all fees, apply CTA) in v2 styling, loaded server-side via getCardById(id) (09-CONTEXT D-02)."
    - "An unknown id at /cards/[id] returns notFound() — a 404 / not-found UI, never a crash or blank page (09-CONTEXT D-02)."
    - "The detail page labels HK-availability (from hkEligible), a last-verified date (from getDatabaseMetadata), and a provenance note — so bulk data never reads as authoritative (DIR-02, 09-CONTEXT D-04)."
    - "The apply CTA reuses applyCtaProps (rel sponsored nofollow noopener, target _blank); a card with no applyUrl shows no apply link (AFF-01 reuse)."
    - "A recommendation result deep-links into /[locale]/cards/[id] (DIR-03, 09-CONTEXT D-07)."
    - "The detail route is read-only — it makes NO recommendation-engine call (DIR-01)."
  artifacts:
    - "src/app/[locale]/cards/[id]/page.tsx — server detail route (getCardById + notFound + provenance labels + apply CTA)"
    - "src/components/CardRecommendationList.tsx — recommender-result deep-link into the detail route"
    - "messages/en.json + messages/zh-HK.json — cardDetail.* namespace"
  key_links:
    - "detail page getCardById(id) -> notFound() on undefined"
    - "detail apply anchor -> applyCtaProps(card.applyUrl)"
    - "detail last-verified/HK-availability <- getDatabaseMetadata() + card.hkEligible"
    - "recommender expanded region -> next-intl Link -> /[locale]/cards/[id]"
  deferred:
    - "crypto-specific detail sections (staking tiers) — deferred until real crypto cards land (RQ-001)."
    - "per-card provenance badges on the grid — detail-page labels only in v1 (09-CONTEXT D-04)."
---

<objective>
Add the per-card detail route `/[locale]/cards/[id]` (server, getCardById + notFound), label its provenance (HK-availability + last-verified + note), reuse the affiliate apply CTA, and wire the recommender-result deep-link into it.

Purpose: complete the browse -> detail loop the tracer's tile links point at, and satisfy the DIR-02 per-entry labeling + the DIR-03 recommender deep-link.

Output: a shareable, SEO-friendly card detail page in the v2 system, plus a deep-link from every recommendation result.
</objective>

<execution_context>
@/Users/jeffreyng/.claude/gsd-core/workflows/execute-plan.md
@/Users/jeffreyng/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-data-page-card-directory/09-CONTEXT.md
@.planning/design/ui-contract-v2.md

# Reuse targets (read before implementing)
@src/components/CreditCardCard.tsx
@src/lib/data/loadCards.ts
@src/lib/affiliate/applyCtaProps.ts
@src/components/CardRecommendationList.tsx
@src/types/card.ts
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Card detail route — getCardById + notFound + v2 detail layout</name>
  <files>src/app/[locale]/cards/[id]/page.tsx</files>
  <read_first>
    - `.planning/design/ui-contract-v2.md` sections 1-3 + 5 — tokens, type scale, hairline/flat/square shape, and the data-grid treatment to echo on the detail layout (Geist Mono tabular values).
    - `src/lib/data/loadCards.ts` — `getCardById(id)` (async, returns CreditCard | undefined) and `getDatabaseMetadata()` (lastUpdated / nextReviewDate / version).
    - `src/lib/affiliate/applyCtaProps.ts` — the locked rel triplet to reuse for the apply anchor.
    - `src/components/CreditCardCard.tsx` — mirror its issuer-bug + type-badge header, hairline label/value rows, and v2 footer CTA styling in the (bespoke, fuller) detail layout.
    - `src/app/[locale]/page.tsx` — the server-component + force-dynamic + async-params pattern (params is a Promise; await it).
  </read_first>
  <action>
    Create `src/app/[locale]/cards/[id]/page.tsx` as a server component with `export const dynamic = 'force-dynamic'`. Await the route params to read `id`, call `getCardById(id)`, and call `notFound()` from `next/navigation` when it returns undefined. Do NOT import or call the recommendation engine — the detail page is read-only (DIR-01).

    Render a bespoke v2 detail layout (NOT the CreditCardCard tile — this is a fuller page): reuse HomeClient/CardDirectoryClient header chrome (Logo linking home, DarkModeToggle, LanguageSwitcher) plus a back-to-directory next-intl Link; a header block with issuer bug, type badge, and the card name in the uppercase display face; a full reward-rules section listing every rule in `card.rewards` (rate as percent, description, using `description_zh` when the locale is zh-HK, mirroring CardRecommendationList's locale-aware text helper); a fees block (annual fee, FX fee, and any redemption fee) with Geist Mono tabular values; and an apply CTA footer that reuses `applyCtaProps(card.applyUrl)` — render the anchor only when props are non-null, otherwise show the no-apply-link line. Pull all copy from the `cardDetail` i18n namespace (finalized in Task 2). Set the page metadata title from the card name for SEO/shareability.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
    <automated>test -z "$(grep -rl recommendCards 'src/app/[locale]/cards/[id]' 2>/dev/null)"</automated>
    <human-check>Run `npm run dev:skip-check`. Click a directory tile at /en/cards -> lands on /en/cards/&lt;id&gt; showing that card's rules + fees + apply CTA in v2 styling. Visit /en/cards/does-not-exist -> a 404 / not-found page. Switch to /zh-HK/cards/&lt;id&gt; -> rule descriptions render in Chinese where description_zh exists.</human-check>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` clean — DIR-01.
    - Observable: a valid /en/cards/[id] renders the card's full detail (rules + fees + apply CTA) — DIR-01 / 09-CONTEXT D-02; an unknown id returns notFound (404) — 09-CONTEXT D-02.
    - Grep confirms no recommendation-engine call in the detail route (read-only) — DIR-01.
    - The apply anchor carries rel sponsored nofollow noopener via applyCtaProps; a link-less card shows no apply anchor.
  </acceptance_criteria>
  <done>/[locale]/cards/[id] renders full card detail via getCardById with notFound() on unknown ids, in v2 styling, in both locales; committed.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Provenance labels (HK-availability + last-verified + note) + bilingual cardDetail namespace</name>
  <files>src/app/[locale]/cards/[id]/page.tsx, messages/en.json, messages/zh-HK.json</files>
  <read_first>
    - `.planning/phases/09-data-page-card-directory/09-CONTEXT.md` D-04 + `.planning/STATE.md` DEC-DATA-001 (provenance labeling) + REQUIREMENTS.md DIR-02 (provenance + last-verified + HK-availability).
    - `src/types/card.ts` — `hkEligible?: boolean` (undefined means eligible for the 11 legacy cards per CRY-05; only an explicit false marks not-available).
    - `src/lib/data/loadCards.ts` — `getDatabaseMetadata()` fields (lastUpdated, nextReviewDate) to source the last-verified date.
  </read_first>
  <action>
    Extend `src/app/[locale]/cards/[id]/page.tsx` with a provenance/availability block satisfying DIR-02 per entry: an HK-availability label derived from `card.hkEligible` (explicit false renders the not-available-to-HK-residents label; true or undefined renders available); a last-verified date sourced from `getDatabaseMetadata()` (use lastUpdated, formatted for the active locale); and a short provenance note echoing DEC-DATA-001 that some data is bulk/community-sourced and may be less authoritative. Style as a flat hairline block per contract section 3.

    Add the `cardDetail` i18n namespace to BOTH `messages/en.json` and `messages/zh-HK.json` with full key parity: backToDirectory, rewardRules, fees, annualFee, fxFee, redemptionFee, apply, noApplyLink (or reuse results.noApplyLink), hkAvailable, hkNotAvailable, lastVerified, provenanceNote, plus any section titles the layout needs. Author real Traditional-Chinese copy for every key — no placeholders, no missing keys (09-CONTEXT D-06).
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
    <automated>node -e "const e=require('./messages/en.json').cardDetail,z=require('./messages/zh-HK.json').cardDetail;const k=Object.keys(e);if(k.some(x=>!z[x]))throw new Error('zh-HK missing key');console.log('cardDetail parity ok',k.length)"</automated>
    <human-check>On /en/cards/&lt;id&gt; and /zh-HK/cards/&lt;id&gt;: the detail page shows an HK-availability label, a last-verified date, and a provenance note, all reading naturally in each language. A card with hkEligible=false shows the not-available label.</human-check>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` clean; cardDetail namespace has identical keys in both message files with real zh copy — DIR-02, 09-CONTEXT D-06.
    - Observable: every detail page shows provenance + last-verified + HK-availability — DIR-02, 09-CONTEXT D-04.
  </acceptance_criteria>
  <done>The detail page labels HK-availability, last-verified date, and provenance in both locales; cardDetail i18n has full parity; committed.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: DIR-03 recommender deep-link into the detail route</name>
  <files>src/components/CardRecommendationList.tsx, messages/en.json, messages/zh-HK.json</files>
  <read_first>
    - `src/components/CardRecommendationList.tsx` — the ranked card head is a toggle BUTTON and the footer is the APPLY anchor, so the deep-link must go in the expandable `children` detail region (a plain div — safe to nest a Link). Reuse the existing `getDescription` locale-aware pattern.
    - `src/i18n/routing.ts` — import `Link` from `@/i18n/routing` for locale-aware navigation.
    - `.planning/phases/09-data-page-card-directory/09-CONTEXT.md` D-07 (recommender deep-link) + REQUIREMENTS.md DIR-03.
  </read_first>
  <action>
    In `CardRecommendationList.tsx`, add a deep-link inside the expandable detail region (the `children` block already rendered per card, e.g. after the fees/min-income lines) — a next-intl `Link` to the card path `/cards/<card.id>` with a bilingual label from a new i18n key (e.g. `results.viewFullDetails`). It must sit in the non-interactive detail region, never inside the toggle button or the APPLY anchor, to avoid nested-interactive markup. Add the `results.viewFullDetails` key to BOTH message files with real zh copy. This is the DIR-03 recommendation-result deep-link into the Data-page detail view.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
    <automated>node -e "const e=require('./messages/en.json').results,z=require('./messages/zh-HK.json').results;if(!e.viewFullDetails||!z.viewFullDetails||e.viewFullDetails===z.viewFullDetails)throw new Error('viewFullDetails missing/placeholder');console.log('deep-link label ok')"</automated>
    <human-check>Run the recommender on the home page, expand a result card: a View full details link appears in the expanded region and routes to /[locale]/cards/&lt;id&gt; for that card, in both locales, with no nested-anchor console warning.</human-check>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` clean; results.viewFullDetails present in both locales with real zh copy — DIR-03, 09-CONTEXT D-06.
    - Observable: expanding a recommendation result exposes a link that deep-links to that card's detail page — DIR-03, 09-CONTEXT D-07.
  </acceptance_criteria>
  <done>Every recommendation result deep-links into /[locale]/cards/[id] from its expanded region, bilingual, no nested-interactive markup; committed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| visitor browser -> detail route | Untrusted GET with a path param `id`; read-only public card data, no auth, no writes. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09-02-01 | Tampering | /[locale]/cards/[id] id param | low | mitigate | getCardById does an exact-id lookup over the loaded set and returns undefined otherwise -> notFound(); no id is interpolated into a query, filesystem path, or HTML sink. |
| T-09-02-02 | Information disclosure | detail page | low | accept | Detail data is public product data (already served by the recommender); provenance labeling is honesty, not a confidentiality control. |

No package installs in this plan — package-legitimacy gate not applicable.
</threat_model>

<verification>
- `npx tsc --noEmit` clean across the detail route + recommender change.
- Read-only: grep confirms no recommendation-engine call in the detail route.
- notFound() fires for unknown ids (human-check).
- i18n: cardDetail namespace + results.viewFullDetails have en/zh-HK parity with real Chinese copy.
- `npm run build` compiles /[locale]/cards/[id] (health-check may need env; tsc is the automated gate, build runs in CI).
</verification>

<success_criteria>
- /[locale]/cards/[id] renders full card detail via getCardById, with notFound() on unknown ids — DIR-01, 09-CONTEXT D-02.
- Each detail page labels provenance + last-verified + HK-availability — DIR-02, 09-CONTEXT D-04.
- Recommendation results deep-link into the detail route — DIR-03, 09-CONTEXT D-07.
- Apply CTA reuses the applyCtaProps rel triplet; link-less cards show no apply anchor — AFF-01 reuse.
</success_criteria>

## Artifacts this phase produces (Plan 09-02)

- **New route:** `src/app/[locale]/cards/[id]/page.tsx` (server detail: getCardById + notFound + rules/fees + provenance labels + apply CTA).
- **Modified component:** `src/components/CardRecommendationList.tsx` (DIR-03 deep-link in the expanded region).
- **New i18n keys:** `cardDetail` namespace (backToDirectory, rewardRules, fees, annualFee, fxFee, redemptionFee, apply, hkAvailable, hkNotAvailable, lastVerified, provenanceNote) + `results.viewFullDetails`, in both message files.

<output>
Create `.planning/phases/09-data-page-card-directory/09-02-SUMMARY.md` when done.
</output>
