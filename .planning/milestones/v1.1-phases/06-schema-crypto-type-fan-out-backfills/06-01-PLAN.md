---
phase: 06-schema-crypto-type-fan-out-backfills
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/types/card.ts
  - src/types/index.ts
  - src/data/cards.json
autonomous: true
requirements: [CRY-01, CRY-02, CRY-03, TECH-02]

must_haves:
  truths:
    - "The CreditCard schema requires a cardType of 'credit' | 'crypto' | 'prepaid'"
    - "RewardUnit includes 'crypto', and a card can name its crypto asset via rewardPrograms.crypto"
    - "A reward tier can declare a staking gate via RewardCondition.minStaking { amount, asset }"
    - "A card carries an hkEligible flag in the schema"
    - "The dead RewardCap interface no longer exists in the type layer"
    - "All 11 cards in cards.json carry cardType 'credit'"
  artifacts:
    - path: "src/types/card.ts"
      provides: "CardType union, required cardType field, widened RewardUnit, RewardPrograms.crypto, RewardCondition.minStaking, CreditCard.hkEligible; RewardCap interface removed"
      contains: "cardType"
    - path: "src/types/index.ts"
      provides: "CardType re-export; RewardCap re-export removed"
      contains: "CardType"
    - path: "src/data/cards.json"
      provides: "All 11 cards carry cardType credit"
      contains: "\"cardType\": \"credit\""
  key_links:
    - from: "src/data/cards.json"
      to: "src/types/card.ts"
      via: "cardsData as CardDatabase must still typecheck once cardType is required"
      pattern: "cardsData as CardDatabase"
---

<objective>
Reshape CardGPT's hand-rolled TypeScript schema (there is NO ORM — `src/types/card.ts` IS the schema) so the card model can represent credit, crypto, and prepaid cards, crypto reward assets, staking-gated tiers, and HK-eligibility — and retire the dead `RewardCap` interface. This is the ROOT of Phase 6: every other plan compiles against these types.

This plan makes the four schema changes and immediately backfills `cardType: "credit"` onto all 11 cards in `cards.json`, so the `cardsData as CardDatabase` cast in `cardRepository.ts` stays green under a now-required `cardType` (all in one wave — no red intermediate state).

Purpose: Establish the schema all of Phase 6 (and Phase 7's valuation engine) depends on.
Output: Widened `card.ts` + `index.ts` types; 11-card local corpus carrying `cardType`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md
</context>

## Artifacts this phase produces

Phase 6 introduces these symbols across its 5 plans. This plan (06-01) creates the schema layer:

- `CardType` union `'credit' | 'crypto' | 'prepaid'` and the required `CreditCard.cardType` field (06-01)
- Widened `RewardUnit` adding `'crypto'` (06-01)
- Crypto named-asset field: `RewardPrograms.crypto?: RewardProgramInfo` (06-01)
- `RewardCondition.minStaking?: { amount: number; asset: string }` (06-01)
- `CreditCard.hkEligible?: boolean` (06-01)
- Removal of the dead `RewardCap` interface + `CreditCard.rewardCap` field + re-export (06-01 type layer; 06-04 data layer)
- Downstream (other plans): reward-unit fan-out fixes (06-02, 06-03), `cards.json`/Redis `cardType` backfills (06-04, 06-05), verification script `scripts/capture-ranking.mts` (06-04), backfill script `scripts/backfill-card-type.mjs` (06-05)

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Widen the card schema and remove the dead RewardCap interface</name>
  <files>src/types/card.ts</files>
  <read_first>
    - src/types/card.ts (the whole file — this IS the schema; note `RewardUnit` at line 8, `RewardCondition` at 33-57, `RewardProgramInfo`/`RewardPrograms` at 209-230, the `RewardCap` interface at 235-250, and `CreditCard` at 255-316 including `rewardCap?` at 278 and `rewardPrograms?` at 315)
    - src/types/index.ts (to understand what is re-exported before you change it in Task 2)
  </read_first>
  <behavior>
    - After the change, a CreditCard object literal without a `cardType` is a TypeScript error.
    - `cardType` accepts exactly 'credit', 'crypto', or 'prepaid'.
    - `RewardUnit` accepts 'crypto' in addition to 'cash' | 'miles' | 'points'.
    - `RewardPrograms` can hold a `crypto` program (a `RewardProgramInfo` naming e.g. USDC or CRO).
    - `RewardCondition` can hold `minStaking` with a numeric `amount` and a string `asset`.
    - `CreditCard` can hold an optional boolean `hkEligible`.
    - Referencing `RewardCap` anywhere is a TypeScript error (interface is gone).
  </behavior>
  <action>
    Add an exported `CardType` union with exactly the three members `'credit'`, `'crypto'`, `'prepaid'`. Add a REQUIRED `cardType: CardType` field to the `CreditCard` interface (not optional — CRY-01 SC1 requires a card missing a type to fail).

    Widen the `RewardUnit` union (currently `'cash' | 'miles' | 'points'`) to add `'crypto'` (per CRY-02).

    Extend the `RewardPrograms` interface with an optional `crypto?: RewardProgramInfo` member so a card can name its crypto reward asset (e.g. name "USD Coin", shortName "USDC") using the existing `rewardPrograms` pattern already used for miles/points (per CRY-02's "named asset").

    Extend the `RewardCondition` interface with an optional `minStaking?: { amount: number; asset: string }` member — the staking/holding gate for crypto tiers (per CRY-03). Add a doc comment noting it is valued at the base un-staked tier by default in Phase 7 and must NOT inherit the `minMonthlySpending` "assume met" no-op. This is a SCHEMA field only — do not add engine logic (that is Phase 7).

    Add an optional `hkEligible?: boolean` field to `CreditCard` (per CRY-03 / the Phase 7 gate). Schema field only — no gate logic here.

    Delete the entire `RewardCap` interface (currently at lines ~235-250) and delete the `rewardCap?: RewardCap` field from `CreditCard` (currently at line ~278). Nothing in `src/lib`, `src/components`, or `src/app` imports the `RewardCap` type (verified — only `card.ts` defines it and `index.ts` re-exports it), so removal is compile-safe. The corresponding data blocks in `cards.json` are removed in plan 06-04, not here.
  </action>
  <acceptance_criteria>
    <!-- planner-discipline-allow: RewardCap -->
    - `grep -c "export type CardType" src/types/card.ts` returns 1 and the union lists credit, crypto, prepaid.
    - `grep -c "cardType: CardType" src/types/card.ts` returns 1 (field is required, no `?`).
    - `grep -E "type RewardUnit" src/types/card.ts` shows the union now includes crypto.
    - `grep -c "crypto?: RewardProgramInfo" src/types/card.ts` returns 1.
    - `grep -c "minStaking" src/types/card.ts` returns at least 1 and the shape is `{ amount: number; asset: string }`.
    - `grep -c "hkEligible" src/types/card.ts` returns at least 1.
    - `grep -vE "^\s*(//|\*)" src/types/card.ts | grep -c "interface RewardCap"` returns 0 (interface removed, ignoring any comment lines).
    - Typecheck deferred to the plan-level gate below (runs after Task 3 so the JSON cast is valid).
  </acceptance_criteria>
  <verify>
    <automated>grep -c "cardType: CardType" src/types/card.ts</automated>
  </verify>
  <done>The schema carries cardType (required), crypto reward unit + asset, minStaking, and hkEligible; the RewardCap interface and field are gone.</done>
</task>

<task type="auto">
  <name>Task 2: Update the central type re-exports</name>
  <files>src/types/index.ts</files>
  <read_first>
    - src/types/index.ts (the `export type { ... } from './card'` block — note `RewardCap` is currently re-exported at line 14)
    - src/types/card.ts (to confirm the new `CardType` export name from Task 1)
  </read_first>
  <action>
    In the `export type { ... } from './card'` block, remove `RewardCap` from the list (the interface no longer exists). Add `CardType` to the same re-export list so consumers can import it from `@/types`. Leave all other re-exports unchanged.
  </action>
  <acceptance_criteria>
    <!-- planner-discipline-allow: RewardCap -->
    - `grep -c "RewardCap" src/types/index.ts` returns 0.
    - `grep -c "CardType" src/types/index.ts` returns 1.
  </acceptance_criteria>
  <verify>
    <automated>grep -c "CardType" src/types/index.ts</automated>
  </verify>
  <done>`@/types` re-exports `CardType` and no longer re-exports `RewardCap`.</done>
</task>

<task type="auto">
  <name>Task 3: Backfill cardType credit onto all 11 cards in cards.json</name>
  <files>src/data/cards.json</files>
  <read_first>
    - src/data/cards.json (the 11 top-level card objects — their ids are: citi-cash-back, sc-smart, sc-simply-cash, sc-cathay-standard, sc-cathay-priority-banking, sc-cathay-priority-private, hsbc-everymile, hsbc-red, dbs-black-world, hang-seng-enjoy, sim-card)
    - src/lib/data/cardRepository.ts (lines 40-55 — the `cardsData as CardDatabase` cast that must stay valid once cardType is required)
  </read_first>
  <action>
    Add `"cardType": "credit"` to each of the 11 top-level card objects (all existing HK cards are credit cards, per CRY-01: the 11 existing cards read as `credit`). Place it near the other identity fields (after `issuer` or near `isActive` — placement is cosmetic; presence is what matters). Do NOT touch the `rewardCap` blocks on citi-cash-back / sc-smart / sc-simply-cash in this plan — those are removed in 06-04. Do NOT change any reward rule, fee, or cap value. Keep the JSON valid (trailing-comma-free).

    This backfill lands in the SAME wave as the required-cardType type change so the `cardsData as CardDatabase` cast never sees a red intermediate state.
  </action>
  <acceptance_criteria>
    - `grep -c "\"cardType\": \"credit\"" src/data/cards.json` returns exactly 11.
    - `node -e "JSON.parse(require('fs').readFileSync('src/data/cards.json','utf8'))"` exits 0 (valid JSON).
    - `node -e "const d=require('./src/data/cards.json'); const bad=d.cards.filter(c=>c.cardType!=='credit'); if(bad.length){console.error(bad.map(c=>c.id));process.exit(1)}"` exits 0 (every card is credit).
  </acceptance_criteria>
  <verify>
    <automated>grep -c "\"cardType\": \"credit\"" src/data/cards.json</automated>
  </verify>
  <done>All 11 cards carry cardType credit; JSON parses; no rewardCap or reward-value change.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| cards.json → typed runtime (`cardsData as CardDatabase`) | Static data crosses into the typed engine via a cast; a required field the data lacks is a latent type/validation hole |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01-01 | Tampering | Required `cardType` vs. 11-card corpus | mitigate | Backfill all 11 cards in the same wave (Task 3) so the cast stays valid; grep asserts exactly 11 credit values |
| T-06-01-02 | Denial of Service | `RewardUnit` widening reaching closed-set consumers | accept (here) | This plan only widens the type; the runtime crash surface (groupByRewardUnit) is mitigated in 06-02 before any crypto data exists |
| T-06-01-SC | Tampering | npm/pip/cargo installs | accept | No new package installs in this plan |
</threat_model>

<verification>
- After all three tasks: run `npm install` (fresh clone has no node_modules), then `npx tsc --noEmit` — must pass with zero errors. This is the schema-compiles gate; it proves required `cardType` + the `cardsData as CardDatabase` cast are consistent.
- `npm run build` succeeds (health-check + next build).
- `grep -c "interface RewardCap" src/types/card.ts` returns 0; `grep -c "RewardCap" src/types/index.ts` returns 0.
- Do NOT run `npm test` / vitest / jest — no test runner exists until Phase 7.
</verification>

<success_criteria>
- CreditCard requires `cardType: 'credit' | 'crypto' | 'prepaid'`.
- RewardUnit includes crypto; RewardPrograms can name a crypto asset; RewardCondition has minStaking; CreditCard has hkEligible.
- RewardCap interface + field + re-export are gone.
- All 11 cards.json cards carry cardType credit; `npx tsc --noEmit` and `npm run build` are green.
</success_criteria>

<output>
Create `.planning/phases/06-schema-crypto-type-fan-out-backfills/06-01-SUMMARY.md` when done.
</output>
