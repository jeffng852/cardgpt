---
phase: 07-crypto-hkd-valuation-engine-hkeligible-gate
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - vitest.config.ts
  - package.json
  - package-lock.json
  - src/lib/engine/__tests__/fiat-regression.test.ts
  - src/lib/engine/__tests__/__snapshots__/fiat-regression.test.ts.snap
  - src/lib/engine/__tests__/engine.test.ts
  - src/lib/parser/__tests__/transactionParser.test.ts
autonomous: true
requirements: [TECH-01]
must_haves:
  truths:
    - "`npm test` runs vitest and the suite exits green"
    - "The existing 11-card fiat ranking is captured in a committed snapshot generated on the CURRENT engine, before any valuation/segmentation change"
    - "The 2 pre-existing dormant test files (engine, parser) either pass or are explicitly quarantined (.skip + note) so the suite is green"
    - "The `@`→`./src` alias resolves for runtime value imports inside tests"
  artifacts:
    - path: "vitest.config.ts"
      provides: "vitest node-env config with globals + @ alias"
      contains: "resolve"
    - path: "package.json"
      provides: "test + test:watch scripts (vitest run / vitest)"
      contains: "vitest run"
    - path: "src/lib/engine/__tests__/fiat-regression.test.ts"
      provides: "byte-identical 11-card fiat ranking snapshot test (TECH-01)"
      min_lines: 30
    - path: "src/lib/engine/__tests__/__snapshots__/fiat-regression.test.ts.snap"
      provides: "committed baseline snapshot of the current fiat ranking"
  key_links:
    - from: "vitest.config.ts"
      to: "src/"
      via: "resolve.alias maps @ to ./src so tests can import runtime values via @/"
      pattern: "alias"
    - from: "src/lib/engine/__tests__/fiat-regression.test.ts"
      to: "src/lib/engine/recommendCards.ts"
      via: "imports recommendCards and runs it over @/data/cards.json for the fixed scenario set"
      pattern: "recommendCards"
---

<objective>
Install the repo's first real test runner (vitest, dev-only) and stand up the regression safety net BEFORE any engine change: configure vitest, triage the two dormant test files that auto-activate under `globals: true`, and generate + commit the byte-identical fiat-ranking baseline snapshot on the CURRENT engine.

Purpose: TECH-01 requires a runner that covers the new valuation logic AND proves the existing 11-card fiat rankings are unchanged. The baseline snapshot MUST be captured on the pre-change engine so any later drift in the fiat sort turns the suite red. This plan is Wave 0 — every subsequent Phase 7 task depends on the runner existing and on this baseline being committed first.

Output: `vitest.config.ts`, `test`/`test:watch` npm scripts, a green suite (dormant tests fixed or quarantined), and a committed `fiat-regression.test.ts` + baseline `.snap`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-crypto-hkd-valuation-engine-hkeligible-gate/07-CONTEXT.md
@.planning/phases/07-crypto-hkd-valuation-engine-hkeligible-gate/07-RESEARCH.md
@.planning/phases/07-crypto-hkd-valuation-engine-hkeligible-gate/07-VALIDATION.md

# The precedent this snapshot ports, and the engine + data it captures
@scripts/capture-ranking.mts
@src/lib/engine/recommendCards.ts
@src/lib/engine/__tests__/engine.test.ts
@src/lib/parser/__tests__/transactionParser.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install vitest and add config + npm scripts</name>
  <read_first>
    - .planning/phases/07-crypto-hkd-valuation-engine-hkeligible-gate/07-RESEARCH.md (Standard Stack; "Code Examples" → the concrete vitest.config.ts; Package Legitimacy Audit — vitest Approved dev-only)
    - .planning/phases/07-crypto-hkd-valuation-engine-hkeligible-gate/07-VALIDATION.md (Test Infrastructure; Wave 0 Requirements)
    - CLAUDE.md (Known Pitfalls: "There is no test runner"; run `npm install` before `npx tsc`; public-repo rule)
    - package.json (existing scripts block; devDependencies)
    - tsconfig.json (resolveJsonModule true; paths `@/*`; exclude `**/__tests__/**`)
  </read_first>
  <files>vitest.config.ts, package.json, package-lock.json</files>
  <action>Run `npm install` first (a fresh clone has no node_modules), then `npm install -D vitest@^4.1.10` — dev-only, the only new dependency this phase (per RESEARCH Package Legitimacy Audit: vitest Approved). Create `vitest.config.ts` at repo root using `defineConfig` from `vitest/config` with `test.environment: 'node'`, `test.globals: true` (the two pre-existing test files use bare describe/it/expect), `test.include: ['src/**/*.{test,spec}.{ts,tsx}']`, and `resolve.alias` mapping `@` to `path.resolve(__dirname, './src')` (RESEARCH Pitfall 3 — needed so new tests can import runtime values via `@/`; the existing engine test only imports `@/types/*` as erased types today). Do NOT add jsdom/happy-dom, `@vitejs/plugin-react`, or coverage — node env + single alias only (single-artifact simplicity). Add two scripts to package.json: `"test": "vitest run"` and `"test:watch": "vitest"` — never a bare-`vitest` (watch) as the `test` script.</action>
  <verify>
    <automated>npx vitest list</automated>
  </verify>
  <acceptance_criteria>
    - `vitest` appears under devDependencies in package.json at `^4.1.10`
    - `vitest.config.ts` exists at repo root with `environment: 'node'`, `globals: true`, and a `resolve.alias` for `@`
    - package.json has `"test": "vitest run"` and `"test:watch": "vitest"`
    - `npx vitest list` collects the pre-existing test files with no `Cannot find module '@/...'` resolution error (assertion pass/fail is Task 2's concern)
  </acceptance_criteria>
  <done>vitest is installed dev-only, `vitest.config.ts` resolves the `@` alias, and `npm test` is a real command that invokes `vitest run`.</done>
</task>

<task type="auto">
  <name>Task 2: Triage the two dormant test files to green</name>
  <read_first>
    - src/lib/engine/__tests__/engine.test.ts (mock cards omit the now-required `cardType`; mock transactions set `merchantType: 'restaurant'` while the rules use `categories` — likely assertion drift, not a type crash, since esbuild strips types and the engine never reads `cardType` for math)
    - src/lib/parser/__tests__/transactionParser.test.ts
    - .planning/phases/07-crypto-hkd-valuation-engine-hkeligible-gate/07-RESEARCH.md (Pitfall 1 — these files auto-activate under `globals: true` and have never run)
    - src/lib/engine/calculateReward.ts (matchesRule — the categories-vs-merchantType matching path that governs whether the dining-bonus assertions hold)
  </read_first>
  <files>src/lib/engine/__tests__/engine.test.ts, src/lib/parser/__tests__/transactionParser.test.ts</files>
  <action>Run the full suite once to observe the pre-existing files' first-ever execution. For each failing assertion, decide per RESEARCH Pitfall 1: if it is a small, correct fix (e.g. the dining-bonus test sets `merchantType: 'restaurant'` but the rule matches on `category` — set `category: 'dining'` on the mock transaction so the bonus rule genuinely matches), fix it; if an assertion encodes a number that was never validated and cannot be trivially reconciled, quarantine that single test with `.skip` and a one-line note stating it is a pre-vitest dormant test pending reconciliation (no security detail — public repo). Do NOT weaken or delete the engine/parser production code to make tests pass; only adjust test fixtures/expectations or `.skip`. Goal: the suite ends green (0 unexpected red) so the runner is trustworthy for the rest of the phase.</action>
  <verify>
    <automated>npm test</automated>
  </verify>
  <acceptance_criteria>
    - `npm test` (`vitest run`) exits 0 with zero failing tests
    - Any quarantined test uses `.skip` with an inline note; no production source file under `src/lib/engine/` or `src/lib/parser/` (non-test) was modified
    - The engine and parser test files still execute (were not deleted)
  </acceptance_criteria>
  <done>The two dormant test files pass or are explicitly quarantined; `npm test` is green, establishing a trustworthy baseline suite.</done>
</task>

<task type="auto">
  <name>Task 3: Generate + commit the byte-identical fiat-ranking baseline snapshot</name>
  <read_first>
    - scripts/capture-ranking.mts (the deterministic serializer + the 5 fixed scenarios to port verbatim; note it filters to 3 target ids — the snapshot must instead capture the FULL 11-card corpus so any perturbation anywhere fails)
    - .planning/phases/07-crypto-hkd-valuation-engine-hkeligible-gate/07-RESEARCH.md ("Byte-identical fiat regression test" code example; "generate the snapshot on the CURRENT engine first, commit it, then implement")
    - src/lib/engine/recommendCards.ts (the current 3-arg signature the test calls; the test passes NO rateTable so only the fiat path runs)
    - src/data/cards.json (the 11-card corpus, all `cardType: 'credit'`)
  </read_first>
  <files>src/lib/engine/__tests__/fiat-regression.test.ts, src/lib/engine/__tests__/__snapshots__/fiat-regression.test.ts.snap</files>
  <action>Create `fiat-regression.test.ts` that imports `recommendCards` from `../recommendCards`, `cardsData` from `@/data/cards.json` (resolveJsonModule is on), and the `CreditCard`/`Transaction` types. Port the 5 scenarios from `scripts/capture-ranking.mts` verbatim (local-hkd-dining-500, local-hkd-general-8000-tier1, sc-smart-designated-mcdonalds-tier2-capped, foreign-usd-2000, local-hkd-travel-hotel-3000, with their `monthlySpending` values). For each scenario call `recommendCards(corpus, tx, monthlySpending !== undefined ? { monthlySpending } : undefined)` — pass NO rateTable (fiat-only path) — and serialize EVERY entry of `result.recommendations` (the full 11-card ranking, not just the 3 target ids) using the exact stable field ordering from capture-ranking.mts's `serializeEntry` (scenario, id, rank, isRecommended, rewardAmount, rewardUnit, effectiveRate, netValue). Assert `expect(lines.join('\n')).toMatchSnapshot()`. Run it once so vitest writes the `.snap`, then COMMIT both the test and the generated snapshot on the current pre-change engine per DEC-VAL-B (the byte-identical guard is a safety net; partition-before-sort in Plan 03 is the structural guarantee).</action>
  <verify>
    <automated>npx vitest run src/lib/engine/__tests__/fiat-regression.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `src/lib/engine/__tests__/fiat-regression.test.ts` exists and snapshots the FULL ranking for all 5 scenarios (not filtered to 3 ids)
    - The test calls `recommendCards` with no rateTable argument (fiat path only)
    - `npx vitest run src/lib/engine/__tests__/fiat-regression.test.ts` passes and a committed `__snapshots__/fiat-regression.test.ts.snap` exists
    - The snapshot was generated against the current engine (no valuation/segmentation code has been added yet)
  </acceptance_criteria>
  <done>A committed baseline snapshot captures the current 11-card fiat ranking; the TECH-01 regression guard now exists and must stay green through all later engine changes.</done>
</task>

</tasks>

<artifacts_produced>
## Artifacts this plan produces
- `vitest.config.ts` — node-env vitest config with `globals: true` and `@`→`./src` alias.
- `package.json` scripts — `test` (`vitest run`) and `test:watch` (`vitest`); `vitest@^4.1.10` devDependency.
- `src/lib/engine/__tests__/fiat-regression.test.ts` — full-corpus byte-identical fiat ranking snapshot test (TECH-01 guard).
- `src/lib/engine/__tests__/__snapshots__/fiat-regression.test.ts.snap` — committed baseline of the current fiat ranking.
- Triaged `engine.test.ts` / `transactionParser.test.ts` — green or explicitly quarantined.
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → repo (dev-dep install) | A new devDependency (vitest) is pulled into the toolchain; a typo/malicious package would execute at test/build time |
| current engine → committed snapshot | The baseline encodes today's fiat ranking as the source of truth for "unchanged"; a wrong baseline would mask real regressions |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-SC | Tampering | `npm install -D vitest` | mitigate | Install ONLY `vitest` (verified legit in RESEARCH Package Legitimacy Audit: 5+ yrs, ~10M/wk, vitest-dev/vitest); pin `^4.1.10`; dev-only, no prod-runtime dep added; no `[ASSUMED]`/`[SUS]` packages so no blocking-human checkpoint required |
| T-07-REG | Tampering | fiat-regression snapshot | mitigate | Snapshot the FULL 11-card corpus across all 5 scenarios on the CURRENT engine and commit BEFORE any change, so later drift anywhere in the fiat sort fails the suite |
| T-07-REG-b | Repudiation | dormant test triage | mitigate | Suite must exit green (fix or explicit `.skip` note) so a red suite later is unambiguously a real regression, not pre-existing dormant noise |
</threat_model>

<verification>
- `npm test` exits 0 (all non-quarantined tests green).
- `npx vitest run src/lib/engine/__tests__/fiat-regression.test.ts` passes against a committed snapshot.
- `vitest.config.ts` present; `@` alias resolves (a runtime `@/data/cards.json` import in the regression test loads without error).
- No production (non-test) engine/parser source was modified.
</verification>

<success_criteria>
- vitest ^4.1.10 installed dev-only; `npm test` runs `vitest run`.
- Baseline fiat-ranking snapshot committed on the pre-change engine (TECH-01 guard live).
- Dormant test files green or explicitly quarantined; suite is trustworthy for Waves 1–2.
</success_criteria>

<output>
Create `.planning/phases/07-crypto-hkd-valuation-engine-hkeligible-gate/07-01-SUMMARY.md` when done.
</output>
