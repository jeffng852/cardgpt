---
phase: 07-crypto-hkd-valuation-engine-hkeligible-gate
verified: 2026-07-25T00:05:00Z
status: passed
score: 5/5 must-haves verified
behavior_unverified: 0
overrides_applied: 0
re_verification:
  previous_status: none
  previous_score: n/a
  note: "Retroactive canonical VERIFICATION.md — phase executed (3 plans/summaries) without one; generated by goal-backward analysis."
---

# Phase 7: Crypto→HKD Valuation Engine & `hkEligible` Gate — Verification Report

**Phase Goal:** Crypto cards rank by real HKD-equivalent net value beside fiat cards, unobtainable cards never surface as recommendations, and the new engine math is test-covered — without changing any existing fiat ranking. (Requirements: CRY-04, CRY-05, TECH-01; decisions DEC-VAL-A/B/C, DEC-DATA-002.)
**Verified:** 2026-07-25
**Status:** passed
**Re-verification:** No — initial (retroactive) verification.

## Goal Achievement

### Observable Truths (ROADMAP Success Criteria)

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | Crypto reward valued in HKD via an **injectable** static rate table (no fetch in engine); cross-unit decision (segment vs normalize) made, recorded, applied (CRY-04, dec b) | ✓ VERIFIED | `valuateCrypto(calc, card, rateTable?)` takes an injected `HkdRateTable`; engine has no fetch. Decision recorded as unit-segmented (`cryptoSegment`, DEC-VAL-B) — crypto is ranked among itself, never compared to fiat net value. `recommendCards.ts:172-207` builds a separate `cryptoSegment`; `segmentation.test.ts` green. |
| 2 | Missing/zero/stale rate **fails safe and visibly** — degrades to value-unavailable / as-of state, never silently #1 (CRY-04, dec a) | ✓ VERIFIED | `valuateCrypto.ts:57` `!rate \|\| !(rate.hkdPerUnit > 0)` ⇒ `hkdEquivalent: null` (absent/zero/negative/NaN); stale (>24h) keeps last-known value + `rateStale: true` + `rateAsOf` (`:62-69`). Null entries appended **unranked** (`recommendCards.ts:189-206`), so a value-unavailable card can never rank #1. 10 tests in `valuateCrypto.test.ts` pass. |
| 3 | Staking-gated crypto rewards valued at **base un-staked tier** by default; no inherited "assume met" no-op (dec c) | ✓ VERIFIED | `calculateReward.ts:172-174` — `if (conditions.minStaking) return false` in `matchesRule`, placed after (and explicitly NOT sharing) the `minMonthlySpending` "assume met" note. `minStaking.test.ts` (3 tests) confirms gated tier excluded, un-gated bonus still applies, base parity. |
| 4 | `hkEligible` gates inclusion **fail-closed inside `recommendCards()`** (`undefined` ⇒ eligible; global cards set `false`); ineligible never recommended, directory still shows it (CRY-05) | ✓ VERIFIED | `recommendCards.ts:70` `if (card.hkEligible === false) return false` in the base eligibility filter, BEFORE partition — excludes from BOTH `recommendations` AND `cryptoSegment`. Keyed on `=== false` (not falsy), so `undefined`/`true` stay eligible. `hkEligible.test.ts` (5 tests incl. crypto-surface leak test) green; 11-card corpus still returns 11. Gate governs recommendations only (directory untouched). |
| 5 | **Regression, test-guarded:** existing 11-card ranking byte-identical, covered by a vitest suite over the new valuation logic (TECH-01) | ✓ VERIFIED | `partition-before-sort` (`recommendCards.ts:82-83`) splits `cardType === 'credit'` into the verbatim fiat pipeline before the comparator — structural guarantee. Committed baseline snapshot `fiat-regression.test.ts.snap` (55 entries) is byte-identical: `git status --porcelain` empty, last touched at Wave 0 baseline `e718dbe` (never `-u` regenerated). Full suite: **7 files, 73 passed / 7 skipped / 0 failed**. |

**Score:** 5/5 truths verified (0 present, behavior-unverified). All 14 plan-level `must_haves.truths` (4+4+6) also verified.

### Focus-Area Deep Checks (per task brief)

| Focus | Finding |
|-------|---------|
| TECH-01 byte-identical (snapshot unchanged AND structurally guaranteed) | Both confirmed. Structural: fiat pipeline runs on `fiatCards` (`cardType === 'credit'`) only; crypto valued on a **separate fresh array** (`recommendCards.ts:174`), never aliasing the `.sort()` target. Empirical: snapshot git-clean, last commit `e718dbe`. |
| hkEligible `=== false` semantics + excluded from BOTH lists | Confirmed. Gate is `=== false` (absent field stays eligible), located in `baseEligibleCards` **before** the fiat/crypto partition, so an ineligible crypto card cannot leak into `cryptoSegment` — explicitly locked by `hkEligible.test.ts:109-115`. |
| valuateCrypto null on bad rates (never fabricated/thrown) | Confirmed. Single guard `!(rate.hkdPerUnit > 0)` rejects 0/neg/NaN/non-number; absent asset/table ⇒ null; unparseable `asOf` ⇒ treated as stale (not thrown) via `Number.isFinite(ageMs)`. No `throw` anywhere in the helper. |
| minStaking not-met-by-default | Confirmed. `matchesRule` returns false on any `conditions.minStaking`; no fiat rule carries it (fiat path unperturbed — snapshot proves it). |
| Additive contract (3-arg callers, unchanged shape) | Confirmed. `rateTable` is an optional 4th param; `cryptoSegment` conditionally spread only when a rateTable is supplied AND ≥1 non-fiat card exists (`recommendCards.ts:211`). All 3 production call sites (`HomeClient.tsx:46`, `integration-example.ts`, `engine/example.ts`) remain 3-arg; `HomeClient` reads only `.recommendations`. `tsc --noEmit` exit 0; build compiles 20/20 pages. `segmentation.test.ts` confirms no-rateTable ⇒ `cryptoSegment` undefined + identical recommendations. |

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `src/lib/engine/valuateCrypto.ts` | valuateCrypto() + staleness + fail-safe | ✓ VERIFIED | 70 lines; exports `valuateCrypto` + `CryptoValuation`; wired into `recommendCards` and re-exported from `index.ts`. |
| `src/lib/engine/recommendCards.ts` | hkEligible gate + partition + cryptoSegment + rateTable param | ✓ VERIFIED | Gate, partition-before-sort, separate crypto array, conditional `cryptoSegment` all present and wired. |
| `src/lib/engine/calculateReward.ts` | minStaking base-tier guard | ✓ VERIFIED | `matchesRule` returns false on `minStaking`. |
| `src/types/card.ts` | HkdRateTable/HkdRate/AssetSymbol + hkEligible consumed | ✓ VERIFIED | Types present, documented (locked shortName-ticker key convention). |
| `src/types/recommendation.ts` | CryptoRecommendation + optional cryptoSegment | ✓ VERIFIED | Additive optional field; existing `.recommendations` consumers untouched. |
| `src/lib/engine/index.ts` | barrel re-exports | ✓ VERIFIED | Exports valuation helper + all new types. |
| `vitest.config.ts` / `package.json` | runner + `npm test` script | ✓ VERIFIED | vitest ^4.1.10 dev-only; `test`/`test:watch` scripts; `@`→`./src` alias resolves. |
| `__tests__/fiat-regression.test.ts(.snap)` | byte-identical baseline | ✓ VERIFIED | Full 11-card × 5-scenario snapshot; git-clean at baseline commit. |
| `__tests__/{valuateCrypto,minStaking,hkEligible,segmentation}.test.ts` | new-logic coverage | ✓ VERIFIED | 26 new tests, all green. |

### Key Link Verification

| From | To | Via | Status |
|------|----|----|--------|
| `recommendCards.ts` | `valuateCrypto.ts` | maps non-fiat cards through `valuateCrypto` with injected rateTable | ✓ WIRED (`:178`) |
| `recommendCards.ts` | `card.ts` | eligibility excludes `hkEligible === false`; partition on `cardType === 'credit'` | ✓ WIRED (`:70,82-83`) |
| `calculateReward.ts` | `card.ts` | `matchesRule` false on `conditions.minStaking` | ✓ WIRED (`:172`) |
| `valuateCrypto.ts` | `card.ts` | consumes `HkdRateTable` keyed by resolved shortName ticker | ✓ WIRED (`:51-52`) |
| `fiat-regression.test.ts` | `recommendCards.ts` | runs recommendCards over cards.json (no rate table) | ✓ WIRED |

### Data-Flow Trace (Level 4)

Not applicable in the rendering sense — Phase 7 is pure engine logic with no UI/dynamic-render surface. The one data-flow note is a **known forward dependency, not a gap:** `cryptoSegment` is inert until a rate table is injected, and the cron-refreshed rate table is explicitly Phase 8 scope (DEC-DATA-002, ROADMAP Phase 8). Correctly documented in 07-UAT.md.

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| Full suite green | `npx vitest run` | 7 files, 73 passed / 7 skipped / 0 failed | ✓ PASS |
| Typecheck clean (additive param/field) | `npx tsc --noEmit` | exit 0 | ✓ PASS |
| Production build | `npm run build` | ✓ Compiled successfully; 20/20 static pages | ✓ PASS |
| Fiat snapshot byte-identical | `git status --porcelain __snapshots__/` | empty; last commit `e718dbe` | ✓ PASS |
| minStaking transition (base-tier default) | `minStaking.test.ts` (named) | 3 tests pass | ✓ PASS |
| hkEligible fail-closed both surfaces | `hkEligible.test.ts` (named) | 5 tests pass | ✓ PASS |

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|-------------|-------------|--------|----------|
| CRY-04 | 07-02, 07-03 | Crypto rewards valued in HKD-equivalent, rank by net value beside fiat; fail-safe on bad/stale rates | ✓ SATISFIED | `valuateCrypto` + `cryptoSegment` + null/stale handling; 20 tests |
| CRY-05 | 07-03 | `hkEligible` fail-closed inside `recommendCards()`; undefined ⇒ eligible | ✓ SATISFIED | `:70` gate; `hkEligible.test.ts` |
| TECH-01 | 07-01, 07-02, 07-03 | vitest runner covering new valuation logic; 11-card ranking byte-identical | ✓ SATISFIED | Runner installed; 26 new tests; snapshot unchanged |

### Anti-Patterns Found

None. No `TBD`/`FIXME`/`XXX`/`HACK`/`PLACEHOLDER`/"not implemented" markers in any phase-modified production file. Null returns in `valuateCrypto` are the intentional fail-safe (value-unavailable), not stubs. The 7 skipped tests are Plan-01's pre-existing quarantined parser assertions (documented deferred follow-up), not new debt.

### Human Verification Required

None. Every deliverable is engine logic observable via the test suite, typecheck, and build — all auto-verified green. No visual, real-time, or external-service surface exists in this phase.

### Gaps Summary

No gaps. All 5 ROADMAP success criteria and all 14 plan-level truths are verified in the codebase with both structural evidence and passing behavioral tests. The byte-identical fiat regression is guaranteed structurally (partition-before-sort) and confirmed empirically (unchanged committed snapshot). Two items correctly scoped as **forward dependencies, not Phase 7 gaps:** the crypto rate table (Phase 8 / DEC-DATA-002) and `cryptoSegment` UI rendering (downstream UI phase).

---

_Verified: 2026-07-25_
_Verifier: Claude (gsd-verifier)_
