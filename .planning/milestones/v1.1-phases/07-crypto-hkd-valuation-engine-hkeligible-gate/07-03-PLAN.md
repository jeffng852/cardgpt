---
phase: 07-crypto-hkd-valuation-engine-hkeligible-gate
plan: 03
type: execute
wave: 2
depends_on: ["07-01", "07-02"]
files_modified:
  - src/lib/engine/recommendCards.ts
  - src/lib/engine/__tests__/hkEligible.test.ts
  - src/lib/engine/__tests__/segmentation.test.ts
autonomous: true
requirements: [CRY-04, CRY-05, TECH-01]
must_haves:
  truths:
    - "A card with hkEligible === false is excluded from recommendations (never returned by recommendCards), while remaining present in the directory data (CRY-05, fail-closed)"
    - "A card with hkEligible === undefined is eligible — all 11 legacy cards are still returned (CRY-05)"
    - "Crypto/non-fiat cards are returned only in cryptoSegment, never merged into recommendations (DEC-VAL-B, unit-segmented)"
    - "The fiat recommendations are byte-identical to the Plan 01 baseline because only cardType === 'credit' cards enter the existing sort (TECH-01, structural via partition-before-sort)"
    - "cryptoSegment is ranked by hkdEquivalent descending, with value-unavailable (null) entries appended unranked (DEC-VAL-A)"
    - "Passing no rateTable leaves cryptoSegment absent and recommendations unchanged (backward-compatible)"
  artifacts:
    - path: "src/lib/engine/recommendCards.ts"
      provides: "hkEligible fail-closed gate + partition-before-sort + cryptoSegment assembly + optional rateTable 4th param"
      contains: "cryptoSegment"
    - path: "src/lib/engine/__tests__/hkEligible.test.ts"
      provides: "gate tests: false⇒excluded, undefined⇒included"
      min_lines: 25
    - path: "src/lib/engine/__tests__/segmentation.test.ts"
      provides: "partition + cryptoSegment ranking + fiat-unchanged tests"
      min_lines: 30
  key_links:
    - from: "src/lib/engine/recommendCards.ts"
      to: "src/lib/engine/valuateCrypto.ts"
      via: "builds cryptoSegment by mapping non-fiat cards through valuateCrypto with the injected rateTable"
      pattern: "valuateCrypto"
    - from: "src/lib/engine/recommendCards.ts"
      to: "src/types/card.ts"
      via: "eligibility filter excludes hkEligible === false; partition splits on cardType === 'credit'"
      pattern: "hkEligible"
---

<objective>
Wire the valuation core into the recommender: add the fail-closed `hkEligible` gate inside `recommendCards()` (CRY-05), partition eligible cards into fiat (`cardType === 'credit'`) and non-fiat sets BEFORE the existing sort, run the current fiat pipeline verbatim, and assemble the non-fiat cards into the new optional `cryptoSegment` via `valuateCrypto` with an additive optional `rateTable` 4th param (CRY-04 / DEC-VAL-B).

Purpose: This is where the structural fiat-immutability guarantee lands — because crypto never enters the fiat comparator, the 11-card ranking is provably unperturbed (the Plan 01 snapshot is the safety net). The gate ensures an HK resident is never recommended an unobtainable card, while the directory still lists it. DEC-VAL-A's softer "value unavailable" policy is safe ONLY because crypto is segmented out here.

Output: an updated `recommendCards.ts` (gate + partition + segment + rateTable param) plus `hkEligible.test.ts` and `segmentation.test.ts`, with the Plan 01 fiat-regression snapshot still byte-identical.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-crypto-hkd-valuation-engine-hkeligible-gate/07-CONTEXT.md
@.planning/phases/07-crypto-hkd-valuation-engine-hkeligible-gate/07-RESEARCH.md
@.planning/phases/07-crypto-hkd-valuation-engine-hkeligible-gate/07-VALIDATION.md

# The file being extended + the sole return-shape consumer (must stay working)
@src/lib/engine/recommendCards.ts
@src/components/HomeClient.tsx

# Depends on Plan 02 (valuateCrypto, HkdRateTable, CryptoRecommendation, cryptoSegment field)
# and Plan 01 (vitest runner + committed fiat baseline snapshot)
@.planning/phases/07-crypto-hkd-valuation-engine-hkeligible-gate/07-02-SUMMARY.md
@.planning/phases/07-crypto-hkd-valuation-engine-hkeligible-gate/07-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: hkEligible fail-closed gate in recommendCards()</name>
  <read_first>
    - src/lib/engine/recommendCards.ts (the base eligibility filter, lines ~55-60: `isActive` + `excludedCardIds` — the exact place to add the gate)
    - src/types/card.ts (CreditCard.hkEligible optional boolean — schema field with its Phase-7 note that the gate is consumed here)
    - .planning/phases/07-crypto-hkd-valuation-engine-hkeligible-gate/07-CONTEXT.md (CRY-05 locked: fail-closed; `false`⇒excluded, `undefined`⇒eligible for the 11 legacy cards; directory still shows ineligible cards)
    - .planning/phases/07-crypto-hkd-valuation-engine-hkeligible-gate/07-RESEARCH.md (System Architecture Diagram — gate sits in the base eligibility filter, before partition)
  </read_first>
  <files>src/lib/engine/recommendCards.ts, src/lib/engine/__tests__/hkEligible.test.ts</files>
  <behavior>
    - `hkEligible === false` → card is filtered out of the eligibility set (never in recommendations or cryptoSegment).
    - `hkEligible === undefined` (or true) → card is eligible; all 11 legacy cards (undefined) still returned.
    - The gate is fail-closed only on an explicit `false`; it does not require the field to be present.
  </behavior>
  <action>In the base eligibility filter of `recommendCards()` (the same `.filter` that checks `card.isActive` and `preferences?.excludedCardIds`), add `if (card.hkEligible === false) return false;` per CRY-05 — fail-closed strictly on explicit `false` so the 11 legacy cards (field undefined) remain eligible. Place it inside the existing eligibility predicate so an ineligible card never reaches EITHER the fiat pipeline or the crypto segment. Do not gate on falsy (which would wrongly exclude undefined); gate on `=== false` only. This filter change must not alter the relative order of the surviving fiat cards (only removes explicit-false cards, of which the 11 corpus has none). Write `hkEligible.test.ts`: a card with `hkEligible: false` is absent from `result.recommendations`; a card with `hkEligible: undefined` and one with `hkEligible: true` are present; assert the 11-card corpus (all undefined) still returns 11 recommendations.</action>
  <verify>
    <automated>npx vitest run src/lib/engine/__tests__/hkEligible.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `recommendCards` excludes any card with `hkEligible === false` from `recommendations` (and cryptoSegment)
    - Cards with `hkEligible === undefined` or `true` are included; the 11-card corpus still yields 11 recommendations
    - The gate keys on `=== false` (not falsy), so undefined stays eligible
    - `npx vitest run src/lib/engine/__tests__/hkEligible.test.ts` passes
  </acceptance_criteria>
  <done>The recommender is fail-closed on `hkEligible === false`; unobtainable cards never surface as recommendations while the directory data is untouched (CRY-05).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Partition-before-sort + cryptoSegment assembly + rateTable param</name>
  <read_first>
    - src/lib/engine/recommendCards.ts (the full pipeline: preference filters, calculateReward map, sort/tie-break, rank/isRecommended, return object — all of which must run VERBATIM on the fiat set)
    - src/lib/engine/valuateCrypto.ts (from Plan 02: valuateCrypto signature + return shape; HkdRateTable, CryptoRecommendation)
    - .planning/phases/07-crypto-hkd-valuation-engine-hkeligible-gate/07-CONTEXT.md (DEC-VAL-B unit-segmented; DEC-VAL-A stale⇒ranked with flag, null⇒appended unranked; the coupling note — softer policy safe only because segmented)
    - .planning/phases/07-crypto-hkd-valuation-engine-hkeligible-gate/07-RESEARCH.md (Pattern 2 partition-before-sort; Pattern 1 additive 4th param; Pitfall 5 groupByRewardUnit crypto bucket now empty is correct; Pitfall 6 `.sort()` mutates in place — build cryptoSegment from a separate array, never alias the fiat array)
    - src/components/HomeClient.tsx (sole consumer: calls with 3 args, reads only `.recommendations` — the 4th param + cryptoSegment must be invisible to it)
    - src/lib/engine/__tests__/fiat-regression.test.ts (the Plan 01 snapshot this task must keep byte-identical)
  </read_first>
  <files>src/lib/engine/recommendCards.ts, src/lib/engine/__tests__/segmentation.test.ts</files>
  <behavior>
    - Fiat set = eligible cards with `cardType === 'credit'`; non-fiat set = everything else (`crypto`, `prepaid`).
    - The existing preference-filter → calculateReward → sort → rank pipeline runs unchanged on the fiat set → `recommendations`.
    - Non-fiat cards are mapped through `valuateCrypto` (with the injected rateTable) into `CryptoRecommendation[]`, ranked by `hkdEquivalent` desc; `hkdEquivalent === null` entries appended after ranked ones.
    - `cryptoSegment` is populated only when a `rateTable` is supplied AND at least one non-fiat card exists; otherwise the field is absent/undefined.
    - No rateTable → identical behavior to today: `recommendations` unchanged, `cryptoSegment` absent.
  </behavior>
  <action>Add an optional 4th param `rateTable?: HkdRateTable` to `recommendCards(cards, transaction, preferences?, rateTable?)` — additive and positional after `preferences`, so `HomeClient`/`example.ts`/`integration-example.ts` (all 3-arg) are unaffected (RESEARCH Pattern 1). After the base eligibility filter (which now includes the hkEligible gate from Task 1), PARTITION the eligible cards: `fiatCards = eligibleCards.filter(c => c.cardType === 'credit')` and `cryptoCards = eligibleCards.filter(c => c.cardType !== 'credit')`. DECIDE + ENCODE the segment boundary (RESEARCH Open Question 1): use `cardType === 'credit'` for the fiat set (guarantees the 11 legacy `credit` cards stay fiat and byte-identical), so both `crypto` and `prepaid` fall into the non-fiat segment; document this and note prepaid may get its own section in Phase 9. Run the EXISTING pipeline (preference filters, calculateReward map, minRewardRate filter, sort + tie-break, rank/isRecommended) VERBATIM on `fiatCards` only → `recommendations` — do not change the comparator or the field computations. Build `cryptoSegment` on a SEPARATE fresh array (RESEARCH Pitfall 6 — never alias the fiat array): map each `cryptoCard` through `calculateReward` then `valuateCrypto(calc, card, rateTable)`, producing `CryptoRecommendation` entries; sort by `hkdEquivalent` descending, appending `hkdEquivalent === null` entries unranked at the end (DEC-VAL-A — cannot rank on a missing number); assign `rank`/`isRecommended` within the segment only over the ranked (non-null) entries. Only attach `cryptoSegment` to the result when `rateTable` is provided and `cryptoCards.length > 0`; otherwise leave it undefined (backward-compatible). Leave `groupByRewardUnit` untouched (RESEARCH Pitfall 5 — its now-always-empty crypto bucket is correct under DEC-VAL-B). Never let a crypto card enter the fiat comparator (RESEARCH Anti-Patterns). Write `segmentation.test.ts`: (a) a mixed corpus (fiat + a crypto card + a prepaid card) returns only `credit` cards in `recommendations` and the non-fiat cards in `cryptoSegment`; (b) cryptoSegment is ranked by hkdEquivalent desc with a null-rate crypto card appended last and unranked; (c) calling with NO rateTable returns undefined `cryptoSegment` and the same `recommendations` as before; (d) the fiat-only 11-card corpus output is unchanged.</action>
  <verify>
    <automated>npx vitest run src/lib/engine/__tests__/segmentation.test.ts src/lib/engine/__tests__/fiat-regression.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `recommendCards` accepts an optional 4th `rateTable?: HkdRateTable`; the 3 existing call sites (HomeClient, example.ts, integration-example.ts) still typecheck and behave identically
    - Fiat set is `cardType === 'credit'`; non-fiat (`crypto`, `prepaid`) go to `cryptoSegment`, never into `recommendations`
    - `cryptoSegment` ranks by `hkdEquivalent` desc; null-rate entries appended unranked (DEC-VAL-A)
    - `cryptoSegment` is undefined when no rateTable is passed or no non-fiat cards exist
    - The Plan 01 `fiat-regression.test.ts` snapshot is STILL byte-identical (no fiat perturbation)
    - `npx tsc --noEmit` passes; `npx vitest run <segmentation + fiat-regression>` passes
  </acceptance_criteria>
  <done>Crypto/non-fiat cards rank in their own `cryptoSegment` beside an unchanged fiat list; the byte-identical fiat guarantee holds structurally via partition-before-sort (CRY-04 / DEC-VAL-B / TECH-01).</done>
</task>

</tasks>

<artifacts_produced>
## Artifacts this plan produces
- Updated `recommendCards()` (`src/lib/engine/recommendCards.ts`): fail-closed `hkEligible` gate, partition-before-sort (`cardType === 'credit'` fiat set), `cryptoSegment` assembly via `valuateCrypto`, and the additive optional `rateTable` 4th param.
- `src/lib/engine/__tests__/hkEligible.test.ts` — gate coverage (false⇒excluded, undefined⇒included, 11 corpus intact).
- `src/lib/engine/__tests__/segmentation.test.ts` — partition, cryptoSegment ranking (incl. null appended), backward-compat, and fiat-unchanged coverage.
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| card data → recommender inclusion | `hkEligible` controls whether a card can be recommended; a gate bug could leak an HK-ineligible card into recommendations |
| caller → engine (rateTable) | The injected rate table drives crypto-segment ranking; bad rates must degrade (handled in Plan 02's valuateCrypto), never reorder or crash the fiat list |
| fiat pipeline ↔ crypto segment | Crypto must never enter the fiat comparator; a shared array reference or merged sort would perturb the fiat ranking |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-GATE | Elevation of Privilege | `hkEligible` gate in eligibility filter | mitigate | Fail-closed on `=== false` inside the base eligibility filter so an ineligible card reaches neither `recommendations` nor `cryptoSegment`; unit-tested (false⇒absent, undefined⇒present); keyed on `=== false` (not falsy) so undefined stays correctly eligible |
| T-07-REG | Tampering | fiat ranking under partition | mitigate | Partition-before-sort: only `cardType === 'credit'` cards enter the EXISTING comparator (unchanged); crypto built on a separate fresh array (never aliasing the fiat `.sort()` target, RESEARCH Pitfall 6); the Plan 01 byte-identical snapshot re-run in this task's verify catches any drift |
| T-07-SEG | Tampering | cryptoSegment ranking with bad rates | mitigate | Non-ranked null-rate entries appended (cannot rank on a missing number); the softer "value unavailable" policy (DEC-VAL-A) is safe ONLY because crypto is segmented out of the fiat answer here — this coupling is preserved by never merging crypto into `recommendations` (DEC-VAL-B) |
</threat_model>

<verification>
- `npx tsc --noEmit` passes (additive 4th param + optional field; 3 existing call sites unchanged).
- `npx vitest run src/lib/engine/__tests__/hkEligible.test.ts` passes (fail-closed gate).
- `npx vitest run src/lib/engine/__tests__/segmentation.test.ts` passes (partition + ranking + backward-compat).
- The Plan 01 `fiat-regression.test.ts` snapshot remains byte-identical.
- `npm test` full suite green; `npm run build` succeeds (HomeClient still compiles against the additive shape).
</verification>

<success_criteria>
- `hkEligible === false` cards are never returned by the recommender; `undefined` cards (the 11 legacy) still are (CRY-05, fail-closed).
- Crypto/non-fiat cards rank within `cryptoSegment` (DEC-VAL-B unit-segmented); stale⇒last-known+flag, null⇒appended unranked (DEC-VAL-A).
- The 11-card fiat ranking is byte-identical to the Plan 01 baseline (TECH-01, structural).
- No rateTable ⇒ fully backward-compatible (cryptoSegment absent, recommendations unchanged).
</success_criteria>

<output>
Create `.planning/phases/07-crypto-hkd-valuation-engine-hkeligible-gate/07-03-SUMMARY.md` when done.
</output>
