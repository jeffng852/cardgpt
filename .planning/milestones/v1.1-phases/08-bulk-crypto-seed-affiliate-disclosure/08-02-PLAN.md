---
phase: 08-bulk-crypto-seed-affiliate-disclosure
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/data/crypto-cards.fixture.json
  - scripts/lib/mergeCards.mjs
  - scripts/seed-crypto-cards.mjs
  - scripts/__tests__/seed-crypto-cards.test.ts
  - vitest.config.ts
autonomous: true
requirements: [D-01, D-09, D-10]

must_haves:
  truths:
    - "mergeCards(existing, incoming) is a pure function that appends only cards whose id is NOT already present and returns the merged array plus a count of appended cards (D-09)"
    - "An incoming card whose id already exists is skipped — the existing card object is returned untouched (idempotent re-run safety, never clobbers live admin edits) (D-09)"
    - "The 11 credit-card ids are never mutated or removed by a merge (D-09)"
    - "The seed script is merge-aware (read → merge-by-id → write-back → independent read-back verify) and supports --dry-run, modelled on backfill-card-type.mjs NOT seed-redis.mjs (D-09)"
    - "A small crypto fixture (cardType:'crypto', a shortName ticker, a minStaking-gated tier, hkEligible true AND false, a null-rate/unmapped-ticker asset) exists and drives machinery testing; the real bulk load stays deferred to RQ-001 (D-01/D-10)"
  artifacts:
    - src/data/crypto-cards.fixture.json
    - scripts/lib/mergeCards.mjs
    - scripts/seed-crypto-cards.mjs
    - scripts/__tests__/seed-crypto-cards.test.ts
    - vitest.config.ts
  key_links:
    - "seed-crypto-cards.mjs → mergeCards() (append-by-id) → whole cards object written back under the existing 'cards' key"
    - "seed script → Redis read → mergeCards → write → independent read-back verify (never trust the in-memory object)"
    - "vitest.config.ts include glob → collects scripts/__tests__/*.test.ts so the pure merge logic is covered by npm test"
---

<objective>
Build and test the merge-aware crypto seed machinery against a small fixture, without touching the 11 live credit cards. The real bulk load is deferred (RQ-001) — this plan proves the script and its pure merge logic; only the input data waits.

Two pieces:
1. A pure, unit-tested `mergeCards(existing, incoming)` helper (append-by-new-id, skip existing) extracted so the safety-critical logic is testable without a live Redis (D-09).
2. A `seed-crypto-cards.mjs` operator script cloned from `backfill-card-type.mjs` (read → merge → write-back → read-back-verify → --dry-run), plus a representative crypto fixture (D-01).

Purpose: DEC-DATA-001 / D-09 / D-10 — a merge that never clobbers live data or admin edits; D-01 — a fixture that drives all Phase 8 machinery testing.
Output: the fixture, the pure helper + its test, the seed script, and a vitest config that collects script tests.
</objective>

<execution_context>
@/Users/jeffreyng/.claude/gsd-core/workflows/execute-plan.md
@/Users/jeffreyng/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-bulk-crypto-seed-affiliate-disclosure/08-CONTEXT.md
@.planning/phases/08-bulk-crypto-seed-affiliate-disclosure/08-RESEARCH.md
@.planning/phases/08-bulk-crypto-seed-affiliate-disclosure/08-PATTERNS.md
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1 (tracer): Pure mergeCards helper — append-by-id, skip existing, never clobber</name>
  <files>scripts/lib/mergeCards.mjs, scripts/__tests__/seed-crypto-cards.test.ts, vitest.config.ts</files>
  <read_first>
    - scripts/backfill-card-type.mjs:79-102 — the merge-aware mutate-in-place + write-back skeleton to model. 08-PATTERNS.md:49-57 gives the exact append-by-id replacement for the mutate loop (skip when an id already exists, else push).
    - src/lib/engine/__tests__/valuateCrypto.test.ts — house vitest style: describe/it/expect, small typed factories, degrade-not-throw assertions. 08-PATTERNS.md:167 recommends testing an extracted pure helper here rather than a live Redis.
    - vitest.config.ts (repo root) — current `include: ['src/**/*.{test,spec}.{ts,tsx}']` does NOT collect anything under scripts/, so a scripts test would not run under `npm test` until the glob is broadened.
  </read_first>
  <behavior>
    mergeCards(existing, incoming) returns { cards, added }:
    - Appends an incoming card whose id is not in existing; increments added.
    - Skips an incoming card whose id already exists; the pre-existing object is left byte-identical (returns the original, does not overwrite from incoming).
    - Never removes or mutates any existing card (all 11 credit ids survive with identical objects).
    - Re-running mergeCards on its own output adds 0 (idempotent).
    - Empty incoming returns existing unchanged, added 0.
  </behavior>
  <action>Create `scripts/lib/mergeCards.mjs` exporting a pure `mergeCards(existing, incoming)` returning an object with the merged `cards` array and an `added` count, per the behavior — no Redis, no file I/O, no timestamps. RECORDED DECISION: broaden `vitest.config.ts` `include` to also collect script tests by adding a second glob entry matching test files under scripts/ (`.ts`/`.mts`/`.mjs`), leaving the existing `src/**` glob intact, so this pure logic runs under `npm test` and the explicit vitest-run command; this is why this plan owns vitest.config.ts. Write `scripts/__tests__/seed-crypto-cards.test.ts` importing mergeCards from `../lib/mergeCards.mjs`, covering every behavior above including the 11-id-survival and idempotent-re-run cases. Cite D-09 and the T-08-SEED threat id in the header comment.</action>
  <verify>
    <automated>npx vitest run scripts/__tests__/seed-crypto-cards.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run scripts/__tests__/seed-crypto-cards.test.ts` exits 0 (proves the broadened glob collects the file).
    - Test asserts: a new id is appended; an existing id is skipped with the original object preserved; an 11-id baseline all survive after merging a crypto card; a second merge of the same input adds 0.
    - `npm test` full suite still green (added glob does not break src/** collection or the Phase 7 snapshot).
  </acceptance_criteria>
  <done>The safety-critical merge logic is a pure, tested function that provably never clobbers existing cards; script tests are collected by vitest.</done>
</task>

<task type="auto">
  <name>Task 2: Crypto fixture + merge-aware seed script</name>
  <files>src/data/crypto-cards.fixture.json, scripts/seed-crypto-cards.mjs</files>
  <read_first>
    - scripts/backfill-card-type.mjs (full, 1-124) — clone the skeleton: credential resolution (lines 29-49, REAL_STORAGE_* → KV_* → UPSTASH_* precedence), `--dry-run` flag (line 27), read + refuse-if-empty guard (lines 60-75), timestamp + write-back (lines 96-102), independent read-back verify (lines 104-118). Do NOT use scripts/seed-redis.mjs (fresh-seed-only; would clobber).
    - src/data/cards.json — CardDatabase shape (`cards[]`, lastUpdated, version, metadata) and card keys (id, cardType, name, issuer, network, applyUrl, termsUrl, tags, isActive, lastUpdated, rewards, fees). Fixture cards mirror this shape.
    - src/types/card.ts:73 (RewardCondition.minStaking amount+asset), :251-253 (rewardPrograms.crypto name+shortName), :355 (hkEligible), :268-280 (rate table keyed by the crypto shortName ticker, EXACT casing — the fixture's tickers must match what the cron in 08-04 maps).
  </read_first>
  <action>Create `src/data/crypto-cards.fixture.json` in CardDatabase shape with 5 crypto cards (all cardType 'crypto', obviously-fixture issuers/names so they never read as authoritative — full provenance/DEC-DATA-001 labeling is the deferred real-load / Phase 9 DIR-02 concern): (a) a stablecoin card paying USDC (rewardPrograms.crypto.shortName "USDC"), hkEligible true, with an applyUrl; (b) a volatile card paying ETH (shortName "ETH"), hkEligible true, NO applyUrl (exercises the D-06 link-less path); (c) a staking-gated card with a base rule plus a bonus rule whose conditions.minStaking is { amount, asset "ETH" }, hkEligible true; (d) an hkEligible:false card (exercises the Phase 7 fail-closed gate); (e) a null-rate card paying a deliberately-unmapped ticker (shortName "NULLX") so valuateCrypto returns null. Keep each card to a SINGLE crypto reward asset (08-RESEARCH Pitfall 4 — no unit-mixing). The MAPPED tickers this fixture relies on are USDC and ETH — this is the ticker contract 08-04's CMC id→ticker map must cover; NULLX is intentionally left unmapped. Then create `scripts/seed-crypto-cards.mjs`: clone backfill-card-type.mjs, load the fixture cards, replace the mutate loop with `mergeCards(existing.cards, fixtureCards)` from `./lib/mergeCards.mjs`, refuse if the 'cards' key is empty/null (this is a merge not a fresh seed), skip the write when added is 0 or under --dry-run, and on write refresh lastUpdated + metadata.totalCards then run an independent read-back verify asserting every fixture id is now present AND the original credit ids still present. Public repo: credentials from env only, no secrets in the file. Header comment records that running against real prod Redis is the deferred RQ-001 operator step (--dry-run first; `vercel env pull` a fresh env first per the KV_* precedence gotcha).</action>
  <verify>
    <automated>node --check scripts/seed-crypto-cards.mjs && node --check scripts/lib/mergeCards.mjs && npx vitest run scripts/__tests__/seed-crypto-cards.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `node --check scripts/seed-crypto-cards.mjs` exits 0 (valid ESM, imports mergeCards; no secrets inlined).
    - `src/data/crypto-cards.fixture.json` parses and contains 5 cards, all cardType 'crypto', with at least one hkEligible:false, at least one hkEligible:true, one card carrying conditions.minStaking, and one card with shortName "NULLX".
    - The seed script calls mergeCards (never a blanket redis.set of the fixture over the whole key) and performs an independent read-back verify branch.
  </acceptance_criteria>
  <done>The fixture exists and covers every machinery case; the seed script merges by id, dry-runs, and read-back-verifies; the pure merge test is green.</done>
</task>

</tasks>

<artifacts>
## Artifacts this plan produces
- `src/data/crypto-cards.fixture.json` — NEW 5-card crypto fixture (D-01)
- `scripts/lib/mergeCards.mjs` — NEW pure `mergeCards(existing, incoming)` → { cards, added }
- `scripts/seed-crypto-cards.mjs` — NEW merge-aware operator seed script (append-by-id)
- `scripts/__tests__/seed-crypto-cards.test.ts` — NEW (D-09 Wave-0 gap)
- `vitest.config.ts` — EDIT (broaden include glob to collect scripts tests)
</artifacts>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator machine → production Upstash Redis | The seed script mutates the live `cards` key (append-by-id) using pulled prod credentials |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-SEED | Tampering | scripts/seed-crypto-cards.mjs writing prod Redis | high | mitigate | Append-by-id only via the pure, tested mergeCards (never a blanket overwrite); `--dry-run` first; independent read-back verify after write; operator-run, never wired into CI/deploy. Modelled on the proven backfill-card-type.mjs, not the clobbering seed-redis.mjs |
| T-08-SEED-KEY | Information Disclosure | Redis credentials in a public repo | high | mitigate | Credentials resolved only from env (REAL_STORAGE_* → KV_* → UPSTASH_*); no secret literal in the committed file; `vercel env pull` supplies them locally |
| T-08-SC | Tampering | npm dependencies | low | accept | No new package introduced this task; mergeCards + script use built-ins and the already-vetted @upstash/redis client |
</threat_model>

<verification>
- `npx vitest run scripts/__tests__/seed-crypto-cards.test.ts` — green
- `node --check scripts/seed-crypto-cards.mjs` and `node --check scripts/lib/mergeCards.mjs` — exit 0
- Fixture parses and covers all required cases (see Task 2 acceptance)
- Wave gate: `npm test` full suite green including the Phase 7 fiat snapshot
- Deferred manual (RQ-001 real load, per 08-VALIDATION Manual-Only): `--dry-run` then run + read-back verify against real prod Redis
</verification>

<success_criteria>
The merge logic is a pure, tested function that never clobbers the 11 credit cards or admin edits; the seed script is merge-aware, dry-run-capable, and read-back-verified; a representative crypto fixture drives all Phase 8 machinery testing. D-01/D-09/D-10 satisfied for the machinery (real data deferred to RQ-001).
</success_criteria>

<output>
Create `.planning/phases/08-bulk-crypto-seed-affiliate-disclosure/08-02-SUMMARY.md` when done.
</output>
