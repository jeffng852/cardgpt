---
phase: 08-bulk-crypto-seed-affiliate-disclosure
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/data/redisStorage.ts
  - src/app/[locale]/page.tsx
  - src/components/HomeClient.tsx
  - src/lib/data/__tests__/rateTable.test.ts
autonomous: true
requirements: [DEC-DATA-002, D-03, D-04]

must_haves:
  truths:
    - "A crypto→HKD rate table stored under the Redis 'crypto-rates' key is read server-side and injected into recommendCards() as its optional 4th argument, keyed by the asset shortName ticker in exact casing (DEC-DATA-002, D-03)"
    - "writeRateTableToRedis merges per-ticker into the existing table — a partial write never nulls a previously-good ticker's rate (D-04, 08-RESEARCH Pitfall 2)"
    - "readRateTableFromRedis returns null (never throws) on an absent key or Redis error, so the home page degrades to crypto-valued-null rather than 500ing (08-RESEARCH Pitfall 3)"
    - "rateTable is optional at every hop (page → HomeClient prop → 4th arg); a 3-arg fiat call stays byte-identical (Phase 7 additive contract)"
    - "cryptoSegment becomes populated when a rate table AND an eligible crypto card are present (the previously-inert engine goes live)"
  artifacts:
    - src/lib/data/redisStorage.ts
    - src/app/[locale]/page.tsx
    - src/components/HomeClient.tsx
    - src/lib/data/__tests__/rateTable.test.ts
  key_links:
    - "page.tsx → readRateTableFromRedis() → <HomeClient rateTable=...> prop → recommendCards(cards, tx, prefs, rateTable) → valuateCrypto"
    - "cron write path (08-04) → writeRateTableToRedis() (merge, never overwrite) → 'crypto-rates' key"
    - "readRateTableFromRedis error → null → engine values crypto as null (fail-safe, Phase 7)"
---

<objective>
Wire the rate-table consumption path end-to-end so Phase 7's inert crypto engine goes live: a rate table in Redis reaches `recommendCards()` and a crypto card is valued in HKD. This is the phase tracer.

Two pieces:
1. `readRateTableFromRedis()` + `writeRateTableToRedis()` in redisStorage.ts — clone of the existing `readCardsFromRedis`, against a new `crypto-rates` key, with a MERGE-not-overwrite write (D-04, Pitfall 2) and a null-safe read (Pitfall 3).
2. Thread an optional `rateTable` from `page.tsx` (server, force-dynamic) → `HomeClient` prop → the existing 3-arg `recommendCards()` call's 4th argument.

Purpose: DEC-DATA-002 (cron-refreshed rate table injected into recommendCards), D-03 (ticker-keyed, exact casing), D-04 (fail-safe, merge-not-overwrite). The engine itself is unchanged (Phase 7 locked contract) — this is pure wiring plus the storage API the 08-04 cron writes into.
Output: the rate-table Redis API, the server→client injection, and a storage+engine integration test.
</objective>

<execution_context>
@/Users/jeffreyng/.claude/gsd-core/workflows/execute-plan.md
@/Users/jeffreyng/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-bulk-crypto-seed-affiliate-disclosure/08-CONTEXT.md
@.planning/phases/08-bulk-crypto-seed-affiliate-disclosure/08-RESEARCH.md
@.planning/phases/08-bulk-crypto-seed-affiliate-disclosure/08-PATTERNS.md
@.planning/phases/07-crypto-hkd-valuation-engine-hkeligible-gate/07-VERIFICATION.md
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1 (tracer): rate-table Redis API + engine injection, proven end-to-end</name>
  <files>src/lib/data/redisStorage.ts, src/lib/data/__tests__/rateTable.test.ts</files>
  <read_first>
    - src/lib/data/redisStorage.ts:18 (CARDS_KEY const), :23-38 (lazy getRedis() singleton — reuse it, no new client), :65-81 (readCardsFromRedis — the exact clone target for readRateTableFromRedis), :87-106 (writeCardsToRedis — the write shape; but the rate write must MERGE per-ticker, not blanket-set).
    - src/types/card.ts:268-280 — HkdRate { hkdPerUnit, asOf } and HkdRateTable = Record<AssetSymbol, HkdRate>, keyed by the crypto shortName ticker in EXACT casing (D-03, no normalization at lookup).
    - src/lib/engine/recommendCards.ts (optional 4th param rateTable) + src/lib/engine/valuateCrypto.ts:51-69 — the guard `!rate || !(rate.hkdPerUnit > 0)` ⇒ hkdEquivalent null; stale (>24h) ⇒ last-known + rateStale. 07-VERIFICATION.md truths 1-2 confirm the additive contract and fail-safe.
    - src/lib/engine/__tests__/hkEligible.test.ts:46-60 and segmentation.test.ts — the cryptoCard() factory + how cryptoSegment is asserted; reuse this style to build the integration test's crypto card and rate table inline.
  </read_first>
  <behavior>
    readRateTableFromRedis():
    - Returns the stored HkdRateTable when the 'crypto-rates' key exists.
    - Returns null (never throws) when the key is absent or the Redis get rejects.
    writeRateTableToRedis(freshRates):
    - Reads the existing 'crypto-rates' table, spreads freshRates OVER it (per-ticker merge), writes the merged object back — a ticker present in the existing table but absent from freshRates keeps its previous rate (never nulled).
    - Writing an empty freshRates leaves the stored table unchanged.
    Integration (engine injection):
    - With an inline crypto card (USDC reward) and a rate table { USDC: { hkdPerUnit>0, asOf: now } }, recommendCards(cards, tx, prefs, rateTable) produces a cryptoSegment whose entry carries a numeric (non-null) hkdEquivalent.
    - With NO rate table (3-arg call), the same cards produce an undefined cryptoSegment and identical fiat recommendations (additive contract).
  </behavior>
  <action>Add `const RATES_KEY = 'crypto-rates'` and export `readRateTableFromRedis(): Promise<HkdRateTable | null>` (clone readCardsFromRedis: try/get/return-null-on-error, reuse getRedis()) and `writeRateTableToRedis(freshRates: HkdRateTable): Promise<{ success: boolean }>` that reads the existing table, merges freshRates over it per-ticker, and writes the merged object back (never a blanket set of only freshRates — this is the D-04 / Pitfall 2 protection, mirroring backfill-card-type.mjs's read-merge-write). Import HkdRateTable from '@/types/card'. Write `src/lib/data/__tests__/rateTable.test.ts` covering every behavior above — mock the Redis client (vi.mock the @upstash/redis module or inject a fake get/set) for the read/merge cases, and call the REAL recommendCards for the injection case. Cite DEC-DATA-002 / D-03 / D-04 and T-08-RATE in the header.</action>
  <verify>
    <automated>npx vitest run src/lib/data/__tests__/rateTable.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/lib/data/__tests__/rateTable.test.ts` exits 0.
    - A test asserts a pre-existing ticker survives a partial write (merge, not overwrite).
    - A test asserts readRateTableFromRedis returns null on a rejected Redis get (no throw).
    - A test calls the real recommendCards with a rate table and asserts a non-null hkdEquivalent in cryptoSegment; and a 3-arg call yields undefined cryptoSegment.
  </acceptance_criteria>
  <done>The rate-table storage API is merge-safe and null-safe, and an injected rate table provably values a crypto card in HKD through the real engine.</done>
</task>

<task type="auto">
  <name>Task 2: Thread rateTable from server page to the engine call</name>
  <files>src/app/[locale]/page.tsx, src/components/HomeClient.tsx</files>
  <read_first>
    - src/app/[locale]/page.tsx (full, 1-19) — server component, `export const dynamic = 'force-dynamic'`, awaits loadCards() and returns <HomeClient cards={cards} />. Add a second server-side read.
    - src/components/HomeClient.tsx:24-26 (HomeClientProps interface), :46-50 (the existing 3-arg recommendCards(cards, result.transaction, preferences) call to extend). 08-PATTERNS.md:182 confirms this is the only change in this file.
    - 08-RESEARCH.md Pattern 2 (lines 232-289) — the exact server-read + prop + 4th-arg wiring; Pitfall 3 (lines 407-412) — rateTable must be optional at every hop.
  </read_first>
  <action>In page.tsx, import readRateTableFromRedis from '@/lib/data/redisStorage', await it into a `rateTable` const alongside `cards`, and pass it as a new prop: <HomeClient cards={cards} rateTable={rateTable} />. In HomeClient.tsx, add `rateTable?: HkdRateTable` to HomeClientProps (import the type from '@/types/card'), accept it in the destructured props, and pass it as the 4th positional argument to the existing recommendCards call. Keep it OPTIONAL everywhere (mirrors recommendCards' own optional 4th param); change no other logic. Do NOT render cryptoSegment — its UI is deferred to a later phase; this task only makes the data non-inert.</action>
  <verify>
    <automated>npx tsc --noEmit && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` exits 0 (optional prop + 4th arg typecheck).
    - `npm run build` compiles successfully (page.tsx server-reads the rate table; HomeClient threads it).
    - rateTable is typed optional in HomeClientProps and passed as the 4th arg to recommendCards; no cryptoSegment rendering is added.
  </acceptance_criteria>
  <done>The server reads the rate table and threads it to the engine; the app builds; the crypto valuation path is live end-to-end (rendering deferred).</done>
</task>

</tasks>

<artifacts>
## Artifacts this plan produces
- `src/lib/data/redisStorage.ts` — EDIT: new `RATES_KEY`, `readRateTableFromRedis()`, `writeRateTableToRedis()` (merge-aware)
- `src/app/[locale]/page.tsx` — EDIT: server read + `rateTable` prop
- `src/components/HomeClient.tsx` — EDIT: optional `rateTable` prop + 4th `recommendCards` arg
- `src/lib/data/__tests__/rateTable.test.ts` — NEW (rate storage merge/null-safe + engine-injection integration)
</artifacts>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Redis → server (SSR read) → client prop | The 'crypto-rates' table is read server-side and serialized to the client as a prop; a poisoned or absent rate must never crash the page or fabricate a value |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-RATE | Tampering | writeRateTableToRedis / valuation path | high | mitigate | Per-ticker MERGE write (never blanket-overwrite) preserves last-known-good rates on a partial refresh (D-04); valuateCrypto's existing `!(hkdPerUnit > 0)` guard (Phase 7) is the second backstop, so a bad rate yields value-unavailable, never a fabricated number. Fetch-boundary rejection of non-positive prices lives in 08-04 |
| T-08-RATE-AVAIL | Denial of Service | readRateTableFromRedis on the home page | medium | mitigate | Read returns null (never throws) on absent key/Redis error; rateTable optional at every hop → page degrades to crypto-valued-null instead of 500 (Pitfall 3) |
| T-08-SC | Tampering | npm dependencies | low | accept | No new package; reuses the installed @upstash/redis client and existing getRedis() singleton |
</threat_model>

<verification>
- `npx vitest run src/lib/data/__tests__/rateTable.test.ts` — green (merge, null-safe, engine injection)
- `npx tsc --noEmit` exits 0; `npm run build` compiles
- Wave gate: `npm test` full suite green including the Phase 7 fiat snapshot (3-arg callers byte-identical)
- Deferred manual (08-VALIDATION Manual-Only): with a real rate table in Redis, confirm a crypto card renders an HKD value in the browser (rendering itself is a later UI phase)
</verification>

<success_criteria>
A rate table stored in Redis is read server-side and injected into recommendCards, valuing a crypto card in HKD; the write is merge-safe and the read is null-safe; the additive 3-arg fiat contract stays byte-identical. DEC-DATA-002 / D-03 / D-04 satisfied for the consumption path.
</success_criteria>

<output>
Create `.planning/phases/08-bulk-crypto-seed-affiliate-disclosure/08-03-SUMMARY.md` when done.
</output>
